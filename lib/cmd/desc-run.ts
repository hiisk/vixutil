/**
 * proc·net·archive 명령 78개의 한 줄 설명 — 열 언어.
 *
 * 명령 이름과 옵션은 옮기지 않는다. `kill -9`는 어느 나라 터미널에서도 `kill -9`이고
 * 번역하면 오히려 그 나라 사람이 검색하는 말과 멀어진다. 옮기는 것은 "이 명령이
 * 무엇을 하고 사람들이 무엇을 틀리는가" 한 문장뿐이다.
 *
 * 열 칸을 배열로 적는다 — 순서는 ko·en·es·pt·ja·de·fr·hi·zh·tw.
 * 아홉 칸에는 한글이 없어야 하고, 명령·옵션·경로는 열 언어 모두 ASCII로 둔다.
 */
import type { Ten } from './types.ts';

export const CMD_DESC_RUN: Record<string, Ten> = {
  /* ───────── proc — 프로세스·시스템 ───────── */

  ps: [
    '지금 이 순간 돌고 있는 프로세스를 한 장으로 찍어 보여줍니다 — `ps aux`는 대시 없는 BSD 꼴이고 `ps -ef`는 POSIX 꼴이라, 두 꼴을 섞어 쓰는 것이 가장 흔한 실수입니다.',
    'Takes a one-off snapshot of the running processes; `ps aux` is the BSD spelling with no dash and `ps -ef` the POSIX one, and mixing the two is the usual mistake.',
    'Toma una foto puntual de los procesos en marcha; `ps aux` es la forma BSD sin guion y `ps -ef` la POSIX, y mezclar ambas es el error habitual.',
    'Tira uma foto instantanea dos processos em execucao; `ps aux` e a forma BSD sem hifen e `ps -ef` a POSIX, e misturar as duas e o erro mais comum.',
    'いま動いているプロセスを一度だけ写し取ります。`ps aux` はダッシュのない BSD 形式、`ps -ef` は POSIX 形式で、この二つを混ぜるのがよくある間違いです。',
    'Zeigt eine Momentaufnahme der laufenden Prozesse; `ps aux` ist die BSD-Form ohne Bindestrich, `ps -ef` die POSIX-Form, und beide zu mischen ist der typische Fehler.',
    "Prend un instantane des processus en cours ; `ps aux` est la forme BSD sans tiret et `ps -ef` la forme POSIX, et melanger les deux est l'erreur classique.",
    'चल रही प्रक्रियाओं का एक बार का स्नैपशॉट देता है; `ps aux` बिना डैश वाला BSD रूप है और `ps -ef` POSIX रूप, और दोनों को मिलाना आम गलती है।',
    '把此刻正在运行的进程拍成一张快照；`ps aux` 是不带减号的 BSD 写法，`ps -ef` 是 POSIX 写法，把两种混着用是最常见的错误。',
    '把此刻正在執行的行程拍成一張快照；`ps aux` 是不帶減號的 BSD 寫法，`ps -ef` 是 POSIX 寫法，把兩種混著用是最常見的錯誤。',
  ],

  top: [
    '프로세스 목록을 몇 초마다 새로 그려 보여줍니다 — 옵션이 Linux와 macOS에서 아예 달라서, 한 번만 찍으려면 Linux는 `-b -n 1`, macOS는 `-l 1`입니다.',
    'Redraws the process list every few seconds; its flags differ completely between platforms, so a single snapshot is `-b -n 1` on Linux and `-l 1` on macOS.',
    'Redibuja la lista de procesos cada pocos segundos; sus opciones cambian por completo segun la plataforma: una sola muestra es `-b -n 1` en Linux y `-l 1` en macOS.',
    'Redesenha a lista de processos a cada poucos segundos; as opcoes mudam completamente entre plataformas: uma amostra unica e `-b -n 1` no Linux e `-l 1` no macOS.',
    'プロセス一覧を数秒ごとに描き直します。オプションが Linux と macOS でまったく違い、一回だけ取るなら Linux は `-b -n 1`、macOS は `-l 1` です。',
    'Zeichnet die Prozessliste alle paar Sekunden neu; die Optionen unterscheiden sich je Plattform vollstandig — eine einzelne Momentaufnahme ist `-b -n 1` unter Linux und `-l 1` unter macOS.',
    "Redessine la liste des processus toutes les quelques secondes ; ses options diffèrent totalement selon la plateforme : un seul releve s'obtient avec `-b -n 1` sous Linux et `-l 1` sous macOS.",
    'हर कुछ सेकंड में प्रक्रिया सूची फिर से बनाता है; इसके फ़्लैग प्लेटफ़ॉर्म के अनुसार पूरी तरह अलग हैं — एक बार का स्नैपशॉट Linux पर `-b -n 1` और macOS पर `-l 1` है।',
    '每隔几秒重画一次进程列表；它的选项在 Linux 和 macOS 上完全不同，只取一次要用 Linux 的 `-b -n 1` 或 macOS 的 `-l 1`。',
    '每隔幾秒重畫一次行程清單；它的選項在 Linux 和 macOS 上完全不同，只取一次要用 Linux 的 `-b -n 1` 或 macOS 的 `-l 1`。',
  ],

  htop: [
    'top을 색과 마우스로 다시 만든 것이라 F9로 바로 신호를 보낼 수 있지만, 기본으로 깔려 있지 않아 `apt install htop`이나 `brew install htop`이 먼저입니다.',
    'A colourful, mouse-aware rewrite of top where F9 sends a signal directly, but it is not installed by default, so `apt install htop` or `brew install htop` comes first.',
    'Una reescritura de top con color y raton donde F9 envia una senal directamente, pero no viene instalado: primero `apt install htop` o `brew install htop`.',
    'Uma reescrita de top com cor e mouse onde F9 envia um sinal direto, mas nao vem instalado: primeiro `apt install htop` ou `brew install htop`.',
    'top を色とマウス操作で作り直したもので、F9 でそのままシグナルを送れますが、既定では入っていないので先に `apt install htop` か `brew install htop` が必要です。',
    'Eine farbige, mausfahige Neufassung von top, in der F9 direkt ein Signal sendet — sie ist aber nicht vorinstalliert, also zuerst `apt install htop` oder `brew install htop`.',
    "Une reecriture de top en couleurs et compatible souris, ou F9 envoie directement un signal, mais elle n'est pas installee par defaut : `apt install htop` ou `brew install htop` d'abord.",
    'top का रंगीन, माउस-समर्थित नया रूप जिसमें F9 सीधे सिग्नल भेजता है, पर यह डिफ़ॉल्ट रूप से नहीं आता — पहले `apt install htop` या `brew install htop` चाहिए।',
    '带颜色和鼠标支持的 top 重写版，F9 就能直接发信号，但系统默认不带，得先 `apt install htop` 或 `brew install htop`。',
    '帶顏色和滑鼠支援的 top 重寫版，F9 就能直接發訊號，但系統預設不帶，得先 `apt install htop` 或 `brew install htop`。',
  ],

  kill: [
    'PID에 신호를 보내 프로세스를 끝냅니다 — 기본 TERM은 정리할 틈을 주지만 `-9`는 막을 수 없어서, 열어둔 파일과 잠금 파일이 그대로 남습니다.',
    'Sends a signal to a PID to shut a process down; the default TERM lets it clean up, while `-9` cannot be caught, so open files and lock files are left behind.',
    'Envia una senal a un PID para cerrar un proceso; el TERM por defecto le deja limpiar, mientras que `-9` no se puede interceptar y deja atras archivos abiertos y bloqueos.',
    'Envia um sinal a um PID para encerrar um processo; o TERM padrao deixa ele limpar, enquanto `-9` nao pode ser capturado e deixa arquivos abertos e locks para tras.',
    'PID にシグナルを送ってプロセスを終わらせます。既定の TERM は片付ける余裕を与えますが、`-9` は捕まえられないため、開いたファイルやロックファイルが残ります。',
    'Sendet ein Signal an eine PID, um einen Prozess zu beenden; das voreingestellte TERM lasst ihm Zeit zum Aufraumen, `-9` kann nicht abgefangen werden und hinterlasst offene Dateien und Sperrdateien.',
    "Envoie un signal a un PID pour arreter un processus ; le TERM par defaut lui laisse le temps de faire le menage, alors que `-9` ne peut pas etre intercepte et laisse fichiers ouverts et verrous derriere lui.",
    'किसी PID को सिग्नल भेजकर प्रक्रिया बंद करता है; डिफ़ॉल्ट TERM उसे सफ़ाई का मौका देता है, जबकि `-9` को रोका नहीं जा सकता, इसलिए खुली फ़ाइलें और लॉक फ़ाइलें पीछे रह जाती हैं।',
    '给指定 PID 发信号让进程退出；默认的 TERM 会留出清理时间，而 `-9` 无法被拦截，打开的文件和锁文件会留在原地。',
    '給指定 PID 發訊號讓行程結束；預設的 TERM 會留出清理時間，而 `-9` 無法被攔截，開著的檔案和鎖檔會留在原地。',
  ],

  killall: [
    'PID이 아니라 이름으로 프로세스를 한꺼번에 끝냅니다 — 실행 파일 이름과 정확히 맞아야 해서 `killall chrome`은 "Google Chrome"을 놓칩니다.',
    'Kills processes by name rather than by PID; the name must match the executable exactly, so `killall chrome` misses a process called "Google Chrome".',
    'Mata procesos por nombre en lugar de por PID; el nombre debe coincidir exactamente con el ejecutable, asi que `killall chrome` no alcanza a "Google Chrome".',
    'Mata processos por nome em vez de por PID; o nome precisa bater exatamente com o executavel, entao `killall chrome` nao pega um processo chamado "Google Chrome".',
    'PID ではなく名前でプロセスをまとめて終わらせます。実行ファイル名と厳密に一致する必要があるため、`killall chrome` は "Google Chrome" を取りこぼします。',
    'Beendet Prozesse anhand des Namens statt der PID; der Name muss genau der ausfuhrbaren Datei entsprechen, daher trifft `killall chrome` einen Prozess namens "Google Chrome" nicht.',
    "Tue les processus par leur nom plutot que par PID ; le nom doit correspondre exactement a l'executable, si bien que `killall chrome` rate un processus nomme \"Google Chrome\".",
    'PID की जगह नाम से प्रक्रियाएँ मारता है; नाम को executable से पूरा मेल खाना चाहिए, इसलिए `killall chrome` "Google Chrome" नाम की प्रक्रिया छोड़ देता है।',
    '按名字而不是 PID 一次结束多个进程；名字必须和可执行文件完全一致，所以 `killall chrome` 抓不到叫 "Google Chrome" 的进程。',
    '按名字而不是 PID 一次結束多個行程；名字必須和執行檔完全一致，所以 `killall chrome` 抓不到叫 "Google Chrome" 的行程。',
  ],

  pkill: [
    '이름 조각으로 맞는 프로세스를 모두 끝냅니다 — `-f` 없이는 명령 이름만 견주고 넓은 조각은 엉뚱한 것까지 죽이니, 같은 조각으로 pgrep을 먼저 돌려 봅니다.',
    'Kills every process matching a pattern; without `-f` only the process name is compared, and a loose pattern takes down more than you meant, so run pgrep with the same pattern first.',
    'Mata todos los procesos que coinciden con un patron; sin `-f` solo se compara el nombre, y un patron amplio se lleva mas de lo previsto: prueba antes pgrep con el mismo patron.',
    'Mata todos os processos que casam com um padrao; sem `-f` apenas o nome e comparado, e um padrao amplo derruba mais do que voce queria: rode pgrep com o mesmo padrao antes.',
    'パターンに一致するプロセスをすべて終わらせます。`-f` を付けないとコマンド名だけを見るうえ、広すぎるパターンは思わぬものまで殺すので、同じパターンで先に pgrep を試します。',
    'Beendet alle Prozesse, die zu einem Muster passen; ohne `-f` wird nur der Prozessname verglichen, und ein zu weites Muster erwischt mehr als beabsichtigt — daher zuerst pgrep mit demselben Muster.',
    "Tue tous les processus correspondant a un motif ; sans `-f`, seul le nom est compare, et un motif trop large emporte plus que prevu : lancez d'abord pgrep avec le meme motif.",
    'पैटर्न से मेल खाती हर प्रक्रिया को मारता है; `-f` के बिना केवल नाम की तुलना होती है, और ढीला पैटर्न ज़रूरत से ज़्यादा मार देता है — इसलिए पहले उसी पैटर्न से pgrep चलाएँ।',
    '把匹配某个模式的进程全部杀掉；不加 `-f` 只比对进程名，而模式写得太宽会连带杀掉别的，所以先用同样的模式跑一遍 pgrep。',
    '把符合某個模式的行程全部殺掉；不加 `-f` 只比對行程名稱，而模式寫得太寬會連帶殺掉別的，所以先用同樣的模式跑一遍 pgrep。',
  ],

  pgrep: [
    'pkill과 똑같은 규칙으로 맞는 프로세스의 PID만 찍어 줍니다 — 그래서 죽이기 전에 무엇이 걸리는지 확인하는 연습 삼기에 딱 맞습니다.',
    'Prints just the PIDs a pattern matches, using exactly the same rules as pkill, which makes it the dry run to do before you kill anything.',
    'Solo imprime los PID que coinciden con un patron, con las mismas reglas que pkill, asi que sirve de ensayo antes de matar nada.',
    'Imprime apenas os PIDs que casam com o padrao, com as mesmas regras do pkill, servindo de ensaio antes de matar qualquer coisa.',
    'pkill とまったく同じ規則で、一致するプロセスの PID だけを表示します。だから何かを殺す前の下見にちょうど向いています。',
    'Gibt nur die PIDs aus, die zu einem Muster passen, und zwar nach genau denselben Regeln wie pkill — der Probelauf, bevor man etwas beendet.',
    "N'affiche que les PID correspondant a un motif, selon les memes regles que pkill : c'est le test a blanc a faire avant de tuer quoi que ce soit.",
    'पैटर्न से मेल खाने वाले सिर्फ़ PID छापता है, ठीक pkill जैसे ही नियमों से — यानी कुछ मारने से पहले का अभ्यास।',
    '用和 pkill 完全相同的规则，只打印匹配到的 PID，所以它正好是动手杀之前的试运行。',
    '用和 pkill 完全相同的規則，只印出符合的 PID，所以它正好是動手殺之前的試跑。',
  ],

  jobs: [
    '이 셸이 띄운 배경 작업만 번호를 붙여 보여주는 셸 내장 명령이라, 다른 터미널의 작업은 보이지 않고 번호는 PID가 아니라 `%1` 꼴로 씁니다.',
    'A shell builtin that numbers the background jobs of this shell only, so jobs from another terminal never appear and you refer to them as `%1`, not by PID.',
    'Un builtin del shell que numera solo los trabajos en segundo plano de este shell: los de otra terminal no aparecen y se referencian como `%1`, no por PID.',
    'Um builtin do shell que numera apenas os trabalhos em segundo plano deste shell: os de outro terminal nao aparecem e voce os referencia como `%1`, nao por PID.',
    'このシェルが起こしたバックグラウンドのジョブだけに番号を付けて示すシェル組み込みで、別の端末のジョブは出てこず、指すときは PID ではなく `%1` の形を使います。',
    'Ein Shell-Builtin, das nur die Hintergrundjobs dieser Shell nummeriert; Jobs aus einem anderen Terminal erscheinen nie, und man spricht sie als `%1` an, nicht per PID.',
    "Une commande interne au shell qui numerote uniquement les taches d'arriere-plan de ce shell : celles d'un autre terminal n'apparaissent jamais et on les designe par `%1`, pas par PID.",
    'शेल का अंतर्निहित आदेश जो केवल इसी शेल के बैकग्राउंड जॉब गिनता है, इसलिए दूसरे टर्मिनल के जॉब कभी नहीं दिखते और उन्हें PID से नहीं, `%1` की तरह बताया जाता है।',
    '这是 shell 内建命令，只给当前 shell 的后台任务编号，别的终端里的任务不会出现，引用时写 `%1` 而不是 PID。',
    '這是 shell 內建命令，只給目前 shell 的背景工作編號，別的終端機裡的工作不會出現，引用時寫 `%1` 而不是 PID。',
  ],

  bg: [
    'Ctrl+Z로 멈춘 작업을 배경에서 다시 돌립니다 — 여전히 이 셸에 매달린 작업이라, 터미널을 닫으면 함께 죽습니다(살리려면 nohup이나 disown).',
    'Resumes a job you stopped with Ctrl+Z, in the background; it still belongs to this shell, so closing the terminal takes it with you unless you used nohup or disown.',
    'Reanuda en segundo plano un trabajo detenido con Ctrl+Z; sigue perteneciendo a este shell, asi que cerrar la terminal se lo lleva salvo que uses nohup o disown.',
    'Retoma em segundo plano um trabalho parado com Ctrl+Z; ele continua pertencendo a este shell, entao fechar o terminal o leva embora, a menos que use nohup ou disown.',
    'Ctrl+Z で止めたジョブをバックグラウンドで再開します。まだこのシェルに属しているので、端末を閉じると一緒に終わります(残すなら nohup か disown)。',
    'Setzt einen mit Ctrl+Z angehaltenen Job im Hintergrund fort; er gehort weiter zu dieser Shell, das Schliessen des Terminals nimmt ihn also mit — ausser bei nohup oder disown.',
    "Reprend en arriere-plan une tache arretee par Ctrl+Z ; elle appartient toujours a ce shell, donc fermer le terminal l'emporte, sauf avec nohup ou disown.",
    'Ctrl+Z से रोके गए जॉब को बैकग्राउंड में फिर चलाता है; वह अब भी इसी शेल का है, इसलिए टर्मिनल बंद करने पर साथ चला जाता है — जब तक nohup या disown न हो।',
    '把用 Ctrl+Z 停住的任务放回后台继续跑；它仍然属于当前 shell，关掉终端就一起结束，除非用了 nohup 或 disown。',
    '把用 Ctrl+Z 停住的工作放回背景繼續跑；它仍然屬於目前 shell，關掉終端機就一起結束，除非用了 nohup 或 disown。',
  ],

  fg: [
    '배경이나 멈춘 상태의 작업을 다시 앞으로 끌어옵니다 — 이 셸의 작업에만 듣고, 가리킬 때는 PID가 아니라 `%1` 같은 작업 번호를 씁니다.',
    'Brings a background or stopped job back to the terminal; it only works on jobs of this shell, and you name them with a job number like `%1`, not a PID.',
    'Trae de vuelta a la terminal un trabajo detenido o en segundo plano; solo funciona con trabajos de este shell y se indican con un numero como `%1`, no con el PID.',
    'Traz de volta ao terminal um trabalho parado ou em segundo plano; funciona apenas com trabalhos deste shell, indicados por um numero como `%1`, nao pelo PID.',
    'バックグラウンドや停止中のジョブを手前に引き戻します。このシェルのジョブにしか効かず、指定は PID ではなく `%1` のようなジョブ番号です。',
    'Holt einen Hintergrund- oder angehaltenen Job zuruck ins Terminal; funktioniert nur bei Jobs dieser Shell, und man nennt sie mit einer Jobnummer wie `%1`, nicht mit der PID.',
    "Ramene au premier plan une tache d'arriere-plan ou arretee ; ne fonctionne que pour les taches de ce shell, designees par un numero comme `%1` et non par un PID.",
    'बैकग्राउंड या रुके हुए जॉब को टर्मिनल में वापस लाता है; यह केवल इसी शेल के जॉब पर चलता है और उन्हें PID से नहीं, `%1` जैसे जॉब नंबर से बताया जाता है।',
    '把后台或已暂停的任务拉回终端前台；只对当前 shell 的任务有效，指定时用 `%1` 这样的任务号，而不是 PID。',
    '把背景或已暫停的工作拉回終端機前景；只對目前 shell 的工作有效，指定時用 `%1` 這樣的工作號，而不是 PID。',
  ],

  nohup: [
    'HUP 신호를 무시하게 만들어 로그아웃 뒤에도 명령이 살아 있게 합니다 — 배경으로 보내주지는 않으니 `&`는 직접 붙여야 하고, 출력은 조용히 ./nohup.out에 쌓입니다.',
    'Makes a command ignore the hangup signal so it survives logout; it does not background anything, so you still add `&` yourself, and output piles up quietly in ./nohup.out.',
    'Hace que un comando ignore la senal de cierre para sobrevivir al cierre de sesion; no lo manda a segundo plano, asi que el `&` lo pones tu, y la salida se acumula en ./nohup.out.',
    'Faz um comando ignorar o sinal de hangup para sobreviver ao logout; ele nao coloca nada em segundo plano, entao voce mesmo acrescenta `&`, e a saida acumula em ./nohup.out.',
    'HUP シグナルを無視させて、ログアウト後もコマンドが生き続けるようにします。バックグラウンドには回してくれないので `&` は自分で付け、出力は静かに ./nohup.out にたまります。',
    'Lasst einen Befehl das Hangup-Signal ignorieren, damit er das Abmelden ubersteht; er wird dabei nicht in den Hintergrund geschickt — das `&` setzen Sie selbst, und die Ausgabe sammelt sich still in ./nohup.out.',
    "Fait ignorer le signal de raccrochage a une commande pour qu'elle survive a la deconnexion ; elle n'est pas mise en arriere-plan pour autant, le `&` reste a votre charge, et la sortie s'accumule discretement dans ./nohup.out.",
    'किसी आदेश को hangup सिग्नल नज़रअंदाज़ कराता है ताकि वह लॉगआउट के बाद भी चले; यह उसे बैकग्राउंड में नहीं भेजता, `&` आपको खुद जोड़ना पड़ता है, और आउटपुट चुपचाप ./nohup.out में जमा होता है।',
    '让命令忽略挂断信号，这样退出登录后它还在跑；它并不会帮你放到后台，`&` 得自己加，输出会悄悄堆在 ./nohup.out 里。',
    '讓命令忽略掛斷訊號，這樣登出後它還在跑；它並不會幫你放到背景，`&` 得自己加，輸出會悄悄堆在 ./nohup.out 裡。',
  ],

  nice: [
    'CPU 우선순위를 낮춰 명령을 시작합니다 — 값은 19(가장 양보)에서 -20(가장 욕심)까지고 음수는 root만 되며, 디스크가 아니라 CPU에만 듣습니다(디스크는 ionice).',
    'Starts a command with a lower CPU priority; the value runs from 19 (most yielding) to -20 (greediest), negative values need root, and it affects only CPU, not disk (that is ionice).',
    'Arranca un comando con menor prioridad de CPU; el valor va de 19 (mas cede) a -20 (mas acapara), los negativos requieren root y solo afecta a la CPU, no al disco (eso es ionice).',
    'Inicia um comando com prioridade de CPU menor; o valor vai de 19 (mais cede) a -20 (mais ganancioso), negativos exigem root, e afeta apenas CPU, nao disco (isso e ionice).',
    'CPU の優先度を下げてコマンドを始めます。値は 19(最も譲る)から -20(最も欲張り)までで、負の値は root だけ、効くのはディスクではなく CPU だけです(ディスクは ionice)。',
    'Startet einen Befehl mit niedrigerer CPU-Prioritat; der Wert reicht von 19 (am nachgiebigsten) bis -20 (am gierigsten), negative Werte brauchen root, und es wirkt nur auf die CPU, nicht auf die Platte (dafur ionice).',
    "Lance une commande avec une priorite CPU plus faible ; la valeur va de 19 (le plus cedant) a -20 (le plus gourmand), les valeurs negatives exigent root, et cela n'agit que sur le CPU, pas sur le disque (c'est ionice).",
    'किसी आदेश को कम CPU प्राथमिकता के साथ शुरू करता है; मान 19 (सबसे उदार) से -20 (सबसे लालची) तक है, ऋणात्मक मान के लिए root चाहिए, और यह केवल CPU पर असर करता है, डिस्क पर नहीं (उसके लिए ionice)।',
    '以更低的 CPU 优先级启动命令；取值从 19（最谦让）到 -20（最抢），负值要 root，而且它只影响 CPU 不影响磁盘（磁盘用 ionice）。',
    '以較低的 CPU 優先權啟動命令；取值從 19（最謙讓）到 -20（最搶），負值要 root，而且它只影響 CPU 不影響磁碟（磁碟用 ionice）。',
  ],

  renice: [
    '이미 돌고 있는 프로세스의 우선순위를 바꿉니다 — 보통 사용자는 더 양보하게만 만들 수 있고, 한 번 낮춘 것을 되돌리려면 root가 필요합니다.',
    'Changes the priority of a process that is already running; an ordinary user can only make it nicer, and taking the priority back again needs root.',
    'Cambia la prioridad de un proceso ya en marcha; un usuario normal solo puede hacerlo mas cedente, y recuperar la prioridad exige root.',
    'Muda a prioridade de um processo que ja esta rodando; um usuario comum so consegue torna-lo mais cedente, e recuperar a prioridade exige root.',
    'すでに動いているプロセスの優先度を変えます。一般利用者は譲る方向にしか動かせず、下げた分を戻すには root が必要です。',
    'Andert die Prioritat eines bereits laufenden Prozesses; ein normaler Benutzer kann ihn nur nachgiebiger machen, das Zurucknehmen erfordert root.',
    "Modifie la priorite d'un processus deja lance ; un utilisateur ordinaire ne peut que le rendre plus cedant, et revenir en arriere demande root.",
    'पहले से चल रही प्रक्रिया की प्राथमिकता बदलता है; सामान्य उपयोगकर्ता उसे केवल अधिक उदार बना सकता है, प्राथमिकता वापस लेने के लिए root चाहिए।',
    '修改已经在运行的进程的优先级；普通用户只能把它调得更谦让，想调回来需要 root。',
    '修改已經在執行的行程的優先權；一般使用者只能把它調得更謙讓，想調回來需要 root。',
  ],

  systemctl: [
    'systemd 서비스를 켜고 끄고 상태를 봅니다 — `enable`은 부팅 때만 정하는 것이라 지금도 켜려면 `enable --now`이고, 유닛 파일을 고친 뒤에는 `daemon-reload`가 필요합니다.',
    'Starts, stops and inspects systemd services; `enable` only sets boot behaviour, so use `enable --now` to also start it, and run `daemon-reload` after editing a unit file.',
    'Arranca, detiene e inspecciona servicios de systemd; `enable` solo fija el arranque, asi que usa `enable --now` para iniciarlo tambien, y ejecuta `daemon-reload` tras editar un unit.',
    'Inicia, para e inspeciona servicos do systemd; `enable` so define o comportamento no boot, entao use `enable --now` para iniciar tambem, e rode `daemon-reload` depois de editar um unit.',
    'systemd のサービスを起こし、止め、状態を見ます。`enable` は起動時の設定だけなので今も動かすなら `enable --now`、ユニットファイルを直したあとは `daemon-reload` が必要です。',
    'Startet, stoppt und prüft systemd-Dienste; `enable` legt nur das Verhalten beim Booten fest, fur den Sofortstart also `enable --now`, und nach dem Bearbeiten einer Unit-Datei `daemon-reload` ausfuhren.',
    "Demarre, arrete et inspecte les services systemd ; `enable` ne regle que le demarrage au boot, utilisez donc `enable --now` pour lancer aussi, et lancez `daemon-reload` apres avoir modifie un fichier unit.",
    'systemd सेवाओं को शुरू, बंद और जाँचता है; `enable` केवल बूट व्यवहार तय करता है, इसलिए अभी भी चालू करने के लिए `enable --now` लें, और unit फ़ाइल बदलने के बाद `daemon-reload` चलाएँ।',
    '启动、停止和查看 systemd 服务；`enable` 只决定开机行为，想现在也启动要用 `enable --now`，改过 unit 文件后必须 `daemon-reload`。',
    '啟動、停止和查看 systemd 服務；`enable` 只決定開機行為，想現在也啟動要用 `enable --now`，改過 unit 檔後必須 `daemon-reload`。',
  ],

  journalctl: [
    'systemd가 이진 형식으로 모아 둔 로그를 읽습니다 — /var/log/journal이 없으면 재부팅 때 로그가 사라지고, `-u`에는 유닛 이름을 정확히 적어야 합니다.',
    "Reads the binary log systemd collects; without /var/log/journal the log is wiped at reboot, and `-u` needs the unit's exact name.",
    'Lee el registro binario que recopila systemd; sin /var/log/journal el registro se borra al reiniciar, y `-u` exige el nombre exacto del unit.',
    'Le o log binario que o systemd coleta; sem /var/log/journal o log e apagado no reboot, e `-u` exige o nome exato do unit.',
    'systemd がバイナリ形式でためたログを読みます。/var/log/journal がなければ再起動でログは消え、`-u` にはユニット名を正確に書く必要があります。',
    'Liest das binare Log, das systemd sammelt; ohne /var/log/journal ist es nach dem Neustart weg, und `-u` braucht den exakten Unit-Namen.',
    "Lit le journal binaire que systemd collecte ; sans /var/log/journal il est effacé au redemarrage, et `-u` exige le nom exact de l'unite.",
    'systemd द्वारा जमा किया गया बाइनरी लॉग पढ़ता है; /var/log/journal न हो तो रीबूट पर लॉग मिट जाता है, और `-u` को unit का ठीक नाम चाहिए।',
    '读取 systemd 以二进制格式收集的日志；没有 /var/log/journal 时日志一重启就没了，而 `-u` 要写单元的准确名字。',
    '讀取 systemd 以二進位格式收集的日誌；沒有 /var/log/journal 時日誌一重開機就沒了，而 `-u` 要寫單元的準確名稱。',
  ],

  service: [
    'SysV 시절부터 남아 있는 얇은 겉껍데기라, systemd 기계에서는 그냥 systemctl로 넘겨주며 알아듣는 낱말이 더 적습니다.',
    'A thin wrapper left over from the SysV era; on a systemd machine it simply forwards to systemctl and understands fewer verbs.',
    'Una envoltura fina heredada de la era SysV; en una maquina con systemd solo reenvia a systemctl y entiende menos verbos.',
    'Um invólucro fino herdado da era SysV; numa maquina com systemd ele apenas repassa para systemctl e entende menos verbos.',
    'SysV 時代から残る薄い外皮で、systemd の機械では単に systemctl へ渡すだけ、理解する語も少なめです。',
    'Eine dunne Hulle aus der SysV-Zeit; auf einer systemd-Maschine leitet sie einfach an systemctl weiter und versteht weniger Verben.',
    "Une fine surcouche heritee de l'epoque SysV ; sur une machine systemd, elle se contente de transmettre a systemctl et comprend moins de verbes.",
    'SysV युग से बचा हुआ पतला आवरण; systemd मशीन पर यह बस systemctl को आगे भेज देता है और कम क्रियाएँ समझता है।',
    '这是 SysV 时代留下的一层薄壳；在 systemd 机器上它只是把命令转给 systemctl，而且认得的动词更少。',
    '這是 SysV 時代留下的一層薄殼；在 systemd 機器上它只是把命令轉給 systemctl，而且認得的動詞更少。',
  ],

  crontab: [
    '사용자별 반복 일정표를 다룹니다 — cron은 PATH가 거의 비어 있고 로그인 설정을 읽지 않으니 경로를 전부 절대경로로 적어야 하고, `crontab -r`은 묻지 않고 표 전체를 지웁니다.',
    "Edits the per-user schedule table; cron runs with almost no PATH and never reads your login profile, so write absolute paths, and `crontab -r` erases the whole table without asking.",
    'Gestiona la tabla de tareas por usuario; cron corre casi sin PATH y no lee tu perfil de inicio, asi que usa rutas absolutas, y `crontab -r` borra la tabla entera sin preguntar.',
    'Gerencia a tabela de tarefas por usuario; cron roda quase sem PATH e nunca le seu perfil de login, entao use caminhos absolutos, e `crontab -r` apaga a tabela toda sem perguntar.',
    '利用者ごとの定期実行表を扱います。cron は PATH がほぼ空でログイン設定も読まないため絶対パスで書く必要があり、`crontab -r` は確認なしで表を丸ごと消します。',
    'Bearbeitet die benutzereigene Zeitplantabelle; cron lauft fast ohne PATH und liest nie Ihr Login-Profil, also absolute Pfade schreiben — und `crontab -r` loscht die ganze Tabelle ohne Ruckfrage.',
    "Gere la table de taches propre a chaque utilisateur ; cron s'execute avec un PATH quasi vide et ne lit jamais votre profil de connexion, donc ecrivez des chemins absolus, et `crontab -r` efface toute la table sans demander.",
    'प्रति-उपयोगकर्ता समय-सारणी संभालता है; cron लगभग खाली PATH के साथ चलता है और लॉगिन प्रोफ़ाइल कभी नहीं पढ़ता, इसलिए पूर्ण पथ लिखें — और `crontab -r` पूरी तालिका बिना पूछे मिटा देता है।',
    '管理每个用户的定时任务表；cron 运行时 PATH 几乎是空的，也不会读登录配置，所以路径要写绝对路径，而 `crontab -r` 会不问一声删掉整张表。',
    '管理每個使用者的定時任務表；cron 執行時 PATH 幾乎是空的，也不會讀登入設定，所以路徑要寫絕對路徑，而 `crontab -r` 會不問一聲刪掉整張表。',
  ],

  at: [
    '정해진 시각에 명령을 딱 한 번 돌립니다 — atd 데몬이 떠 있어야 하고(요즘 배포판은 깔려 있지도 않습니다), 출력은 화면이 아니라 메일로 갑니다.',
    'Runs a command exactly once at a time you name; the atd daemon has to be running (often it is not even installed), and the output is mailed to you rather than printed.',
    'Ejecuta un comando una sola vez a la hora que indiques; el demonio atd debe estar activo (a menudo ni siquiera esta instalado) y la salida llega por correo, no a la pantalla.',
    'Executa um comando exatamente uma vez no horario que voce indicar; o daemon atd precisa estar rodando (muitas vezes nem esta instalado) e a saida vai por e-mail, nao para a tela.',
    '指定した時刻に一度だけコマンドを実行します。atd デーモンが動いている必要があり(最近の配布物では入ってさえいないことも)、出力は画面ではなくメールに届きます。',
    'Fuhrt einen Befehl genau einmal zu einer genannten Zeit aus; der atd-Dienst muss laufen (oft ist er nicht einmal installiert), und die Ausgabe kommt per Mail, nicht auf den Bildschirm.',
    "Execute une commande une seule fois a l'heure indiquee ; le demon atd doit tourner (souvent il n'est meme pas installe) et la sortie arrive par courrier, pas a l'ecran.",
    'बताए हुए समय पर किसी आदेश को ठीक एक बार चलाता है; atd डीमन चालू होना चाहिए (अक्सर वह स्थापित ही नहीं होता) और आउटपुट स्क्रीन पर नहीं, मेल से आता है।',
    '在你指定的时刻只运行命令一次；需要 atd 守护进程在跑（很多发行版根本没装），而且输出是发邮件给你，不会打印到屏幕。',
    '在你指定的時刻只執行命令一次；需要 atd 守護行程在跑（很多發行版根本沒裝），而且輸出是寄信給你，不會印到螢幕。',
  ],

  uptime: [
    '기계가 켜져 있던 시간과 부하 평균을 한 줄로 보여줍니다 — 세 숫자는 1·5·15분 동안 실행을 기다린 프로세스의 평균이고 백분율이 아니라서, 4코어에서 4.00이면 꽉 찬 것입니다.',
    'Shows how long the machine has been up plus the load average; those three numbers are runnable processes averaged over 1, 5 and 15 minutes, not percentages, so 4.00 on a 4-core box means fully busy.',
    'Muestra cuanto lleva encendida la maquina y la carga media; esos tres numeros son procesos listos promediados en 1, 5 y 15 minutos, no porcentajes, asi que 4.00 en 4 nucleos es carga plena.',
    'Mostra quanto tempo a maquina esta ligada e a carga media; esses tres numeros sao processos prontos na media de 1, 5 e 15 minutos, nao porcentagens, entao 4.00 em 4 nucleos significa cheio.',
    '機械が動いていた時間と負荷平均を一行で示します。三つの数字は 1・5・15 分間に実行を待ったプロセスの平均で、百分率ではないため、4 コアで 4.00 なら満杯です。',
    'Zeigt die Laufzeit der Maschine und die Lastmittelwerte; die drei Zahlen sind laufbereite Prozesse im Mittel uber 1, 5 und 15 Minuten, keine Prozentwerte — 4.00 auf vier Kernen heisst also voll ausgelastet.',
    "Indique depuis combien de temps la machine tourne et la charge moyenne ; ces trois nombres sont des processus pret a s'executer moyennes sur 1, 5 et 15 minutes, pas des pourcentages : 4.00 sur 4 coeurs signifie pleine charge.",
    'बताता है मशीन कितनी देर से चालू है और लोड औसत क्या है; वे तीन संख्याएँ 1, 5 और 15 मिनट में चलने को तैयार प्रक्रियाओं का औसत हैं, प्रतिशत नहीं — इसलिए 4 कोर पर 4.00 का मतलब पूरा भरा हुआ है।',
    '一行显示机器已开机多久和平均负载；那三个数字是 1、5、15 分钟内可运行进程的平均数，不是百分比，所以四核上 4.00 就是满负荷。',
    '一行顯示機器已開機多久和平均負載；那三個數字是 1、5、15 分鐘內可執行行程的平均數，不是百分比，所以四核上 4.00 就是滿載。',
  ],

  free: [
    'Linux에서 메모리와 스왑이 얼마나 남았는지 셉니다 — 봐야 하는 칸은 free가 아니라 available입니다(캐시는 필요하면 내주니까), macOS에는 없고 vm_stat을 씁니다.',
    'Counts how much memory and swap are left on Linux; the column to read is available, not free, because cache is handed back on demand, and macOS has no free at all (use vm_stat).',
    'Cuenta cuanta memoria y swap quedan en Linux; la columna que importa es available, no free, porque la cache se devuelve cuando hace falta, y macOS no tiene free (usa vm_stat).',
    'Conta quanta memoria e swap restam no Linux; a coluna a ler e available, nao free, porque o cache e devolvido quando preciso, e o macOS nao tem free (use vm_stat).',
    'Linux でメモリとスワップの残りを数えます。見るべき列は free ではなく available です(キャッシュは必要になれば返されるため)。macOS には無く、vm_stat を使います。',
    'Zahlt unter Linux, wie viel Speicher und Swap frei sind; die richtige Spalte ist available, nicht free, denn Cache wird bei Bedarf zuruckgegeben — und macOS hat free nicht (dort vm_stat).',
    "Compte la memoire et le swap restants sous Linux ; la colonne a lire est available et non free, car le cache est rendu au besoin, et macOS n'a pas free du tout (utilisez vm_stat).",
    'Linux पर बची हुई मेमोरी और स्वैप गिनता है; पढ़ने वाला स्तंभ free नहीं, available है, क्योंकि कैश ज़रूरत पड़ने पर लौटा दिया जाता है — और macOS में free नहीं होता (वहाँ vm_stat)।',
    '在 Linux 上统计还剩多少内存和交换区；该看的一列是 available 而不是 free，因为缓存需要时会还回来，而 macOS 根本没有 free（用 vm_stat）。',
    '在 Linux 上統計還剩多少記憶體和交換區；該看的一欄是 available 而不是 free，因為快取需要時會還回來，而 macOS 根本沒有 free（用 vm_stat）。',
  ],

  vmstat: [
    '메모리·스왑·디스크·CPU 통계를 정해진 간격으로 계속 찍습니다 — 첫 줄은 지금이 아니라 부팅 이후의 평균이라 반드시 버리고, si/so와 wa 칸부터 봅니다.',
    'Prints memory, swap, disk and CPU counters at a fixed interval; throw away the first line, which is an average since boot rather than now, and look at the si/so and wa columns first.',
    'Imprime contadores de memoria, swap, disco y CPU a intervalos fijos; descarta la primera linea, que es un promedio desde el arranque y no el momento actual, y mira antes las columnas si/so y wa.',
    'Imprime contadores de memoria, swap, disco e CPU em intervalos fixos; descarte a primeira linha, que e uma media desde o boot e nao o momento atual, e olhe primeiro as colunas si/so e wa.',
    'メモリ・スワップ・ディスク・CPU の統計を一定間隔で出し続けます。最初の行はいまではなく起動以来の平均なので必ず捨て、si/so と wa の列から見ます。',
    'Gibt Speicher-, Swap-, Platten- und CPU-Zahler in festem Intervall aus; die erste Zeile ist ein Mittelwert seit dem Start und nicht der aktuelle Moment — verwerfen und zuerst auf si/so und wa schauen.',
    "Affiche a intervalle fixe les compteurs memoire, swap, disque et CPU ; jetez la premiere ligne, qui est une moyenne depuis le demarrage et non l'instant present, et regardez d'abord les colonnes si/so et wa.",
    'निश्चित अंतराल पर मेमोरी, स्वैप, डिस्क और CPU के काउंटर छापता है; पहली पंक्ति फेंक दें, वह अभी का नहीं बूट के बाद का औसत है, और पहले si/so तथा wa स्तंभ देखें।',
    '按固定间隔不断打印内存、交换、磁盘和 CPU 的计数；第一行要丢掉，它是开机以来的平均值而不是此刻，然后先看 si/so 和 wa 两列。',
    '按固定間隔不斷印出記憶體、交換、磁碟和 CPU 的計數；第一行要丟掉，它是開機以來的平均值而不是此刻，然後先看 si/so 和 wa 兩欄。',
  ],

  iostat: [
    '디스크마다 얼마나 읽고 쓰고 얼마나 기다리는지 보여줍니다 — 첫 묶음은 부팅 이후 평균이고, Linux에서는 sysstat을 깔아야 하며 macOS는 `-w 1 -c 5`처럼 옵션이 다릅니다.',
    'Shows per-disk throughput and how long requests wait; the first block is an average since boot, Linux needs the sysstat package, and macOS takes different flags such as `-w 1 -c 5`.',
    'Muestra el rendimiento por disco y cuanto esperan las peticiones; el primer bloque es un promedio desde el arranque, Linux necesita el paquete sysstat y macOS usa otras opciones como `-w 1 -c 5`.',
    'Mostra a taxa por disco e quanto os pedidos esperam; o primeiro bloco e uma media desde o boot, o Linux precisa do pacote sysstat e o macOS usa outras opcoes como `-w 1 -c 5`.',
    'ディスクごとの読み書き量と待ち時間を示します。最初の一組は起動以来の平均で、Linux では sysstat の導入が必要、macOS は `-w 1 -c 5` のようにオプションが違います。',
    'Zeigt Durchsatz je Platte und wie lange Anfragen warten; der erste Block ist ein Mittelwert seit dem Start, Linux benotigt das Paket sysstat, und macOS nimmt andere Optionen wie `-w 1 -c 5`.',
    "Montre le debit par disque et le temps d'attente des requetes ; le premier bloc est une moyenne depuis le demarrage, Linux exige le paquet sysstat, et macOS emploie d'autres options comme `-w 1 -c 5`.",
    'हर डिस्क का थ्रूपुट और अनुरोधों का प्रतीक्षा समय दिखाता है; पहला खंड बूट के बाद का औसत है, Linux पर sysstat पैकेज चाहिए, और macOS `-w 1 -c 5` जैसे भिन्न फ़्लैग लेता है।',
    '按磁盘显示读写量和请求等待时间；第一段是开机以来的平均值，Linux 上需要装 sysstat，而 macOS 的选项不同，例如 `-w 1 -c 5`。',
    '按磁碟顯示讀寫量和請求等待時間；第一段是開機以來的平均值，Linux 上需要裝 sysstat，而 macOS 的選項不同，例如 `-w 1 -c 5`。',
  ],

  lsblk: [
    '디스크와 그 안의 파티션을 나무 모양으로 늘어놓습니다 — 파티션이 잡히지 않은 빈 공간은 보여주지 않고(그건 fdisk나 parted), Linux 전용이라 macOS에서는 `diskutil list`를 씁니다.',
    'Lays out the disks and their partitions as a tree; it does not show unpartitioned free space (that is fdisk or parted), and it is Linux only, so macOS uses `diskutil list`.',
    'Presenta los discos y sus particiones en arbol; no muestra el espacio libre sin particionar (para eso fdisk o parted) y es solo de Linux, asi que en macOS se usa `diskutil list`.',
    'Apresenta os discos e suas particoes em arvore; nao mostra o espaco livre nao particionado (isso e fdisk ou parted) e existe apenas no Linux, entao no macOS use `diskutil list`.',
    'ディスクとその中の分割を木の形で並べます。分割されていない空き領域は出ません(それは fdisk や parted)。Linux 専用なので macOS では `diskutil list` を使います。',
    'Stellt Platten und ihre Partitionen als Baum dar; unpartitionierten freien Platz zeigt es nicht (dafur fdisk oder parted), und es gibt es nur unter Linux — macOS nutzt `diskutil list`.',
    "Presente les disques et leurs partitions sous forme d'arbre ; il n'affiche pas l'espace libre non partitionne (c'est fdisk ou parted) et n'existe que sous Linux, macOS utilisant `diskutil list`.",
    'डिस्क और उनके विभाजनों को पेड़ की तरह दिखाता है; बिना विभाजन वाली खाली जगह नहीं दिखाता (उसके लिए fdisk या parted) और यह केवल Linux पर है, macOS में `diskutil list` लें।',
    '以树状列出磁盘和其中的分区；它不显示尚未分区的空闲空间（那要用 fdisk 或 parted），而且只有 Linux 有，macOS 用 `diskutil list`。',
    '以樹狀列出磁碟和其中的分割區；它不顯示尚未分割的空閒空間（那要用 fdisk 或 parted），而且只有 Linux 有，macOS 用 `diskutil list`。',
  ],

  uname: [
    '커널 이름과 판, 그리고 아키텍처를 알려줍니다 — 배포판 이름은 알려주지 않으니 그건 /etc/os-release(macOS는 `sw_vers`)를 읽어야 하고, `-o`는 GNU에만 있습니다.',
    'Reports the kernel name, its release and the machine architecture; it will not tell you the distribution, for which you read /etc/os-release (or `sw_vers` on macOS), and `-o` is GNU only.',
    'Informa del nombre del nucleo, su version y la arquitectura; no dice la distribucion, para eso se lee /etc/os-release (o `sw_vers` en macOS), y `-o` solo existe en GNU.',
    'Informa o nome do kernel, sua versao e a arquitetura; nao diz a distribuicao, para isso leia /etc/os-release (ou `sw_vers` no macOS), e `-o` existe so no GNU.',
    'カーネルの名前と版、そして機械の種類を教えます。配布物の名前は分からないので /etc/os-release(macOS は `sw_vers`)を読む必要があり、`-o` は GNU にしかありません。',
    'Nennt Kernelname, Kernelversion und Maschinenarchitektur; die Distribution verrat es nicht — dafur /etc/os-release lesen (unter macOS `sw_vers`) — und `-o` gibt es nur bei GNU.',
    "Donne le nom du noyau, sa version et l'architecture de la machine ; il ne dit pas la distribution, pour cela lisez /etc/os-release (ou `sw_vers` sous macOS), et `-o` n'existe que chez GNU.",
    'कर्नेल का नाम, उसका रिलीज़ और मशीन की आर्किटेक्चर बताता है; डिस्ट्रो का नाम नहीं बताता, उसके लिए /etc/os-release पढ़ें (macOS पर `sw_vers`), और `-o` केवल GNU में है।',
    '报出内核名称、内核版本和机器架构；它不会告诉你发行版，那要读 /etc/os-release（macOS 用 `sw_vers`），而 `-o` 只有 GNU 才有。',
    '報出核心名稱、核心版本和機器架構；它不會告訴你發行版，那要讀 /etc/os-release（macOS 用 `sw_vers`），而 `-o` 只有 GNU 才有。',
  ],

  hostname: [
    '이 기계의 이름을 찍거나 바꿉니다 — `hostname web01`은 재부팅까지만 버티니 오래 두려면 Linux는 `hostnamectl`, macOS는 `scutil --set HostName`이고, IP를 다 보는 `-I`는 Linux에만 있습니다.',
    "Prints or sets this machine's name; `hostname web01` only lasts until the next reboot, so make it permanent with `hostnamectl` on Linux or `scutil --set HostName` on macOS, and `-I` for every IP is Linux only.",
    'Imprime o cambia el nombre de la maquina; `hostname web01` solo dura hasta el reinicio, asi que hazlo permanente con `hostnamectl` en Linux o `scutil --set HostName` en macOS, y `-I` para ver todas las IP existe solo en Linux.',
    'Imprime ou define o nome da maquina; `hostname web01` dura so ate o proximo reboot, entao torne permanente com `hostnamectl` no Linux ou `scutil --set HostName` no macOS, e `-I` para todos os IPs existe apenas no Linux.',
    'この機械の名前を表示または変更します。`hostname web01` は再起動までしか保たないので、恒久化には Linux は `hostnamectl`、macOS は `scutil --set HostName` を使い、すべての IP を出す `-I` は Linux だけです。',
    'Zeigt oder setzt den Namen dieser Maschine; `hostname web01` halt nur bis zum nachsten Neustart, dauerhaft geht es mit `hostnamectl` (Linux) oder `scutil --set HostName` (macOS), und `-I` fur alle IPs gibt es nur unter Linux.',
    "Affiche ou definit le nom de la machine ; `hostname web01` ne tient que jusqu'au redemarrage, rendez-le permanent avec `hostnamectl` sous Linux ou `scutil --set HostName` sous macOS, et `-I` qui liste toutes les IP n'existe que sous Linux.",
    'इस मशीन का नाम छापता या बदलता है; `hostname web01` अगले रीबूट तक ही टिकता है, इसलिए स्थायी करने के लिए Linux पर `hostnamectl` या macOS पर `scutil --set HostName` लें, और सभी IP दिखाने वाला `-I` केवल Linux में है।',
    '打印或设置本机名字；`hostname web01` 只保留到下次重启，要永久生效得用 Linux 的 `hostnamectl` 或 macOS 的 `scutil --set HostName`，而列出全部 IP 的 `-I` 只有 Linux 有。',
    '印出或設定本機名稱；`hostname web01` 只保留到下次重開機，要永久生效得用 Linux 的 `hostnamectl` 或 macOS 的 `scutil --set HostName`，而列出全部 IP 的 `-I` 只有 Linux 有。',
  ],

  date: [
    '지금 시각을 원하는 꼴로 찍거나 날짜를 계산합니다 — `+`로 시작하는 형식은 어디서나 같지만, 날짜를 옮기는 옵션은 GNU가 `-d`, macOS/BSD가 `-v`와 `-j -f`로 완전히 다릅니다.',
    'Prints the current time in any format you like and does date arithmetic; the format after `+` is the same everywhere, but shifting a date is `-d` on GNU and `-v` or `-j -f` on macOS/BSD.',
    'Imprime la hora actual en el formato que quieras y hace aritmetica de fechas; el formato tras `+` es igual en todas partes, pero desplazar una fecha es `-d` en GNU y `-v` o `-j -f` en macOS/BSD.',
    'Imprime a hora atual no formato que voce quiser e faz aritmetica de datas; o formato depois de `+` e igual em todo lugar, mas deslocar uma data e `-d` no GNU e `-v` ou `-j -f` no macOS/BSD.',
    'いまの時刻を好きな形式で表示し、日付の計算もします。`+` のあとの書式はどこでも同じですが、日付をずらすオプションは GNU が `-d`、macOS/BSD が `-v` と `-j -f` でまったく違います。',
    'Gibt die aktuelle Zeit in beliebigem Format aus und rechnet mit Datumsangaben; das Format nach `+` ist uberall gleich, doch das Verschieben eines Datums heisst bei GNU `-d` und unter macOS/BSD `-v` bzw. `-j -f`.',
    "Affiche l'heure actuelle dans le format voulu et fait du calcul de dates ; le format apres `+` est identique partout, mais decaler une date s'ecrit `-d` chez GNU et `-v` ou `-j -f` sous macOS/BSD.",
    'वर्तमान समय को मनचाहे रूप में छापता है और तारीख़ का गणित करता है; `+` के बाद का रूप हर जगह एक जैसा है, पर तारीख़ खिसकाना GNU में `-d` और macOS/BSD में `-v` या `-j -f` है।',
    '按你要的格式打印当前时间，也能做日期加减；`+` 后面的格式各平台一致，但挪动日期在 GNU 是 `-d`，在 macOS/BSD 是 `-v` 或 `-j -f`，完全不同。',
    '按你要的格式印出目前時間，也能做日期加減；`+` 後面的格式各平台一致，但挪動日期在 GNU 是 `-d`，在 macOS/BSD 是 `-v` 或 `-j -f`，完全不同。',
  ],

  env: [
    '환경 변수를 다 찍거나, 변수를 얹어 명령 하나를 돌립니다 — 그 변수는 그 한 번의 실행에만 붙고 셸에는 아무것도 남지 않습니다.',
    'Prints the whole environment, or runs one command with extra variables attached; those variables apply to that single run only and nothing is left behind in your shell.',
    'Imprime todo el entorno o ejecuta un comando con variables anadidas; esas variables solo valen para esa ejecucion y no queda nada en tu shell.',
    'Imprime todo o ambiente ou executa um comando com variaveis extras; essas variaveis valem apenas para aquela execucao e nada fica no seu shell.',
    '環境変数をすべて表示するか、変数を足してコマンドを一つ動かします。その変数はその一回の実行にだけ付き、シェルには何も残りません。',
    'Gibt die gesamte Umgebung aus oder startet einen Befehl mit zusatzlichen Variablen; diese gelten nur fur diesen einen Lauf, in Ihrer Shell bleibt nichts zuruck.',
    "Affiche tout l'environnement, ou lance une commande avec des variables supplementaires ; celles-ci ne valent que pour cette execution et ne laissent rien dans votre shell.",
    'पूरा एनवायरनमेंट छापता है, या अतिरिक्त वेरिएबल के साथ एक आदेश चलाता है; वे वेरिएबल केवल उसी एक बार के लिए लगते हैं और शेल में कुछ नहीं बचता।',
    '打印整个环境变量表，或者带上额外变量运行一条命令；那些变量只对这一次运行生效，你的 shell 里不留下任何东西。',
    '印出整個環境變數表，或者帶上額外變數執行一條命令；那些變數只對這一次執行生效，你的 shell 裡不留下任何東西。',
  ],

  export: [
    '변수를 자식 프로세스에게 물려주도록 표시합니다 — 이 셸을 닫으면 사라지니 오래 두려면 ~/.bashrc나 ~/.zshrc에 적어야 하고, export 없이 `VAR=x`만 하면 자식 프로그램은 그 변수를 못 봅니다.',
    'Marks a variable so child processes inherit it; it dies with this shell, so put it in ~/.bashrc or ~/.zshrc to keep it, and a bare `VAR=x` without export stays invisible to the programs you run.',
    'Marca una variable para que la hereden los procesos hijos; muere con este shell, asi que ponla en ~/.bashrc o ~/.zshrc para conservarla, y un `VAR=x` sin export sigue invisible para los programas que lances.',
    'Marca uma variavel para que os processos filhos a herdem; ela morre com este shell, entao coloque em ~/.bashrc ou ~/.zshrc para manter, e um `VAR=x` sem export continua invisivel para os programas que voce roda.',
    '変数を子プロセスに引き継がせる印を付けます。このシェルを閉じると消えるので残したければ ~/.bashrc か ~/.zshrc に書き、export なしの `VAR=x` だけでは実行するプログラムから見えません。',
    'Markiert eine Variable, damit Kindprozesse sie erben; sie stirbt mit dieser Shell — fur Dauerhaftigkeit in ~/.bashrc oder ~/.zshrc schreiben — und ein blosses `VAR=x` ohne export bleibt fur gestartete Programme unsichtbar.',
    "Marque une variable pour que les processus enfants en heritent ; elle meurt avec ce shell, mettez-la donc dans ~/.bashrc ou ~/.zshrc pour la conserver, et un simple `VAR=x` sans export reste invisible aux programmes lances.",
    'किसी वेरिएबल पर निशान लगाता है ताकि चाइल्ड प्रक्रियाएँ उसे पाएँ; यह इस शेल के साथ ख़त्म हो जाता है, इसलिए बनाए रखने के लिए ~/.bashrc या ~/.zshrc में लिखें, और export के बिना केवल `VAR=x` चलाए जाने वाले प्रोग्राम को नहीं दिखता।',
    '给变量打上标记，让子进程能继承它；它随这个 shell 一起消失，想长期保留就写进 ~/.bashrc 或 ~/.zshrc，而只写 `VAR=x` 不加 export，你启动的程序看不到它。',
    '給變數打上標記，讓子行程能繼承它；它隨這個 shell 一起消失，想長期保留就寫進 ~/.bashrc 或 ~/.zshrc，而只寫 `VAR=x` 不加 export，你啟動的程式看不到它。',
  ],

  which: [
    'PATH를 뒤져 그 이름의 실행 파일이 어디 있는지 알려줍니다 — PATH만 보기 때문에 별칭·함수·셸 내장 명령에 대해서는 거짓을 말하고, 정직한 답은 `type -a`나 `command -v`가 줍니다.',
    'Searches PATH and tells you where the executable of that name lives; because it looks only at PATH it lies about aliases, functions and shell builtins, and the honest answer comes from `type -a` or `command -v`.',
    'Recorre PATH y dice donde esta el ejecutable con ese nombre; como solo mira PATH, miente sobre alias, funciones y builtins del shell, y la respuesta honesta la da `type -a` o `command -v`.',
    'Percorre o PATH e diz onde esta o executavel com aquele nome; como olha somente o PATH, ele mente sobre aliases, funcoes e builtins do shell, e a resposta honesta vem de `type -a` ou `command -v`.',
    'PATH をたどって、その名前の実行ファイルがどこにあるかを教えます。PATH しか見ないので別名・関数・シェル組み込みについては嘘をつき、正直な答えは `type -a` か `command -v` が返します。',
    'Durchsucht PATH und nennt den Ort der ausfuhrbaren Datei dieses Namens; da es nur PATH betrachtet, tauscht es bei Aliassen, Funktionen und Shell-Builtins — die ehrliche Antwort liefert `type -a` oder `command -v`.',
    "Parcourt PATH et indique ou se trouve l'executable de ce nom ; comme il ne regarde que PATH, il ment sur les alias, les fonctions et les commandes internes, et la reponse honnete vient de `type -a` ou `command -v`.",
    'PATH में खोजकर बताता है कि उस नाम का executable कहाँ है; चूँकि यह केवल PATH देखता है, alias, function और shell builtin के बारे में झूठ बोलता है — सच्चा उत्तर `type -a` या `command -v` देता है।',
    '在 PATH 里查找同名可执行文件在哪；因为它只看 PATH，对别名、函数和 shell 内建命令会给出错误答案，老实的答案来自 `type -a` 或 `command -v`。',
    '在 PATH 裡尋找同名執行檔在哪；因為它只看 PATH，對別名、函式和 shell 內建命令會給出錯誤答案，老實的答案來自 `type -a` 或 `command -v`。',
  ],

  type: [
    '어떤 이름이 실제로 무엇인지 — 별칭인지 함수인지 내장 명령인지 파일인지 — 셸에게 직접 물어봅니다. 그래서 PATH만 보는 which가 놓치는 것을 봅니다.',
    'Asks the shell itself what a name really is, whether alias, function, builtin or file, which is exactly what `which` cannot see because it only looks at PATH.',
    'Pregunta al propio shell que es realmente un nombre: alias, funcion, builtin o archivo, justo lo que `which` no ve porque solo mira PATH.',
    'Pergunta ao proprio shell o que um nome realmente e: alias, funcao, builtin ou arquivo, exatamente o que `which` nao ve porque so olha o PATH.',
    'ある名前が実際に何なのか — 別名か、関数か、組み込みか、ファイルか — をシェル自身に尋ねます。PATH しか見ない `which` が取りこぼすものが分かります。',
    'Fragt die Shell selbst, was ein Name wirklich ist — Alias, Funktion, Builtin oder Datei — genau das, was `which` nicht sehen kann, weil es nur PATH betrachtet.',
    "Demande au shell lui-meme ce qu'est vraiment un nom : alias, fonction, commande interne ou fichier, exactement ce que `which` ne peut pas voir puisqu'il ne regarde que PATH.",
    'शेल से ही पूछता है कि कोई नाम असल में क्या है — alias, function, builtin या फ़ाइल — यही वह चीज़ है जो केवल PATH देखने वाला `which` नहीं देख पाता।',
    '直接问 shell 某个名字到底是什么——别名、函数、内建还是文件，这正是只看 PATH 的 `which` 看不到的部分。',
    '直接問 shell 某個名稱到底是什麼——別名、函式、內建還是檔案，這正是只看 PATH 的 `which` 看不到的部分。',
  ],

  lsof: [
    '열려 있는 파일과 소켓을 모두 늘어놓아 "3000번 포트를 누가 쥐고 있나"에 답합니다 — sudo 없이는 내 프로세스만 보이니, 빈 결과가 포트가 비었다는 뜻은 아닙니다.',
    'Lists every open file and socket, which is how you answer "what is holding port 3000"; without sudo you only see your own processes, so an empty result does not mean the port is free.',
    'Lista cada archivo y socket abierto, y asi responde a "quien tiene el puerto 3000"; sin sudo solo ves tus procesos, por lo que un resultado vacio no significa que el puerto este libre.',
    'Lista todo arquivo e socket aberto, e assim responde "quem esta segurando a porta 3000"; sem sudo voce so ve seus proprios processos, entao um resultado vazio nao significa que a porta esta livre.',
    '開いているファイルとソケットをすべて並べ、「3000 番ポートを誰が握っているか」に答えます。sudo なしでは自分のプロセスしか見えないので、結果が空でもポートが空いている証拠にはなりません。',
    'Listet jede offene Datei und jeden Socket und beantwortet damit die Frage, wer Port 3000 halt; ohne sudo sehen Sie nur eigene Prozesse — ein leeres Ergebnis heisst also nicht, dass der Port frei ist.',
    "Liste tous les fichiers et sockets ouverts, ce qui repond a la question \"qui occupe le port 3000\" ; sans sudo vous ne voyez que vos propres processus, donc un resultat vide ne prouve pas que le port est libre.",
    'हर खुली फ़ाइल और सॉकेट सूचीबद्ध करता है, इसी से पता चलता है कि पोर्ट 3000 किसने पकड़ रखा है; sudo के बिना आप केवल अपनी प्रक्रियाएँ देखते हैं, इसलिए खाली परिणाम का अर्थ नहीं है कि पोर्ट खाली है।',
    '列出所有打开的文件和套接字，这就是回答“谁占着 3000 端口”的办法；不加 sudo 只能看到自己的进程，所以结果为空并不表示端口是空的。',
    '列出所有開啟的檔案和 socket，這就是回答「誰占著 3000 埠」的辦法；不加 sudo 只能看到自己的行程，所以結果為空並不表示埠是空的。',
  ],

  fuser: [
    '어떤 파일·마운트·포트를 쓰고 있는 프로세스를 찾아내고 원하면 죽입니다 — `-k`는 SIGKILL을 보내 정리할 틈을 주지 않으며, Linux(psmisc)에만 있고 macOS판에는 `-cfu`뿐입니다.',
    'Finds, and if you ask kills, whatever is using a file, a mount point or a port; `-k` sends SIGKILL with no chance to clean up, and it exists only on Linux (psmisc) since the macOS version has just `-cfu`.',
    'Encuentra, y si lo pides mata, lo que esta usando un archivo, un punto de montaje o un puerto; `-k` envia SIGKILL sin margen para limpiar y solo existe en Linux (psmisc), porque la version de macOS solo tiene `-cfu`.',
    'Encontra, e se voce pedir mata, o que esta usando um arquivo, um ponto de montagem ou uma porta; `-k` envia SIGKILL sem chance de limpeza e existe apenas no Linux (psmisc), pois a versao do macOS tem so `-cfu`.',
    'あるファイル・マウント・ポートを使っているプロセスを見つけ、望めば殺します。`-k` は SIGKILL を送って片付ける余裕を与えず、Linux(psmisc)にしかありません — macOS 版は `-cfu` だけです。',
    'Findet — und auf Wunsch beendet — was eine Datei, einen Mountpunkt oder einen Port benutzt; `-k` sendet SIGKILL ohne Gelegenheit zum Aufraumen und gibt es nur unter Linux (psmisc), da die macOS-Fassung allein `-cfu` kennt.',
    "Trouve, et tue si vous le demandez, ce qui utilise un fichier, un point de montage ou un port ; `-k` envoie SIGKILL sans laisser le temps de faire le menage et n'existe que sous Linux (psmisc), la version macOS ne connaissant que `-cfu`.",
    'पता लगाता है — और कहें तो मार देता है — कि कोई फ़ाइल, माउंट या पोर्ट कौन इस्तेमाल कर रहा है; `-k` SIGKILL भेजता है, सफ़ाई का मौका नहीं देता, और यह केवल Linux (psmisc) में है क्योंकि macOS संस्करण में सिर्फ़ `-cfu` हैं।',
    '找出正在使用某个文件、挂载点或端口的进程，需要时把它杀掉；`-k` 发的是 SIGKILL，不留清理时间，而且它只在 Linux（psmisc）上有，macOS 版只有 `-cfu`。',
    '找出正在使用某個檔案、掛載點或埠的行程，需要時把它殺掉；`-k` 發的是 SIGKILL，不留清理時間，而且它只在 Linux（psmisc）上有，macOS 版只有 `-cfu`。',
  ],

  watch: [
    '같은 명령을 몇 초마다 다시 돌리며 화면을 갈아 그립니다 — 파이프나 `*`가 든 명령은 반드시 따옴표로 묶어야 하고(안 그러면 셸이 watch 자신에게 한 번만 적용합니다), macOS에는 기본으로 없습니다.',
    'Reruns the same command every few seconds and redraws the screen; quote anything with a pipe or a `*` in it, or the shell applies it once to watch itself, and macOS does not ship it.',
    'Vuelve a ejecutar el mismo comando cada pocos segundos y redibuja la pantalla; entrecomilla todo lo que lleve tuberia o `*`, o el shell lo aplicara una sola vez a watch, y macOS no lo incluye.',
    'Reexecuta o mesmo comando a cada poucos segundos e redesenha a tela; coloque entre aspas qualquer coisa com pipe ou `*`, senao o shell aplica uma vez ao proprio watch, e o macOS nao o traz.',
    '同じコマンドを数秒ごとに走らせ直し、画面を描き替えます。パイプや `*` を含む命令は必ず引用符で囲みます(でないとシェルが watch 自身に一度だけ適用します)。macOS には既定で入っていません。',
    'Fuhrt denselben Befehl alle paar Sekunden erneut aus und zeichnet den Bildschirm neu; alles mit Pipe oder `*` in Anfuhrungszeichen setzen, sonst wendet die Shell es einmal auf watch selbst an — und macOS liefert es nicht mit.',
    "Relance la meme commande toutes les quelques secondes et redessine l'ecran ; mettez entre guillemets tout ce qui contient un tube ou un `*`, sinon le shell l'applique une seule fois a watch lui-meme, et macOS ne le fournit pas.",
    'हर कुछ सेकंड में वही आदेश दोबारा चलाकर स्क्रीन नए सिरे से बनाता है; pipe या `*` वाले आदेश को उद्धरण में रखें, वरना शेल उसे एक बार watch पर ही लगा देगा — और macOS में यह नहीं आता।',
    '每隔几秒重新运行同一条命令并重画屏幕；带管道或 `*` 的命令一定要加引号，否则 shell 只会先对 watch 本身展开一次，而且 macOS 默认没有它。',
    '每隔幾秒重新執行同一條命令並重畫畫面；帶管線或 `*` 的命令一定要加引號，否則 shell 只會先對 watch 本身展開一次，而且 macOS 預設沒有它。',
  ],

  timeout: [
    '명령에 시간 제한을 걸고 넘기면 끊습니다 — 제한이 걸리면 종료 코드 124가 나오고, 기본 신호는 TERM이라 멈춘 프로그램은 무시할 수 있으니 `-k`로 KILL을 뒤따르게 합니다(GNU coreutils, macOS에는 없습니다).',
    'Runs a command under a time limit and cuts it off when the limit passes, exiting 124; the default signal is TERM, which a hung program can ignore, so add `-k` to follow with KILL (GNU coreutils, absent on macOS).',
    'Ejecuta un comando con limite de tiempo y lo corta al pasarse, saliendo con 124; la senal por defecto es TERM, que un programa colgado puede ignorar, asi que anade `-k` para rematar con KILL (GNU coreutils, no esta en macOS).',
    'Executa um comando com limite de tempo e o corta quando o limite passa, saindo com 124; o sinal padrao e TERM, que um programa travado pode ignorar, entao acrescente `-k` para seguir com KILL (GNU coreutils, ausente no macOS).',
    'コマンドに時間制限をかけ、超えたら打ち切って終了コード 124 を返します。既定のシグナルは TERM で、固まったプログラムは無視できるため `-k` で KILL を続けさせます(GNU coreutils で、macOS には入っていません)。',
    'Fuhrt einen Befehl mit Zeitlimit aus und bricht ihn beim Uberschreiten mit Status 124 ab; das Standardsignal ist TERM, das ein hangendes Programm ignorieren kann, daher mit `-k` ein KILL nachschicken (GNU coreutils, unter macOS nicht vorhanden).',
    "Execute une commande avec une limite de temps et la coupe au depassement, en sortant avec 124 ; le signal par defaut est TERM, qu'un programme bloque peut ignorer, ajoutez donc `-k` pour enchainer avec KILL (GNU coreutils, absent de macOS).",
    'किसी आदेश को समय-सीमा में चलाता है और सीमा पार होने पर काट देता है, exit code 124 के साथ; डिफ़ॉल्ट सिग्नल TERM है जिसे अटका प्रोग्राम नज़रअंदाज़ कर सकता है, इसलिए `-k` जोड़कर KILL भेजें (GNU coreutils, macOS में नहीं)।',
    '给命令加上时间上限，超时就掐掉并以 124 退出；默认信号是 TERM，卡死的程序可以不理它，所以要加 `-k` 随后补一个 KILL（属 GNU coreutils，macOS 没有）。',
    '給命令加上時間上限，逾時就掐掉並以 124 結束；預設訊號是 TERM，卡死的程式可以不理它，所以要加 `-k` 隨後補一個 KILL（屬 GNU coreutils，macOS 沒有）。',
  ],

  strace: [
    '프로세스가 커널에 던지는 시스템 호출을 하나씩 찍어 어디서 멈췄는지 보여줍니다 — 출력이 stderr로 나가므로 `2>&1`로 돌려야 하고, ptrace 권한(보통 sudo)이 필요하며 Linux 전용입니다(macOS는 dtruss).',
    'Prints every system call a process makes, which is how you see where it is stuck; the output goes to stderr so redirect it with `2>&1`, it needs ptrace permission (usually sudo), and it is Linux only (macOS has dtruss).',
    'Imprime cada llamada al sistema que hace un proceso, y asi ves donde se atasca; la salida va a stderr, redirigela con `2>&1`, necesita permiso ptrace (normalmente sudo) y solo existe en Linux (macOS tiene dtruss).',
    'Imprime cada chamada de sistema que um processo faz, e assim voce ve onde ele travou; a saida vai para stderr, redirecione com `2>&1`, precisa de permissao ptrace (geralmente sudo) e existe apenas no Linux (macOS tem dtruss).',
    'プロセスがカーネルに投げるシステムコールを一つずつ表示し、どこで止まっているかを見せます。出力は stderr へ行くので `2>&1` で回し、ptrace の権限(通常は sudo)が必要で、Linux 専用です(macOS は dtruss)。',
    'Gibt jeden Systemaufruf eines Prozesses aus, sodass man sieht, wo er hangt; die Ausgabe geht auf stderr, also mit `2>&1` umleiten, es braucht ptrace-Rechte (meist sudo) und gibt es nur unter Linux (macOS: dtruss).',
    "Affiche chaque appel systeme d'un processus, ce qui montre ou il se bloque ; la sortie part sur stderr, redirigez-la avec `2>&1`, il faut le droit ptrace (generalement sudo) et cela n'existe que sous Linux (macOS a dtruss).",
    'किसी प्रक्रिया के हर सिस्टम कॉल को छापता है, इससे दिखता है वह कहाँ अटकी है; आउटपुट stderr पर जाता है, इसलिए `2>&1` से मोड़ें, ptrace अनुमति (आम तौर पर sudo) चाहिए, और यह केवल Linux पर है (macOS में dtruss)।',
    '把进程发出的每个系统调用逐条打印出来，用来看它卡在哪里；输出走的是 stderr，要用 `2>&1` 转过来，需要 ptrace 权限（通常是 sudo），而且只有 Linux 有（macOS 用 dtruss）。',
    '把行程發出的每個系統呼叫逐條印出來，用來看它卡在哪裡；輸出走的是 stderr，要用 `2>&1` 轉過來，需要 ptrace 權限（通常是 sudo），而且只有 Linux 有（macOS 用 dtruss）。',
  ],

  dmesg: [
    '커널이 남긴 메시지 고리버퍼를 읽어 디스크나 USB가 방금 무슨 일을 겪었는지 보여줍니다 — 크기가 정해진 버퍼라 오래된 줄은 이미 밀려났고, 요즘 배포판은 대개 root를 요구합니다(macOS는 `log show`).',
    'Reads the kernel ring buffer, the first place to look after a disk or USB problem; it is a fixed-size buffer so older lines are already gone, and most distributions now require root (macOS uses `log show`).',
    'Lee el buffer circular del nucleo, el primer sitio donde mirar tras un problema de disco o USB; es un buffer de tamano fijo, asi que las lineas viejas ya se fueron, y casi todas las distribuciones piden root (macOS usa `log show`).',
    'Le o buffer circular do kernel, o primeiro lugar para olhar depois de um problema de disco ou USB; e um buffer de tamanho fixo, entao linhas antigas ja se foram, e a maioria das distribuicoes exige root (macOS usa `log show`).',
    'カーネルが残す環状バッファを読み、ディスクや USB に何が起きたかをまず教えてくれます。大きさが決まったバッファなので古い行はすでに押し出され、最近の配布物はたいてい root を求めます(macOS は `log show`)。',
    'Liest den Kernel-Ringpuffer, die erste Anlaufstelle nach einem Platten- oder USB-Problem; der Puffer hat eine feste Grosse, altere Zeilen sind also schon verdrangt, und die meisten Distributionen verlangen root (macOS: `log show`).',
    "Lit le tampon circulaire du noyau, le premier endroit a consulter apres un incident disque ou USB ; sa taille est fixe, donc les lignes anciennes ont deja disparu, et la plupart des distributions exigent root (macOS utilise `log show`).",
    'कर्नेल का रिंग बफ़र पढ़ता है, डिस्क या USB की गड़बड़ी के बाद पहली जगह जहाँ देखना चाहिए; बफ़र का आकार तय है इसलिए पुरानी पंक्तियाँ जा चुकी होती हैं, और अब अधिकांश डिस्ट्रो root माँगते हैं (macOS में `log show`)।',
    '读取内核的环形缓冲区，磁盘或 USB 出问题后第一个该看的地方；缓冲区大小固定，旧行早被挤掉了，而现在多数发行版要 root（macOS 用 `log show`）。',
    '讀取核心的環形緩衝區，磁碟或 USB 出問題後第一個該看的地方；緩衝區大小固定，舊行早被擠掉了，而現在多數發行版要 root（macOS 用 `log show`）。',
  ],

  /* ───────── net — 네트워크 ───────── */

  curl: [
    '주소 하나를 그대로 받아 본문을 화면에 뿌립니다 — 브라우저와 달리 `-L` 없이는 넘김을 따라가지 않아서, 301이 오면 빈 응답처럼 보입니다.',
    'Fetches a URL and prints the body to the terminal; unlike a browser it does not follow redirects without `-L`, so a 301 looks like an empty response.',
    'Descarga una URL e imprime el cuerpo en la terminal; a diferencia de un navegador, no sigue redirecciones sin `-L`, asi que un 301 parece una respuesta vacia.',
    'Baixa uma URL e imprime o corpo no terminal; ao contrario de um navegador, nao segue redirecionamentos sem `-L`, entao um 301 parece uma resposta vazia.',
    'URL を一つ取ってきて本文を画面に出します。ブラウザーと違い `-L` なしでは転送を追わないので、301 が返ると空の応答のように見えます。',
    'Holt eine URL und schreibt den Body ins Terminal; anders als ein Browser folgt es ohne `-L` keinen Weiterleitungen, sodass ein 301 wie eine leere Antwort aussieht.',
    "Recupere une URL et affiche le corps dans le terminal ; contrairement a un navigateur, il ne suit pas les redirections sans `-L`, si bien qu'un 301 ressemble a une reponse vide.",
    'किसी URL को लाकर उसका मुख्य भाग टर्मिनल पर छापता है; ब्राउज़र के उलट यह `-L` के बिना रीडायरेक्ट का पीछा नहीं करता, इसलिए 301 आने पर खाली उत्तर जैसा लगता है।',
    '取回一个网址并把正文打印到终端；和浏览器不同，不加 `-L` 它不会跟随跳转，所以遇到 301 看起来像是空响应。',
    '取回一個網址並把正文印到終端機；和瀏覽器不同，不加 `-L` 它不會跟隨轉址，所以遇到 301 看起來像是空回應。',
  ],

  wget: [
    '주소를 파일로 내려받고 넘김도 알아서 따라갑니다 — curl은 시키지 않으면 화면에 뿌리는 것이 차이이고, macOS에는 기본으로 깔려 있지 않습니다.',
    'Downloads a URL into a file and follows redirects on its own; that is the difference from curl, which prints to the terminal unless told otherwise, and macOS does not ship wget.',
    'Descarga una URL a un archivo y sigue las redirecciones por si mismo; esa es la diferencia con curl, que imprime en la terminal si no se le indica otra cosa, y macOS no lo incluye.',
    'Baixa uma URL para um arquivo e segue redirecionamentos por conta propria; essa e a diferenca em relacao ao curl, que imprime no terminal se nao mandarem o contrario, e o macOS nao o traz.',
    'URL をファイルとして落とし、転送も自分で追います。curl は指示しなければ画面に出すのが違いで、macOS には既定で入っていません。',
    'Ladt eine URL in eine Datei und folgt Weiterleitungen selbstandig; genau darin unterscheidet es sich von curl, das ohne weitere Angabe ins Terminal schreibt — und macOS liefert wget nicht mit.',
    "Telecharge une URL dans un fichier et suit les redirections de lui-meme ; c'est la difference avec curl, qui ecrit dans le terminal sauf indication contraire, et macOS ne le fournit pas.",
    'किसी URL को फ़ाइल में डाउनलोड करता है और रीडायरेक्ट अपने आप निभाता है; यही curl से अंतर है, जो कहे बिना टर्मिनल पर छापता है — और macOS में wget नहीं आता।',
    '把网址下载成文件，并且会自己跟随跳转；这正是它和 curl 的区别，curl 不特意指定就打印到终端，而 macOS 默认不带 wget。',
    '把網址下載成檔案，並且會自己跟隨轉址；這正是它和 curl 的區別，curl 不特意指定就印到終端機，而 macOS 預設不帶 wget。',
  ],

  ping: [
    'ICMP echo를 보내 상대가 답하는지, 얼마나 걸리는지 봅니다 — 답이 없다고 죽은 것은 아닙니다. 방화벽이 ICMP만 버리고 TCP는 잘 받는 경우가 흔합니다.',
    'Sends ICMP echo requests to see whether a host answers and how long it takes; no answer does not mean it is down, because plenty of firewalls drop ICMP while TCP works fine.',
    'Envia peticiones ICMP echo para ver si un equipo responde y cuanto tarda; que no responda no significa que este caido, porque muchos cortafuegos descartan ICMP mientras TCP funciona.',
    'Envia pedidos ICMP echo para ver se um host responde e quanto tempo leva; sem resposta nao significa que esta fora, porque muitos firewalls descartam ICMP enquanto TCP funciona.',
    'ICMP echo を送って相手が答えるか、どれだけかかるかを見ます。答えがなくても落ちているとは限りません — ICMP だけ捨てて TCP は通す防火壁が多いからです。',
    'Sendet ICMP-Echo-Anfragen, um zu sehen, ob ein Host antwortet und wie lange es dauert; keine Antwort heisst nicht, dass er tot ist, denn viele Firewalls verwerfen ICMP, wahrend TCP einwandfrei lauft.',
    "Envoie des requetes ICMP echo pour voir si un hote repond et en combien de temps ; l'absence de reponse ne signifie pas qu'il est en panne, car beaucoup de pare-feu jettent l'ICMP alors que le TCP fonctionne.",
    'ICMP echo भेजकर देखता है कि होस्ट जवाब देता है या नहीं और कितनी देर लगती है; जवाब न आना मतलब बंद होना नहीं, क्योंकि कई फ़ायरवॉल ICMP गिरा देते हैं जबकि TCP ठीक चलता है।',
    '发送 ICMP echo 请求，看对方是否应答、要多久；没有应答不等于机器挂了，很多防火墙只丢 ICMP 而 TCP 照常。',
    '發送 ICMP echo 請求，看對方是否回應、要多久；沒有回應不等於機器掛了，很多防火牆只丟 ICMP 而 TCP 照常。',
  ],

  traceroute: [
    '목적지까지 지나는 라우터를 한 줄씩 늘어놓습니다 — 중간에 `* * *`가 보이면 길이 끊긴 것이 아니라 그 라우터가 답하지 않기로 한 것입니다.',
    'Lists the routers a packet passes on the way to a destination; a `* * *` line in the middle does not mean the path is broken, only that that router declines to answer.',
    'Enumera los routers por los que pasa un paquete hasta el destino; una linea `* * *` en medio no significa que el camino este roto, solo que ese router no responde.',
    'Lista os roteadores pelos quais um pacote passa ate o destino; uma linha `* * *` no meio nao significa caminho rompido, apenas que aquele roteador nao responde.',
    '目的地までに通る経路装置を一行ずつ並べます。途中に `* * *` が出ても道が切れたのではなく、その装置が答えないだけです。',
    'Listet die Router auf, die ein Paket zum Ziel durchlauft; eine `* * *`-Zeile in der Mitte bedeutet nicht, dass der Weg unterbrochen ist, sondern nur, dass dieser Router nicht antwortet.',
    "Enumere les routeurs traverses par un paquet jusqu'a la destination ; une ligne `* * *` au milieu ne veut pas dire que le chemin est coupe, seulement que ce routeur refuse de repondre.",
    'गंतव्य तक पैकेट जिन राउटरों से गुज़रता है उन्हें एक-एक पंक्ति में दिखाता है; बीच में `* * *` का मतलब रास्ता टूटा नहीं, बस वह राउटर जवाब नहीं देता।',
    '逐行列出数据包到目的地途经的路由器；中间出现 `* * *` 不代表链路断了，只是那一跳不愿意回应。',
    '逐行列出封包到目的地途經的路由器；中間出現 `* * *` 不代表鏈路斷了，只是那一跳不願意回應。',
  ],

  dig: [
    'DNS 서버에 직접 물어 기록을 그대로 보여줍니다 — /etc/hosts와 시스템 캐시를 건너뛰기 때문에 브라우저가 보는 것과 다를 수 있고, 배포판에 따라 dnsutils나 bind-utils를 깔아야 있습니다.',
    'Asks a DNS server directly and shows the records as they come back; it bypasses /etc/hosts and the system cache, so the answer can differ from what the browser sees, and some distributions need dnsutils or bind-utils installed first.',
    'Pregunta directamente a un servidor DNS y muestra los registros tal cual; se salta /etc/hosts y la cache del sistema, asi que la respuesta puede diferir de lo que ve el navegador, y algunas distribuciones exigen instalar dnsutils o bind-utils.',
    'Pergunta direto a um servidor DNS e mostra os registros como voltam; ignora /etc/hosts e o cache do sistema, entao a resposta pode diferir do que o navegador ve, e algumas distribuicoes exigem instalar dnsutils ou bind-utils.',
    'DNS サーバーに直接尋ねて記録をそのまま見せます。/etc/hosts と系のキャッシュを飛ばすため、ブラウザーが見るものと違うことがあり、配布物によっては dnsutils か bind-utils の導入が必要です。',
    'Fragt einen DNS-Server direkt und zeigt die Records unverandert; es umgeht /etc/hosts und den Systemcache, die Antwort kann also von der des Browsers abweichen, und manche Distributionen brauchen erst dnsutils oder bind-utils.',
    "Interroge directement un serveur DNS et montre les enregistrements tels quels ; il contourne /etc/hosts et le cache du systeme, la reponse peut donc differer de celle du navigateur, et certaines distributions exigent d'installer dnsutils ou bind-utils.",
    'सीधे DNS सर्वर से पूछकर रिकॉर्ड जैसे आते हैं वैसे दिखाता है; यह /etc/hosts और सिस्टम कैश को छोड़ देता है, इसलिए उत्तर ब्राउज़र से भिन्न हो सकता है, और कुछ डिस्ट्रो में पहले dnsutils या bind-utils चाहिए।',
    '直接向 DNS 服务器提问并原样显示记录；它绕过 /etc/hosts 和系统缓存，所以结果可能和浏览器看到的不一样，有些发行版还要先装 dnsutils 或 bind-utils。',
    '直接向 DNS 伺服器提問並原樣顯示記錄；它繞過 /etc/hosts 和系統快取，所以結果可能和瀏覽器看到的不一樣，有些發行版還要先裝 dnsutils 或 bind-utils。',
  ],

  nslookup: [
    '이름을 주소로 바꿔 보는 옛 도구로 아직 돌기는 하지만, 출력 꼴과 종료 상태가 일정하지 않아 스크립트에는 맞지 않고 지금은 dig가 그 자리를 대신합니다.',
    'The older name-to-address lookup tool: it still runs, but its output layout and exit status are inconsistent enough to be useless in scripts, and dig has taken its place.',
    'La herramienta antigua para resolver nombres: aun funciona, pero su formato de salida y su codigo de salida son tan inconsistentes que no sirve en scripts, y dig ha ocupado su lugar.',
    'A ferramenta antiga de resolucao de nomes: ainda funciona, mas o formato de saida e o codigo de saida sao inconsistentes o bastante para nao servir em scripts, e o dig assumiu seu lugar.',
    '名前を住所に変える古い道具で、まだ動きますが、出力の形も終了状態も一定でないため台本には向かず、いまは dig がその役を引き継いでいます。',
    'Das altere Werkzeug fur Namensauflosung: es lauft noch, doch Ausgabeform und Ruckgabewert sind so uneinheitlich, dass es fur Skripte untauglich ist — dig hat seinen Platz eingenommen.',
    "L'ancien outil de resolution de noms : il fonctionne encore, mais son format de sortie et son code de retour sont trop irreguliers pour un script, et dig a pris sa place.",
    'नाम से पता निकालने का पुराना औज़ार: यह अब भी चलता है, पर इसका आउटपुट और exit status इतने अस्थिर हैं कि स्क्रिप्ट में बेकार हैं, और अब dig ने इसकी जगह ले ली है।',
    '把名字解析成地址的老工具：它还能跑，但输出格式和退出状态都不稳定，写脚本没法用，如今这活由 dig 接了过去。',
    '把名稱解析成位址的老工具：它還能跑，但輸出格式和結束狀態都不穩定，寫腳本沒法用，如今這活由 dig 接了過去。',
  ],

  host: [
    '이름을 물으면 주소와 메일 서버를 두어 줄로 짧게 답하는 도구입니다 — 빠른 확인에는 가장 편하지만 기록 전체를 보려면 dig를 써야 합니다.',
    'Answers a name lookup in two or three short lines of addresses and mail servers; it is the handiest for a quick check, but for the full record view you need dig.',
    'Responde una consulta de nombre en dos o tres lineas con direcciones y servidores de correo; es lo mas comodo para una comprobacion rapida, pero para ver todos los registros hace falta dig.',
    'Responde a uma consulta de nome em duas ou tres linhas curtas com enderecos e servidores de correio; e o mais pratico para uma checagem rapida, mas para ver todos os registros use dig.',
    '名前を尋ねると住所とメールサーバーを二、三行で短く答えます。手早い確認には一番楽ですが、記録をすべて見るには dig が必要です。',
    'Antwortet auf eine Namensabfrage mit zwei oder drei kurzen Zeilen zu Adressen und Mailservern; am bequemsten fur eine schnelle Prufung, doch fur die vollstandige Record-Ansicht braucht man dig.',
    "Repond a une requete de nom en deux ou trois lignes donnant adresses et serveurs de courrier ; c'est le plus pratique pour une verification rapide, mais pour voir tous les enregistrements il faut dig.",
    'नाम पूछने पर दो-तीन छोटी पंक्तियों में पते और मेल सर्वर बताता है; झटपट जाँच के लिए सबसे सुविधाजनक, पर पूरे रिकॉर्ड देखने के लिए dig चाहिए।',
    '查一个名字时用两三行短短地给出地址和邮件服务器；快速确认最省事，但要看完整记录还得用 dig。',
    '查一個名稱時用兩三行短短地給出位址和郵件伺服器；快速確認最省事，但要看完整記錄還得用 dig。',
  ],

  ss: [
    'Linux에서 열린 소켓을 늘어놓는 요즘 도구로, netstat 자리를 이어받았고 연결이 많은 기계에서 훨씬 빠릅니다 — 다만 `-p`로 프로세스 이름을 보려면 root여야 합니다.',
    'The modern way to list open sockets on Linux, the successor to netstat and much faster on a busy host; note that `-p` only reveals the owning process when you are root.',
    'La forma moderna de listar sockets abiertos en Linux, sucesora de netstat y mucho mas rapida en equipos con mucho trafico; ojo: `-p` solo muestra el proceso dueno si eres root.',
    'A forma moderna de listar sockets abertos no Linux, sucessora do netstat e muito mais rapida num host movimentado; note que `-p` so revela o processo dono quando voce e root.',
    'Linux で開いたソケットを並べる今どきの道具で、netstat の後継、接続が多い機械でずっと速いです。ただし `-p` で持ち主のプロセスを見るには root が必要です。',
    'Der moderne Weg, offene Sockets unter Linux aufzulisten — Nachfolger von netstat und auf belebten Hosts deutlich schneller; `-p` zeigt den besitzenden Prozess allerdings nur als root.',
    "La facon moderne de lister les sockets ouverts sous Linux, successeur de netstat et bien plus rapide sur une machine chargee ; attention, `-p` ne revele le processus proprietaire qu'en root.",
    'Linux पर खुले सॉकेट सूचीबद्ध करने का आधुनिक तरीक़ा, netstat का उत्तराधिकारी और व्यस्त होस्ट पर बहुत तेज़; ध्यान रहे `-p` मालिक प्रक्रिया केवल root होने पर दिखाता है।',
    '在 Linux 上列出已打开套接字的现代做法，是 netstat 的接班人，在连接很多的机器上快得多；注意 `-p` 只有以 root 运行时才显示占用进程。',
    '在 Linux 上列出已開啟 socket 的現代做法，是 netstat 的接班人，在連線很多的機器上快得多；注意 `-p` 只有以 root 執行時才顯示占用行程。',
  ],

  netstat: [
    '소켓과 경로표를 보여주는 옛 도구입니다 — Linux에서는 ss에 자리를 넘겼고 아예 안 깔려 있기도 하며, macOS에는 남아 있지만 소켓의 주인 프로세스를 알려주지 못해 그 답은 lsof가 줍니다.',
    'The old way to see sockets and the routing table; on Linux it has been handed over to ss and is often not installed at all, and although macOS keeps it, it cannot tell you which process owns a socket, so lsof answers that.',
    'La forma antigua de ver sockets y la tabla de rutas; en Linux ha cedido su puesto a ss y muchas veces no esta instalada, y aunque macOS la conserva, no dice que proceso posee un socket: eso lo responde lsof.',
    'A forma antiga de ver sockets e a tabela de rotas; no Linux ela deu lugar ao ss e muitas vezes nem esta instalada, e embora o macOS a mantenha, ela nao diz qual processo possui um socket: isso o lsof responde.',
    'ソケットと経路表を見る古い道具です。Linux では ss に座を譲り、そもそも入っていないこともあり、macOS には残っていますがソケットの持ち主のプロセスは分からないので、その答えは lsof が出します。',
    'Der alte Weg zu Sockets und Routingtabelle; unter Linux hat ss ubernommen und oft ist es gar nicht installiert, und obwohl macOS es behalt, verrat es nicht, welcher Prozess einen Socket besitzt — das beantwortet lsof.',
    "L'ancienne facon de voir les sockets et la table de routage ; sous Linux ss a pris le relais et il n'est souvent meme pas installe, et bien que macOS le conserve, il ne dit pas quel processus possede un socket : c'est lsof qui repond.",
    'सॉकेट और रूटिंग टेबल देखने का पुराना तरीक़ा; Linux पर इसकी जगह ss ने ले ली है और अक्सर यह स्थापित ही नहीं होता, और macOS इसे रखता है पर यह नहीं बताता कि सॉकेट किस प्रक्रिया का है — वह उत्तर lsof देता है।',
    '查看套接字和路由表的老办法；在 Linux 上这活已交给 ss，而且常常根本没装，macOS 虽然保留了它，却无法说出套接字属于哪个进程，那要靠 lsof。',
    '查看 socket 和路由表的老辦法；在 Linux 上這活已交給 ss，而且常常根本沒裝，macOS 雖然保留了它，卻無法說出 socket 屬於哪個行程，那要靠 lsof。',
  ],

  ip: [
    'Linux에서 주소·경로·이웃표를 모두 다루는 하나의 명령으로 ifconfig와 route와 arp를 대신합니다 — 여기서 바꾼 것은 재부팅하면 사라지니, 남기려면 네트워크 설정 파일에 적어야 합니다.',
    'The single Linux command for addresses, routes and the neighbour table, replacing ifconfig, route and arp; anything you change with it is gone after a reboot unless you also write it into the network configuration.',
    'El unico comando de Linux para direcciones, rutas y tabla de vecinos, que sustituye a ifconfig, route y arp; lo que cambies con el desaparece al reiniciar salvo que lo escribas tambien en la configuracion de red.',
    'O comando unico do Linux para enderecos, rotas e tabela de vizinhos, substituindo ifconfig, route e arp; o que voce mudar com ele desaparece no reboot, a menos que escreva tambem na configuracao de rede.',
    'Linux で住所・経路・近隣表をすべて扱う一つの命令で、ifconfig と route と arp を置き換えます。ここで変えたものは再起動で消えるため、残すには網の設定ファイルにも書きます。',
    'Der eine Linux-Befehl fur Adressen, Routen und Nachbartabelle, der ifconfig, route und arp ersetzt; alles damit Geanderte ist nach einem Neustart weg, sofern es nicht auch in der Netzkonfiguration steht.',
    "La commande Linux unique pour les adresses, les routes et la table de voisins, qui remplace ifconfig, route et arp ; tout changement fait avec elle disparait au redemarrage, sauf s'il est aussi ecrit dans la configuration reseau.",
    'Linux में पते, रूट और neighbour तालिका सब संभालने वाला एक ही आदेश, जो ifconfig, route और arp की जगह लेता है; इससे किया कोई भी बदलाव रीबूट पर मिट जाता है, जब तक नेटवर्क कॉन्फ़िग में भी न लिखा जाए।',
    'Linux 上统管地址、路由和邻居表的单一命令，取代了 ifconfig、route 和 arp；用它改的东西一重启就没了，要长期生效还得写进网络配置。',
    'Linux 上統管位址、路由和鄰居表的單一命令，取代了 ifconfig、route 和 arp；用它改的東西一重開機就沒了，要長期生效還得寫進網路設定。',
  ],

  ifconfig: [
    '네트워크 인터페이스를 보고 손대는 옛 명령입니다 — Linux에서는 `ip addr`가 대신하고 아예 안 깔려 있기도 하며 ip로 더한 주소를 다 못 보여주지만, macOS에서는 여전히 이것이 진짜 도구입니다.',
    'The old command for looking at and touching network interfaces; on Linux `ip addr` has replaced it, it may not even be installed, and it cannot show every address ip added — on macOS, though, it is still the real tool.',
    'El comando antiguo para ver y tocar interfaces de red; en Linux `ip addr` lo ha sustituido, puede que ni este instalado y no muestra todas las direcciones anadidas con ip, pero en macOS sigue siendo la herramienta real.',
    'O comando antigo para ver e mexer em interfaces de rede; no Linux `ip addr` o substituiu, pode nem estar instalado e nao mostra todos os enderecos adicionados por ip — no macOS, porem, ele ainda e a ferramenta de verdade.',
    '網の口を見て触る古い命令です。Linux では `ip addr` が代わりを務め、そもそも入っていないこともあり、ip で足した住所をすべて出せませんが、macOS では今もこれが本物の道具です。',
    'Der alte Befehl zum Ansehen und Andern von Netzschnittstellen; unter Linux hat `ip addr` ihn ersetzt, oft ist er nicht installiert und zeigt nicht alle mit ip gesetzten Adressen — unter macOS ist er dagegen weiter das eigentliche Werkzeug.',
    "L'ancienne commande pour consulter et modifier les interfaces reseau ; sous Linux `ip addr` l'a remplacee, elle n'est parfois pas installee et n'affiche pas toutes les adresses ajoutees par ip — sous macOS, en revanche, elle reste l'outil de reference.",
    'नेटवर्क इंटरफ़ेस देखने और बदलने का पुराना आदेश; Linux में `ip addr` ने इसकी जगह ले ली है, यह स्थापित ही न हो सकता है और ip से जोड़े सभी पते नहीं दिखाता — पर macOS में यह आज भी असली औज़ार है।',
    '查看和修改网络接口的老命令；在 Linux 上已由 `ip addr` 取代，甚至可能没装，而且看不到用 ip 添加的全部地址——不过在 macOS 上它仍然是正主。',
    '查看和修改網路介面的老命令；在 Linux 上已由 `ip addr` 取代，甚至可能沒裝，而且看不到用 ip 新增的全部位址——不過在 macOS 上它仍然是正主。',
  ],

  route: [
    '커널 경로표를 보고 고치는 명령이지만 Linux에서는 `ip route`로 넘어갔고, BSD/macOS에서는 `route -n get default`처럼 문법이 아예 달라서 두 쪽 예시를 섞으면 통하지 않습니다.',
    'Reads and edits the kernel routing table, but on Linux it has given way to `ip route`, and on BSD/macOS the grammar is different altogether, such as `route -n get default`, so examples do not carry across.',
    'Lee y edita la tabla de rutas del nucleo, pero en Linux ha cedido el paso a `ip route`, y en BSD/macOS la sintaxis es distinta, como `route -n get default`, asi que los ejemplos no se trasladan.',
    'Le e edita a tabela de rotas do kernel, mas no Linux cedeu lugar a `ip route`, e no BSD/macOS a sintaxe e outra, como `route -n get default`, entao os exemplos nao servem nos dois lados.',
    'カーネルの経路表を見て直す命令ですが、Linux では `ip route` に移り、BSD/macOS では `route -n get default` のように文法が別物なので、両者の例を混ぜても通りません。',
    'Liest und bearbeitet die Kernel-Routingtabelle, doch unter Linux hat `ip route` ubernommen, und unter BSD/macOS ist die Syntax vollig anders, etwa `route -n get default` — Beispiele lassen sich also nicht ubertragen.',
    "Lit et modifie la table de routage du noyau, mais sous Linux `ip route` a pris la suite, et sous BSD/macOS la syntaxe est tout autre, comme `route -n get default` : les exemples ne se transposent pas.",
    'कर्नेल की रूटिंग टेबल पढ़ता और बदलता है, पर Linux में इसकी जगह `ip route` ने ले ली है, और BSD/macOS में व्याकरण ही अलग है, जैसे `route -n get default` — इसलिए उदाहरण दोनों जगह नहीं चलते।',
    '读取和修改内核路由表，但在 Linux 上这活已让给 `ip route`，而 BSD/macOS 的语法完全不同，例如 `route -n get default`，两边的例子不能互抄。',
    '讀取和修改核心路由表，但在 Linux 上這活已讓給 `ip route`，而 BSD/macOS 的語法完全不同，例如 `route -n get default`，兩邊的例子不能互抄。',
  ],

  arp: [
    '같은 랜에 있는 이웃의 MAC 주소를 모아둔 표를 보여줍니다 — 라우터를 넘어간 상대는 절대 여기 없고, Linux에서는 `ip neigh`가 요즘 도구입니다.',
    'Shows the table of MAC addresses for neighbours on the same local network; anything beyond the router is never in it, and on Linux `ip neigh` is the current tool.',
    'Muestra la tabla de direcciones MAC de los vecinos de la misma red local; nada mas alla del router aparece ahi, y en Linux la herramienta actual es `ip neigh`.',
    'Mostra a tabela de enderecos MAC dos vizinhos na mesma rede local; nada alem do roteador aparece ali, e no Linux a ferramenta atual e `ip neigh`.',
    '同じ構内網にいる近隣の MAC 住所をためた表を見せます。経路装置の向こう側は決してここに載らず、Linux では `ip neigh` が今の道具です。',
    'Zeigt die Tabelle der MAC-Adressen von Nachbarn im selben lokalen Netz; alles jenseits des Routers steht dort nie, und unter Linux ist `ip neigh` das aktuelle Werkzeug.',
    "Affiche la table des adresses MAC des voisins du meme reseau local ; rien au-dela du routeur n'y figure, et sous Linux l'outil actuel est `ip neigh`.",
    'एक ही स्थानीय नेटवर्क के पड़ोसियों के MAC पतों की तालिका दिखाता है; राउटर के उस पार का कुछ भी इसमें कभी नहीं होता, और Linux में आजकल का औज़ार `ip neigh` है।',
    '显示同一局域网内邻居的 MAC 地址表；路由器之外的机器绝不会出现在里面，而 Linux 上现在的工具是 `ip neigh`。',
    '顯示同一區域網路內鄰居的 MAC 位址表；路由器之外的機器絕不會出現在裡面，而 Linux 上現在的工具是 `ip neigh`。',
  ],

  nc: [
    'TCP·UDP를 맨손으로 붙여 포트가 답하는지 보고 두 기계 사이에 관을 놓습니다 — 이름이 같고 서로 안 맞는 판이 셋(OpenBSD·GNU·Nmap ncat) 있어서 `-z`나 `-p`가 없을 수도 있습니다.',
    'Opens a raw TCP or UDP connection to see whether a port answers, and pipes bytes between two machines; three incompatible netcats share the name (OpenBSD, GNU, Nmap ncat), so `-z` or `-p` may simply not exist on yours.',
    'Abre una conexion TCP o UDP en crudo para ver si un puerto responde y canaliza bytes entre dos maquinas; tres netcat incompatibles comparten el nombre (OpenBSD, GNU, ncat de Nmap), asi que `-z` o `-p` puede que no existan en el tuyo.',
    'Abre uma conexao TCP ou UDP crua para ver se uma porta responde e encaminha bytes entre duas maquinas; tres netcats incompativeis dividem o nome (OpenBSD, GNU, ncat do Nmap), entao `-z` ou `-p` pode simplesmente nao existir no seu.',
    'TCP や UDP を素のままつないで、ポートが答えるかを見たり二台の間に管を通したりします。名前が同じで互換のない版が三つ(OpenBSD・GNU・Nmap の ncat)あり、`-z` や `-p` が無いこともあります。',
    'Offnet eine rohe TCP- oder UDP-Verbindung, um zu prufen, ob ein Port antwortet, und leitet Bytes zwischen zwei Maschinen; drei untereinander unvertragliche netcats teilen den Namen (OpenBSD, GNU, ncat von Nmap), daher fehlen `-z` oder `-p` moglicherweise.',
    "Ouvre une connexion TCP ou UDP brute pour voir si un port repond et fait passer des octets entre deux machines ; trois netcat incompatibles portent le meme nom (OpenBSD, GNU, ncat de Nmap), donc `-z` ou `-p` peut tout simplement ne pas exister chez vous.",
    'कच्चा TCP या UDP कनेक्शन खोलकर देखता है कि पोर्ट जवाब देता है या नहीं, और दो मशीनों के बीच बाइट बहाता है; एक ही नाम के तीन असंगत netcat हैं (OpenBSD, GNU, Nmap का ncat), इसलिए `-z` या `-p` आपके यहाँ हो भी सकता है और नहीं भी।',
    '开一条裸的 TCP 或 UDP 连接来看端口是否应答，也能在两台机器间当管道；同名却互不兼容的 netcat 有三种（OpenBSD、GNU、Nmap 的 ncat），所以 `-z` 或 `-p` 在你这台上可能根本没有。',
    '開一條裸的 TCP 或 UDP 連線來看埠是否回應，也能在兩台機器間當管線；同名卻互不相容的 netcat 有三種（OpenBSD、GNU、Nmap 的 ncat），所以 `-z` 或 `-p` 在你這台上可能根本沒有。',
  ],

  telnet: [
    '평문 TCP 연결을 열어 주고받은 것이 그대로 회선에 흐르니 로그인에는 절대 쓰지 않고(그건 ssh), 지금 남은 쓸모는 포트가 답하는지 보는 것뿐이며 나올 때는 Ctrl+] 다음에 quit입니다.',
    'Opens a plain TCP session in which everything travels in clear text, so it must never be used to log in (that is ssh); its remaining use is checking whether a port answers, and you leave with Ctrl+] then quit.',
    'Abre una sesion TCP en claro donde todo viaja sin cifrar, asi que nunca debe usarse para iniciar sesion (para eso ssh); su uso restante es comprobar si un puerto responde, y se sale con Ctrl+] y luego quit.',
    'Abre uma sessao TCP em texto puro na qual tudo viaja sem cifra, entao nunca deve ser usada para login (isso e ssh); o uso que resta e checar se uma porta responde, e voce sai com Ctrl+] e depois quit.',
    '平文の TCP 接続を開き、やり取りがそのまま回線を流れるためログインには決して使わず(それは ssh)、いま残る用途はポートが答えるかを見ることだけで、抜けるときは Ctrl+] のあと quit です。',
    'Offnet eine unverschlusselte TCP-Sitzung, in der alles im Klartext lauft — zum Anmelden also nie verwenden (dafur ssh); geblieben ist der Zweck zu prufen, ob ein Port antwortet, und man verlasst es mit Ctrl+] und quit.',
    "Ouvre une session TCP en clair ou tout circule sans chiffrement : ne l'utilisez jamais pour vous connecter (c'est le role de ssh) ; son seul usage restant est de verifier si un port repond, et on en sort par Ctrl+] puis quit.",
    'सादा TCP सत्र खोलता है जिसमें सब कुछ बिना एन्क्रिप्शन जाता है, इसलिए लॉगिन के लिए इसे कभी न लें (वह ssh का काम है); बचा हुआ उपयोग यह देखना है कि पोर्ट जवाब देता है या नहीं, और निकलने के लिए Ctrl+] फिर quit।',
    '开一条明文 TCP 会话，来回的内容原样走在线路上，所以绝不能用它登录（那是 ssh 的活）；它剩下的用处只是看端口是否应答，退出要按 Ctrl+] 再输 quit。',
    '開一條明文 TCP 工作階段，來回的內容原樣走在線路上，所以絕不能用它登入（那是 ssh 的活）；它剩下的用處只是看埠是否回應，離開要按 Ctrl+] 再輸 quit。',
  ],

  ssh: [
    '다른 기계에 암호화된 셸을 엽니다 — 비밀 키 파일이 남에게도 읽히면 ssh는 아예 거부하니 `chmod 600`이 필요하고, 포트는 여기서 `-p`인데 scp와 sftp에서는 `-P`입니다.',
    'Opens an encrypted shell on another machine; ssh flatly refuses a private key that others can read, so `chmod 600` it, and the port is `-p` here but `-P` in scp and sftp.',
    'Abre una shell cifrada en otra maquina; ssh rechaza de plano una clave privada que otros puedan leer, asi que aplicale `chmod 600`, y el puerto es `-p` aqui pero `-P` en scp y sftp.',
    'Abre um shell cifrado em outra maquina; o ssh recusa de saida uma chave privada que outros possam ler, entao use `chmod 600`, e a porta e `-p` aqui mas `-P` no scp e no sftp.',
    '別の機械に暗号化された shell を開きます。秘密鍵が他人にも読める許可なら ssh は端から拒むので `chmod 600` が必要で、ポートはここでは `-p` ですが scp と sftp では `-P` です。',
    'Offnet eine verschlusselte Shell auf einer anderen Maschine; ssh verweigert einen privaten Schlussel, den andere lesen konnen, kategorisch — also `chmod 600` — und der Port heisst hier `-p`, bei scp und sftp aber `-P`.',
    "Ouvre un shell chiffre sur une autre machine ; ssh refuse categoriquement une cle privee lisible par autrui, faites-lui donc `chmod 600`, et le port s'ecrit `-p` ici mais `-P` avec scp et sftp.",
    'दूसरी मशीन पर एन्क्रिप्टेड शेल खोलता है; जिस निजी कुंजी को दूसरे पढ़ सकें उसे ssh सिरे से मना कर देता है, इसलिए `chmod 600` करें, और पोर्ट यहाँ `-p` है पर scp तथा sftp में `-P`।',
    '在另一台机器上打开一个加密的 shell；私钥文件只要别人也能读，ssh 就直接拒绝，所以要 `chmod 600`，而端口在这里写 `-p`，到了 scp 和 sftp 里却是 `-P`。',
    '在另一台機器上開一個加密的 shell；私鑰檔只要別人也能讀，ssh 就直接拒絕，所以要 `chmod 600`，而埠在這裡寫 `-p`，到了 scp 和 sftp 裡卻是 `-P`。',
  ],

  'ssh-keygen': [
    '공개 키와 비밀 키 한 쌍을 만듭니다 — 서버에 올리는 것은 `.pub` 쪽뿐이고, 암구호 없이 만든 키는 그 파일 하나만 있으면 누구든 그대로 로그인할 수 있는 상태입니다.',
    'Creates a public and private key pair; only the `.pub` half ever goes on a server, and a key made without a passphrase is a plain file that anyone who copies it can log in with.',
    'Crea un par de clave publica y privada; a un servidor solo sube la mitad `.pub`, y una clave hecha sin frase de paso es un archivo suelto con el que puede entrar cualquiera que lo copie.',
    'Cria um par de chave publica e privada; para o servidor vai apenas a metade `.pub`, e uma chave feita sem passphrase e um arquivo simples com o qual qualquer um que o copie consegue entrar.',
    '公開鍵と秘密鍵の対を作ります。サーバーに置くのは `.pub` の側だけで、合言葉なしで作った鍵はそのファイル一つあれば誰でもそのまま入れてしまう状態です。',
    'Erzeugt ein Schlusselpaar aus offentlichem und privatem Teil; auf den Server kommt nur die `.pub`-Halfte, und ein ohne Passphrase erzeugter Schlussel ist eine einfache Datei, mit der sich jeder anmelden kann, der sie kopiert.',
    "Cree une paire de cles publique et privee ; seule la moitie `.pub` va sur un serveur, et une cle creee sans phrase secrete est un simple fichier avec lequel quiconque le copie peut se connecter.",
    'सार्वजनिक और निजी कुंजी की जोड़ी बनाता है; सर्वर पर केवल `.pub` वाला आधा जाता है, और बिना passphrase बनाई कुंजी एक सादी फ़ाइल है जिसे कोई भी कॉपी करके लॉगिन कर सकता है।',
    '生成一对公钥和私钥；放到服务器上的只有 `.pub` 那一半，而不设口令生成的密钥就是一个普通文件，谁复制走都能直接登录。',
    '產生一對公鑰和私鑰；放到伺服器上的只有 `.pub` 那一半，而不設口令產生的金鑰就是一個普通檔案，誰複製走都能直接登入。',
  ],

  'ssh-copy-id': [
    '내 공개 키를 서버의 ~/.ssh/authorized_keys에 붙여 넣어 비밀번호를 그만 묻게 만듭니다 — 처음 한 번은 비밀번호 로그인이 열려 있어야 하고, `-i`에는 비밀 키가 아니라 `.pub` 파일을 줍니다.',
    "Appends your public key to the server's ~/.ssh/authorized_keys so it stops asking for a password; password login has to still be open for that first run, and `-i` takes the `.pub` file, not the private key.",
    'Agrega tu clave publica al ~/.ssh/authorized_keys del servidor para que deje de pedir contrasena; el acceso por contrasena debe seguir abierto en esa primera vez, y `-i` recibe el archivo `.pub`, no la clave privada.',
    'Acrescenta sua chave publica ao ~/.ssh/authorized_keys do servidor para ele parar de pedir senha; o login por senha precisa continuar aberto nessa primeira vez, e `-i` recebe o arquivo `.pub`, nao a chave privada.',
    '自分の公開鍵をサーバーの ~/.ssh/authorized_keys に書き足して、合言葉を尋ねられないようにします。最初の一度は合言葉での login が開いている必要があり、`-i` には秘密鍵ではなく `.pub` を渡します。',
    'Hangt Ihren offentlichen Schlussel an die ~/.ssh/authorized_keys des Servers, damit er nicht mehr nach einem Passwort fragt; fur diesen ersten Lauf muss die Passwortanmeldung noch offen sein, und `-i` erwartet die `.pub`-Datei, nicht den privaten Schlussel.',
    "Ajoute votre cle publique au ~/.ssh/authorized_keys du serveur pour qu'il cesse de demander un mot de passe ; la connexion par mot de passe doit encore etre ouverte pour cette premiere fois, et `-i` attend le fichier `.pub`, pas la cle privee.",
    'आपकी सार्वजनिक कुंजी को सर्वर के ~/.ssh/authorized_keys में जोड़ देता है ताकि वह पासवर्ड न माँगे; उस पहली बार के लिए पासवर्ड लॉगिन खुला होना चाहिए, और `-i` को निजी कुंजी नहीं, `.pub` फ़ाइल दें।',
    '把你的公钥追加到服务器的 ~/.ssh/authorized_keys，好让它不再问密码；第一次做这件事时密码登录必须还开着，而 `-i` 要给 `.pub` 文件而不是私钥。',
    '把你的公鑰附加到伺服器的 ~/.ssh/authorized_keys，好讓它不再問密碼；第一次做這件事時密碼登入必須還開著，而 `-i` 要給 `.pub` 檔而不是私鑰。',
  ],

  sftp: [
    'ssh 연결 위에서 파일을 주고받는 대화식 도구입니다 — 포트가 ssh의 `-p`가 아니라 대문자 `-P`이고, 원격 셸 명령은 못 돌리며 put·get·ls 같은 자기 명령만 압니다.',
    'An interactive file transfer tool riding on the ssh connection; the port flag is a capital `-P` rather than ssh\'s `-p`, and it cannot run remote shell commands, only its own put, get and ls.',
    'Una herramienta interactiva de transferencia sobre la conexion ssh; la opcion de puerto es `-P` en mayuscula, no la `-p` de ssh, y no ejecuta comandos remotos: solo sus propios put, get y ls.',
    'Uma ferramenta interativa de transferencia sobre a conexao ssh; a opcao de porta e `-P` maiusculo, nao o `-p` do ssh, e ela nao roda comandos remotos: apenas seus proprios put, get e ls.',
    'ssh の接続の上でファイルをやり取りする対話式の道具です。ポートは ssh の `-p` ではなく大文字の `-P` で、遠隔の shell 命令は動かせず put・get・ls など自前の命令だけを解します。',
    'Ein interaktives Ubertragungswerkzeug auf der ssh-Verbindung; der Port ist ein grosses `-P` statt des `-p` von ssh, und es kann keine Remote-Shell-Befehle ausfuhren, nur eigene wie put, get und ls.',
    "Un outil interactif de transfert de fichiers porte par la connexion ssh ; l'option de port est un `-P` majuscule et non le `-p` de ssh, et il n'execute pas de commandes distantes : seulement ses propres put, get et ls.",
    'ssh कनेक्शन के ऊपर चलने वाला संवादात्मक फ़ाइल-स्थानांतरण औज़ार; पोर्ट का फ़्लैग ssh वाले `-p` की जगह बड़ा `-P` है, और यह रिमोट शेल आदेश नहीं चला सकता, केवल अपने put, get और ls समझता है।',
    '架在 ssh 连接之上的交互式传输工具；端口选项是大写 `-P` 而不是 ssh 的 `-p`，而且它不能执行远端 shell 命令，只认自己的 put、get、ls。',
    '架在 ssh 連線之上的互動式傳輸工具；埠選項是大寫 `-P` 而不是 ssh 的 `-p`，而且它不能執行遠端 shell 命令，只認自己的 put、get、ls。',
  ],

  scp: [
    'ssh를 타고 파일을 한 번에 복사합니다 — 포트는 대문자 `-P`이고 원격 쪽 경로에는 콜론이 필요하며, 크거나 되풀이하는 전송이면 안 바뀐 것을 건너뛰고 이어받는 rsync가 낫습니다.',
    'Copies files over ssh in one shot; the port is a capital `-P`, the remote side needs a colon in its path, and for anything large or repeated rsync is better because it skips unchanged files and resumes.',
    'Copia archivos por ssh de una vez; el puerto es `-P` en mayuscula, la ruta remota necesita dos puntos, y para transferencias grandes o repetidas rsync es mejor porque omite lo que no cambio y reanuda.',
    'Copia arquivos por ssh de uma vez; a porta e `-P` maiusculo, o lado remoto precisa de dois-pontos no caminho, e para algo grande ou repetido o rsync e melhor porque pula o que nao mudou e retoma.',
    'ssh に乗せてファイルを一度で複写します。ポートは大文字の `-P`、遠隔側の道にはコロンが必要で、大きい転送や繰り返す転送なら、変わっていないものを飛ばして再開できる rsync のほうが向きます。',
    'Kopiert Dateien in einem Zug uber ssh; der Port ist ein grosses `-P`, der entfernte Pfad braucht einen Doppelpunkt, und fur Grosses oder Wiederholtes ist rsync besser, weil es Unveranderte uberspringt und fortsetzt.',
    "Copie des fichiers via ssh en une seule fois ; le port est un `-P` majuscule, le chemin distant exige deux-points, et pour du volumineux ou du repete rsync vaut mieux car il saute l'inchange et reprend.",
    'ssh के ज़रिये फ़ाइलें एक बार में कॉपी करता है; पोर्ट बड़ा `-P` है, रिमोट पथ में कोलन चाहिए, और बड़े या बार-बार होने वाले स्थानांतरण के लिए rsync बेहतर है क्योंकि वह अपरिवर्तित फ़ाइलें छोड़ देता और अधूरा काम आगे बढ़ाता है।',
    '通过 ssh 一次性复制文件；端口是大写 `-P`，远端路径要带冒号，而体量大或要反复传的场合 rsync 更合适，因为它会跳过没变的文件并能续传。',
    '透過 ssh 一次性複製檔案；埠是大寫 `-P`，遠端路徑要帶冒號，而體量大或要反覆傳的場合 rsync 更合適，因為它會跳過沒變的檔案並能續傳。',
  ],

  iptables: [
    '지금 돌고 있는 패킷 필터를 고칩니다 — 규칙은 위에서 아래로 먼저 맞는 것이 이기므로 ACCEPT 뒤에 붙인 DROP은 영원히 안 걸리고, 저장하지 않으면 재부팅에 사라지며, 22번을 열기 전에 기본 정책을 DROP으로 두면 원격 기계에서 스스로 잠깁니다.',
    'Edits the live packet filter; rules are matched top to bottom and the first match wins, so a DROP appended after an ACCEPT never fires, the rules vanish at reboot unless saved, and setting the default policy to DROP before allowing port 22 locks you out of a remote machine.',
    'Edita el filtro de paquetes en vivo; las reglas se evaluan de arriba abajo y gana la primera coincidencia, asi que un DROP anadido tras un ACCEPT nunca actua, las reglas se pierden al reiniciar si no las guardas, y poner la politica por defecto en DROP antes de permitir el puerto 22 te deja fuera de una maquina remota.',
    'Edita o filtro de pacotes em execucao; as regras sao avaliadas de cima para baixo e a primeira que casa vence, entao um DROP colocado depois de um ACCEPT nunca dispara, as regras desaparecem no reboot se nao salvas, e definir a politica padrao como DROP antes de liberar a porta 22 tranca voce fora de uma maquina remota.',
    'いま動いている包の濾過規則を書き換えます。規則は上から下へ見て最初に合ったものが勝つので、ACCEPT のあとに足した DROP は永遠に効かず、保存しなければ再起動で消え、22 番を開ける前に既定方針を DROP にすると遠隔の機械で自分を締め出します。',
    'Bearbeitet den laufenden Paketfilter; Regeln werden von oben nach unten geprüft und die erste Ubereinstimmung gewinnt, ein nach einem ACCEPT angehangtes DROP greift also nie, ungespeicherte Regeln sind nach dem Neustart weg, und eine Standardpolitik DROP vor der Freigabe von Port 22 sperrt Sie aus einer entfernten Maschine aus.',
    "Modifie le filtre de paquets en cours ; les regles sont examinees de haut en bas et la premiere qui correspond gagne, donc un DROP ajoute apres un ACCEPT ne se declenche jamais, les regles disparaissent au redemarrage si elles ne sont pas sauvegardees, et passer la politique par defaut a DROP avant d'autoriser le port 22 vous enferme dehors sur une machine distante.",
    'चालू पैकेट फ़िल्टर को बदलता है; नियम ऊपर से नीचे जाँचे जाते हैं और पहला मेल जीतता है, इसलिए ACCEPT के बाद जोड़ा गया DROP कभी नहीं चलता, बिना सहेजे नियम रीबूट पर मिट जाते हैं, और पोर्ट 22 खोलने से पहले डिफ़ॉल्ट नीति DROP करने पर दूरस्थ मशीन से आप ही बाहर हो जाते हैं।',
    '修改正在生效的包过滤规则；规则自上而下匹配、先命中者胜，所以追加在 ACCEPT 之后的 DROP 永远不会生效，不保存的话重启就没了，而在放通 22 端口之前把默认策略设成 DROP，会把你自己关在远程机器外面。',
    '修改正在生效的封包過濾規則；規則自上而下比對、先命中者勝，所以附加在 ACCEPT 之後的 DROP 永遠不會生效，不儲存的話重開機就沒了，而在放通 22 埠之前把預設政策設成 DROP，會把你自己關在遠端機器外面。',
  ],

  ufw: [
    'Debian·Ubuntu에서 iptables를 쉬운 낱말로 감싼 방화벽입니다 — `enable` 전에 SSH를 먼저 열어야 지금 붙어 있는 연결이 끊기지 않고, Docker는 포트를 iptables에 직접 꽂기 때문에 ufw 규칙이 그 포트를 막지 못합니다.',
    'A friendly wrapper around iptables on Debian and Ubuntu; allow SSH before you `enable` it or you cut the session you are sitting in, and Docker publishes ports straight into iptables where ufw rules do not cover them.',
    'Una envoltura amable de iptables en Debian y Ubuntu; permite SSH antes de hacer `enable` o cortaras la sesion en la que estas, y Docker publica puertos directamente en iptables, donde las reglas de ufw no llegan.',
    'Um invólucro amigavel do iptables no Debian e no Ubuntu; libere SSH antes de dar `enable` ou voce corta a propria sessao, e o Docker publica portas direto no iptables, onde as regras do ufw nao alcancam.',
    'Debian・Ubuntu で iptables を易しい語で包んだ防火壁です。`enable` の前に SSH を開けておかないと、いま座っている接続を自分で切ってしまい、Docker はポートを iptables に直接差すため ufw の規則では塞げません。',
    'Eine freundliche Hulle um iptables unter Debian und Ubuntu; SSH freigeben, bevor Sie `enable` ausfuhren, sonst trennen Sie die Sitzung, in der Sie sitzen — und Docker veroffentlicht Ports direkt in iptables, wo ufw-Regeln nicht greifen.',
    "Une surcouche conviviale d'iptables sous Debian et Ubuntu ; autorisez SSH avant de faire `enable`, sinon vous coupez la session ou vous vous trouvez, et Docker publie ses ports directement dans iptables, hors de portee des regles ufw.",
    'Debian और Ubuntu पर iptables को आसान शब्दों में लपेटने वाला फ़ायरवॉल; `enable` से पहले SSH खोल लें वरना आप अपनी ही चालू सत्र काट देंगे, और Docker पोर्ट सीधे iptables में डालता है जहाँ ufw के नियम नहीं पहुँचते।',
    '在 Debian 和 Ubuntu 上用简单词汇包装 iptables 的防火墙；`enable` 之前先放通 SSH，否则会切断你正坐着的这条连接，而 Docker 把端口直接插进 iptables，ufw 的规则管不到那里。',
    '在 Debian 和 Ubuntu 上用簡單詞彙包裝 iptables 的防火牆；`enable` 之前先放通 SSH，否則會切斷你正坐著的這條連線，而 Docker 把埠直接插進 iptables，ufw 的規則管不到那裡。',
  ],

  tcpdump: [
    '회선을 지나는 패킷을 그대로 잡아 보여줍니다 — root가 필요하고, 무엇을 잡을지는 옵션이 아니라 뒤에 붙이는 자기만의 필터 문법(`port 443 and host x`)이며, 바쁜 인터페이스에서 필터 없이 잡으면 화면이 순식간에 파묻힙니다.',
    'Captures the packets going past on the wire; it needs root, what to capture is written after the flags in its own filter syntax such as `port 443 and host x`, and capturing without a filter on a busy interface buries the terminal instantly.',
    'Captura los paquetes que pasan por el cable; requiere root, lo que se captura se escribe tras las opciones en su propia sintaxis de filtro como `port 443 and host x`, y capturar sin filtro en una interfaz cargada sepulta la terminal al instante.',
    'Captura os pacotes que passam no cabo; exige root, o que capturar se escreve depois das opcoes na sintaxe propria de filtro como `port 443 and host x`, e capturar sem filtro numa interface movimentada soterra o terminal na hora.',
    '回線を通る包をそのまま捕らえて見せます。root が必要で、何を捕らえるかはオプションではなく後ろに置く独自の絞り込み文法(`port 443 and host x`)であり、混んだ口で絞らずに捕らえると画面はたちまち埋まります。',
    'Fangt die Pakete ab, die uber die Leitung gehen; es braucht root, was gefangen wird steht nach den Optionen in eigener Filtersyntax wie `port 443 and host x`, und ohne Filter auf einer belebten Schnittstelle ist das Terminal sofort begraben.',
    "Capture les paquets qui passent sur le fil ; il faut root, ce qu'on capture s'ecrit apres les options dans sa propre syntaxe de filtre comme `port 443 and host x`, et capturer sans filtre sur une interface chargee ensevelit le terminal instantanement.",
    'तार से गुज़र रहे पैकेट जैसे हैं वैसे पकड़ लेता है; इसे root चाहिए, क्या पकड़ना है यह फ़्लैग के बाद अपनी अलग फ़िल्टर भाषा में लिखा जाता है जैसे `port 443 and host x`, और व्यस्त इंटरफ़ेस पर बिना फ़िल्टर पकड़ने से टर्मिनल तुरंत दब जाता है।',
    '把线路上经过的数据包原样抓下来；它需要 root，抓什么写在选项之后、用它自己的过滤语法如 `port 443 and host x`，而在繁忙接口上不加过滤直接抓，屏幕瞬间就被埋掉。',
    '把線路上經過的封包原樣抓下來；它需要 root，抓什麼寫在選項之後、用它自己的過濾語法如 `port 443 and host x`，而在繁忙介面上不加過濾直接抓，畫面瞬間就被埋掉。',
  ],

  whois: [
    '도메인이나 IP 대역의 등록 정보를 조회합니다 — 개인 정보는 대개 가려져 있고 TLD마다 서버가 제 나름의 꼴로 답하기 때문에, 스크립트에서 그 출력을 파싱하는 것은 쉽게 깨집니다.',
    'Looks up the registration record of a domain or IP block; personal fields are mostly redacted now and every TLD server formats its reply its own way, so parsing that output in a script breaks easily.',
    'Consulta el registro de un dominio o bloque de IP; los datos personales suelen estar ocultos y cada servidor de TLD responde con su propio formato, asi que analizar esa salida en un script se rompe con facilidad.',
    'Consulta o registro de um dominio ou bloco de IP; os campos pessoais em geral estao ocultos e cada servidor de TLD formata a resposta do seu jeito, entao analisar essa saida num script quebra facil.',
    'ドメインや IP 帯の登録情報を照会します。個人の欄はたいてい隠され、TLD ごとにサーバーが独自の形で答えるため、その出力を台本で解析するとすぐ壊れます。',
    'Fragt den Registrierungsdatensatz einer Domain oder eines IP-Blocks ab; personliche Felder sind heute meist geschwarzt und jeder TLD-Server formatiert seine Antwort anders, weshalb das Parsen dieser Ausgabe in Skripten leicht bricht.',
    "Interroge l'enregistrement d'un domaine ou d'un bloc d'adresses ; les champs personnels sont desormais souvent masques et chaque serveur de TLD met en forme sa reponse a sa facon, si bien qu'analyser cette sortie dans un script casse vite.",
    'किसी डोमेन या IP ब्लॉक का पंजीकरण रिकॉर्ड देखता है; निजी जानकारी अब अधिकतर छिपी होती है और हर TLD सर्वर अपने ढंग से उत्तर सजाता है, इसलिए स्क्रिप्ट में उस आउटपुट को पार्स करना जल्दी टूटता है।',
    '查询域名或 IP 段的注册信息；个人字段如今大多被隐去，而每个顶级域的服务器各按自己的格式回复，所以在脚本里解析这些输出很容易失效。',
    '查詢網域或 IP 區段的註冊資訊；個人欄位如今大多被隱去，而每個頂級域的伺服器各按自己的格式回覆，所以在腳本裡解析這些輸出很容易失效。',
  ],

  nmap: [
    '상대 기계에서 어떤 포트가 답하는지 훑습니다 — `-sS`와 `-O`는 root가 필요하고, 내 것이 아닌 기계를 훑으면 상대 로그에 공격으로 남고 나라에 따라 불법이니 자기 망에서만 씁니다.',
    'Sweeps a host to see which ports answer; `-sS` and `-O` need root, and scanning a machine you do not own is recorded as an attack in its logs and is illegal in some countries, so keep it to your own networks.',
    'Barre un equipo para ver que puertos responden; `-sS` y `-O` requieren root, y escanear una maquina ajena queda registrado como ataque en sus logs y es ilegal en algunos paises, asi que limitalo a tus propias redes.',
    'Varre um host para ver quais portas respondem; `-sS` e `-O` exigem root, e escanear uma maquina que nao e sua fica registrado como ataque nos logs dela e e ilegal em alguns paises, entao limite-se as suas redes.',
    '相手の機械でどのポートが答えるかを走査します。`-sS` と `-O` は root が必要で、自分のものでない機械を走査すると相手の記録に攻撃として残り、国によっては違法なので自分の網だけに留めます。',
    'Tastet einen Host ab, welche Ports antworten; `-sS` und `-O` brauchen root, und das Scannen einer fremden Maschine wird in deren Protokollen als Angriff vermerkt und ist in manchen Landern strafbar — bleiben Sie also im eigenen Netz.',
    "Balaye un hote pour voir quels ports repondent ; `-sS` et `-O` exigent root, et scanner une machine qui ne vous appartient pas est consigne comme une attaque dans ses journaux et illegal dans certains pays : restez sur vos propres reseaux.",
    'किसी होस्ट को खँगालकर देखता है कि कौन-कौन से पोर्ट जवाब देते हैं; `-sS` और `-O` के लिए root चाहिए, और पराई मशीन स्कैन करना उसके लॉग में हमले की तरह दर्ज होता है और कई देशों में अवैध है — इसलिए अपने ही नेटवर्क तक सीमित रहें।',
    '扫一台主机看哪些端口应答；`-sS` 和 `-O` 需要 root，而扫描不属于你的机器会在对方日志里被记成攻击，在有些国家还违法，所以只在自己的网络里用。',
    '掃一台主機看哪些埠會回應；`-sS` 和 `-O` 需要 root，而掃描不屬於你的機器會在對方日誌裡被記成攻擊，在有些國家還違法，所以只在自己的網路裡用。',
  ],

  'openssl-s-client': [
    'TLS 연결을 맨손으로 열어 인증서와 악수 과정을 들여다봅니다 — `-servername`을 빼면 한 IP에 여러 사이트가 있는 서버가 기본 인증서를 내주어 "엉뚱한" 것이 보이고, `< /dev/null`을 붙이지 않으면 입력을 기다리며 멈춰 있습니다.',
    'Opens a raw TLS connection so you can inspect the certificate and the handshake; leave out `-servername` and a server hosting many sites on one address hands you its default certificate, and without `< /dev/null` it sits there waiting for input.',
    'Abre una conexion TLS en crudo para inspeccionar el certificado y el saludo; sin `-servername`, un servidor con muchos sitios en una direccion te entrega su certificado por defecto, y sin `< /dev/null` se queda esperando entrada.',
    'Abre uma conexao TLS crua para inspecionar o certificado e o handshake; sem `-servername`, um servidor com muitos sites num endereco entrega o certificado padrao, e sem `< /dev/null` ele fica ali esperando entrada.',
    'TLS 接続を素で開き、証明書と握手の過程をのぞきます。`-servername` を省くと一つの住所に多くの site を置くサーバーが既定の証明書を返して「別物」が見え、`< /dev/null` を付けないと入力を待って止まったままになります。',
    'Offnet eine rohe TLS-Verbindung, um Zertifikat und Handshake zu prufen; ohne `-servername` liefert ein Server mit vielen Seiten auf einer Adresse sein Standardzertifikat, und ohne `< /dev/null` wartet er endlos auf Eingabe.',
    "Ouvre une connexion TLS brute pour inspecter le certificat et la poignee de main ; sans `-servername`, un serveur qui heberge plusieurs sites sur une adresse vous renvoie son certificat par defaut, et sans `< /dev/null` il reste a attendre une saisie.",
    'कच्चा TLS कनेक्शन खोलता है ताकि आप प्रमाणपत्र और handshake देख सकें; `-servername` छोड़ देने पर एक ही पते पर कई साइट रखने वाला सर्वर अपना डिफ़ॉल्ट प्रमाणपत्र देता है, और `< /dev/null` के बिना यह इनपुट की प्रतीक्षा में अटका रहता है।',
    '开一条裸的 TLS 连接，用来查看证书和握手过程；不写 `-servername`，一个地址上放多个站点的服务器就会给你默认证书，而不加 `< /dev/null` 它会一直等着输入不返回。',
    '開一條裸的 TLS 連線，用來查看憑證和交握過程；不寫 `-servername`，一個位址上放多個網站的伺服器就會給你預設憑證，而不加 `< /dev/null` 它會一直等著輸入不返回。',
  ],

  speedtest: [
    '가까운 서버와 실제로 주고받아 회선 속도를 잽니다 — 어느 운영체제에도 기본으로 없고, 이름이 같은 프로그램이 둘(Ookla의 speedtest와 파이썬 speedtest-cli) 있어 옵션이 서로 다릅니다.',
    'Measures line speed by actually pushing traffic to a nearby server; it is not preinstalled on any system, and two different programs share the name (Ookla\'s speedtest and the Python speedtest-cli) with different flags.',
    'Mide la velocidad de la linea enviando trafico real a un servidor cercano; no viene instalado en ningun sistema y dos programas distintos comparten el nombre (el speedtest de Ookla y el speedtest-cli de Python) con opciones diferentes.',
    'Mede a velocidade da linha empurrando trafego real para um servidor proximo; nao vem instalado em nenhum sistema e dois programas diferentes dividem o nome (o speedtest da Ookla e o speedtest-cli em Python), com opcoes distintas.',
    '近くのサーバーと実際にやり取りして回線の速さを測ります。どの系にも既定では入っておらず、同じ名前の別物が二つ(Ookla の speedtest と Python の speedtest-cli)あってオプションが違います。',
    'Misst die Leitungsgeschwindigkeit, indem es echten Verkehr zu einem nahen Server schickt; es ist auf keinem System vorinstalliert, und zwei verschiedene Programme teilen den Namen (Ooklas speedtest und das Python-speedtest-cli) mit unterschiedlichen Optionen.',
    "Mesure le debit de la ligne en envoyant du trafic reel vers un serveur proche ; il n'est preinstalle sur aucun systeme, et deux programmes differents portent le meme nom (le speedtest d'Ookla et le speedtest-cli en Python) avec des options distinctes.",
    'नज़दीकी सर्वर से वास्तविक ट्रैफ़िक भेजकर लाइन की गति नापता है; यह किसी भी सिस्टम में पहले से नहीं होता, और एक ही नाम के दो अलग प्रोग्राम हैं (Ookla का speedtest और Python का speedtest-cli) जिनके फ़्लैग भिन्न हैं।',
    '通过真的向附近服务器收发流量来测线路速度；任何系统都不自带它，而且同名的其实有两个程序（Ookla 的 speedtest 和 Python 的 speedtest-cli），选项互不相同。',
    '透過真的向附近伺服器收發流量來測線路速度；任何系統都不自帶它，而且同名的其實有兩個程式（Ookla 的 speedtest 和 Python 的 speedtest-cli），選項互不相同。',
  ],

  mtr: [
    'traceroute와 ping을 한 표에 합쳐 계속 갱신하니, 간간이 끊기는 손실을 잡는 데 맞습니다 — 중간 홉의 손실은 대개 그 라우터가 ICMP 응답을 아껴 그런 것이고, 마지막 홉까지 이어지는 손실만 실제 문제입니다.',
    'Merges traceroute and ping into one continuously updated table, the right tool for intermittent packet loss; loss shown at a middle hop is usually just that router rate-limiting its replies, and only loss that continues to the final hop is real.',
    'Combina traceroute y ping en una tabla que se actualiza sin parar, la herramienta adecuada para perdidas intermitentes; la perdida en un salto intermedio suele ser ese router limitando sus respuestas, y solo la que llega hasta el ultimo salto es real.',
    'Junta traceroute e ping numa tabela que se atualiza sem parar, a ferramenta certa para perda intermitente; perda num salto do meio geralmente e aquele roteador limitando as respostas, e so a perda que continua ate o ultimo salto e real.',
    'traceroute と ping を一つの表にまとめて更新し続けるので、時々起きる欠落を捕らえるのに向きます。途中の跳びの損失はたいていその装置が返答を節約しているだけで、最後の跳びまで続く損失だけが本物です。',
    'Vereint traceroute und ping in einer laufend aktualisierten Tabelle — das richtige Werkzeug fur sporadischen Paketverlust; Verlust an einem mittleren Hop ist meist nur ein Router, der seine Antworten begrenzt, und nur Verlust, der bis zum letzten Hop anhalt, ist echt.',
    "Fusionne traceroute et ping dans un tableau mis a jour en continu, l'outil qu'il faut pour les pertes intermittentes ; une perte a un saut intermediaire est le plus souvent ce routeur qui limite ses reponses, et seule celle qui persiste jusqu'au dernier saut est reelle.",
    'traceroute और ping को एक ही लगातार अद्यतन होती तालिका में मिला देता है, रुक-रुक कर होने वाले नुक़सान के लिए सही औज़ार; बीच के hop पर दिखता नुक़सान आम तौर पर उस राउटर का जवाब सीमित करना होता है, और असली समस्या केवल वह है जो अंतिम hop तक बनी रहे।',
    '把 traceroute 和 ping 合成一张持续刷新的表，正是抓时断时续丢包的工具；中间某跳显示的丢包通常只是那台路由器在限速回应，只有一直延续到最后一跳的丢包才是真问题。',
    '把 traceroute 和 ping 合成一張持續更新的表，正是抓時斷時續掉包的工具；中間某跳顯示的掉包通常只是那台路由器在限速回應，只有一直延續到最後一跳的掉包才是真問題。',
  ],

  /* ───────── archive — 압축·전송 ───────── */

  tar: [
    '파일을 한 묶음으로 묶기만 하고 그 자체로는 압축하지 않습니다 — 줄이는 일은 `-z`(gzip)·`-j`(bzip2)·`-J`(xz)가 맡고, `-c` 만들기·`-x` 풀기·`-t` 들여다보기 가운데 하나만 골라 앞에 두며, 옛 꼴에서는 `-f`가 옵션 가운데 마지막이라 그 바로 뒤에 묶음 이름이 옵니다.',
    'Bundles files into one archive but does not compress on its own — `-z` (gzip), `-j` (bzip2) and `-J` (xz) do that part — and you pick exactly one of `-c` to create, `-x` to extract or `-t` to list, keeping `-f` as the last flag so the archive name follows it directly.',
    'Empaqueta archivos en uno solo pero no comprime por sí mismo: de eso se encargan `-z` (gzip), `-j` (bzip2) y `-J` (xz); elige exactamente uno entre `-c` para crear, `-x` para extraer o `-t` para listar, y deja `-f` como última opción, porque el nombre del paquete va justo detrás.',
    'Junta arquivos em um só pacote, mas não comprime por conta própria: quem faz isso é `-z` (gzip), `-j` (bzip2) ou `-J` (xz); escolha exatamente um entre `-c` para criar, `-x` para extrair e `-t` para listar, e deixe `-f` como última opção, pois o nome do pacote vem logo depois dele.',
    'ファイルを一つの束にまとめるだけで、それ自体は圧縮しません。縮めるのは `-z`(gzip)・`-j`(bzip2)・`-J`(xz) の役目で、作る `-c`・取り出す `-x`・中身を見る `-t` のうち一つだけを先に置き、古い書き方では `-f` を最後のオプションにして、その直後に書庫名を書きます。',
    'Fasst Dateien nur zu einem Archiv zusammen und komprimiert selbst nicht — das übernehmen `-z` (gzip), `-j` (bzip2) und `-J` (xz) —, dabei wählt man genau eines von `-c` zum Erstellen, `-x` zum Entpacken oder `-t` zum Auflisten, und `-f` steht als letzte Option unmittelbar vor dem Archivnamen.',
    'Ne fait que regrouper des fichiers dans une seule archive sans compresser lui-même : ce sont `-z` (gzip), `-j` (bzip2) et `-J` (xz) qui s’en chargent ; on choisit exactement un mode parmi `-c` pour créer, `-x` pour extraire ou `-t` pour lister, et `-f` reste la dernière option, le nom de l’archive venant juste après.',
    'फ़ाइलों को एक ही बंडल में बाँधता है पर खुद कुछ छोटा नहीं करता — दबाने का काम `-z` (gzip), `-j` (bzip2) और `-J` (xz) करते हैं; बनाने के `-c`, खोलने के `-x` और सूची के `-t` में से ठीक एक चुनें, और `-f` को सबसे अंतिम फ़्लैग रखें, क्योंकि उसके तुरंत बाद बंडल का नाम आता है।',
    '它只把文件打成一个包，本身并不压缩——压缩由 `-z`（gzip）、`-j`（bzip2）、`-J`（xz）来做；创建 `-c`、解压 `-x`、查看 `-t` 三种模式只能选一个且要写在前面，而经典写法里 `-f` 必须是最后一个选项，包名紧跟其后。',
    '它只把檔案打成一個包，本身並不壓縮——壓縮是 `-z`（gzip）、`-j`（bzip2）、`-J`（xz）的事；建立 `-c`、解壓 `-x`、查看 `-t` 三種模式只能選一個且要寫在前面，而經典寫法裡 `-f` 必須是最後一個選項，包名緊跟其後。',
  ],

  gzip: [
    '파일 하나를 .gz로 압축합니다 — 기본 동작이 원본을 지우고 .gz만 남기는 것이라, 원본을 두려면 `-k`를 붙이거나 `-c`로 표준출력에 받아야 하고, 여러 파일을 하나로 묶지는 못해 tar와 함께 씁니다.',
    'Compresses a single file into .gz and, by default, deletes the original once it succeeds, so keep it with `-k` or by writing to stdout with `-c`; it never bundles several files, which is why tar comes first.',
    'Comprime un solo archivo en .gz y, por defecto, borra el original al terminar, así que consérvalo con `-k` o escribiendo a la salida estándar con `-c`; nunca agrupa varios archivos, y por eso tar va delante.',
    'Comprime um único arquivo em .gz e, por padrão, apaga o original ao terminar, então guarde-o com `-k` ou escrevendo na saída padrão com `-c`; ele nunca junta vários arquivos, e é por isso que o tar vem antes.',
    'ファイル一つを .gz に圧縮しますが、既定では成功した時点で元のファイルを消してしまうので、残したいときは `-k` を付けるか `-c` で標準出力に流します。複数をまとめる働きはなく、だから前に tar が来ます。',
    'Komprimiert eine einzelne Datei zu .gz und löscht dabei standardmäßig das Original, sobald es geklappt hat — mit `-k` oder über `-c` auf die Standardausgabe bleibt es erhalten; mehrere Dateien bündelt gzip nie, deshalb steht tar davor.',
    'Compresse un seul fichier en .gz et supprime l’original par défaut dès que l’opération réussit : gardez-le avec `-k` ou en écrivant sur la sortie standard avec `-c` ; il ne regroupe jamais plusieurs fichiers, d’où la présence de tar en amont.',
    'एक फ़ाइल को .gz में दबाता है और सफल होते ही मूल फ़ाइल डिफ़ॉल्ट रूप से मिटा देता है, इसलिए उसे रखने के लिए `-k` लगाएँ या `-c` से मानक आउटपुट पर लिखें; यह कई फ़ाइलों को एक में नहीं बाँधता, इसीलिए पहले tar आता है।',
    '把单个文件压成 .gz，而且默认在成功后删掉原文件，想留着就加 `-k` 或用 `-c` 写到标准输出；它从不打包多个文件，所以前面要配 tar。',
    '把單個檔案壓成 .gz，而且預設在成功後刪掉原檔，想留著就加 `-k` 或用 `-c` 寫到標準輸出；它從不打包多個檔案，所以前面要配 tar。',
  ],

  gunzip: [
    '.gz를 풀어 원래 파일로 되돌립니다 — 성공하면 .gz 쪽을 지우니 남기려면 `-k`이고, 같은 이름의 파일이 이미 있으면 `-f` 없이는 거부하며, 디스크에 쓰지 않고 바로 흘려보내려면 `-c`(zcat과 같습니다)를 씁니다.',
    'Unpacks a .gz back to the original file and removes the .gz on success, so `-k` keeps it, `-f` is needed when a file of that name already exists, and `-c` streams the contents out without ever writing them to disk, exactly like zcat.',
    'Descomprime un .gz y devuelve el archivo original, eliminando el .gz si todo va bien: `-k` lo conserva, `-f` hace falta cuando ya existe un archivo con ese nombre, y `-c` envía el contenido a la salida estándar sin escribir nada en disco, igual que zcat.',
    'Descomprime um .gz de volta ao arquivo original e remove o .gz quando dá certo: `-k` o conserva, `-f` é necessário se já existe um arquivo com aquele nome, e `-c` manda o conteúdo para a saída padrão sem gravar nada em disco, igual ao zcat.',
    '.gz を展開して元のファイルに戻し、成功すると .gz の側を消すので、残すなら `-k`、同名のファイルが既にあるときは `-f` が必要で、ディスクに書かずに流したいときは `-c`(zcat と同じ)を使います。',
    'Entpackt eine .gz-Datei zurück zum Original und entfernt bei Erfolg die .gz — `-k` behält sie, `-f` braucht man, wenn eine Datei dieses Namens schon existiert, und `-c` schreibt den Inhalt auf die Standardausgabe, ohne ihn je auf die Platte zu legen, genau wie zcat.',
    'Décompresse un .gz vers le fichier d’origine et supprime le .gz en cas de succès : `-k` le conserve, `-f` devient nécessaire si un fichier du même nom existe déjà, et `-c` envoie le contenu sur la sortie standard sans rien écrire sur le disque, exactement comme zcat.',
    '.gz को खोलकर मूल फ़ाइल वापस बनाता है और सफल होने पर .gz हटा देता है — `-k` उसे बचा लेता है, उसी नाम की फ़ाइल पहले से हो तो `-f` चाहिए, और `-c` सामग्री को डिस्क पर लिखे बिना मानक आउटपुट पर भेज देता है, ठीक zcat की तरह।',
    '把 .gz 解开还原成原文件，成功后就删掉 .gz——想留着用 `-k`，同名文件已存在时得加 `-f`，而 `-c` 会把内容直接送到标准输出，一点也不落盘，和 zcat 一样。',
    '把 .gz 解開還原成原檔，成功後就刪掉 .gz——想留著要用 `-k`，同名檔案已存在時得加 `-f`，而 `-c` 會把內容直接送到標準輸出，完全不落地寫檔，和 zcat 一樣。',
  ],

  zip: [
    '압축까지 스스로 하는 묶음 꼴이라, Windows에서 아무것도 깔지 않고 그냥 열리는 것이 이것입니다 — 폴더를 담으려면 `-r`이 있어야 하고(없으면 폴더 항목만 들어가고 속은 비어 있습니다), `-e`로 거는 잠금은 옛 ZipCrypto라 약합니다.',
    'Unlike tar it carries its own compression, and it is the format a Windows machine opens with no extra software; a directory needs `-r` or you archive the folder entry and nothing inside, and the password from `-e` is legacy ZipCrypto and weak.',
    'A diferencia de tar, incluye su propia compresión y es el formato que Windows abre sin instalar nada; para una carpeta hace falta `-r`, o solo guardarás la entrada del directorio y nada de su contenido, y la contraseña de `-e` es ZipCrypto antiguo y débil.',
    'Diferente do tar, ele já traz a própria compressão e é o formato que o Windows abre sem instalar nada; uma pasta exige `-r`, senão você guarda só a entrada do diretório e nada do que há dentro, e a senha de `-e` é o ZipCrypto antigo e fraco.',
    'tar と違って圧縮まで自前で行い、Windows が何も入れずに開けるのはこの形式です。ディレクトリを入れるには `-r` が必要で、付けないと入れ物だけが入って中身は空、`-e` で掛ける錠は古い ZipCrypto なので弱いままです。',
    'Anders als tar bringt es die Kompression selbst mit und ist das Format, das ein Windows-Rechner ohne Zusatzsoftware öffnet; ein Verzeichnis braucht `-r`, sonst landet nur der Ordnereintrag und nichts darin im Archiv, und das Passwort aus `-e` ist altes, schwaches ZipCrypto.',
    'Contrairement à tar, il embarque sa propre compression et c’est le format qu’une machine Windows ouvre sans rien installer ; un répertoire exige `-r`, sinon vous n’archivez que l’entrée du dossier et rien de son contenu, et le mot de passe de `-e` repose sur l’ancien ZipCrypto, qui est faible.',
    'tar से उलट यह संपीड़न खुद अपने भीतर रखता है और यही वह रूप है जिसे Windows बिना कुछ इंस्टॉल किए खोल लेता है; किसी डायरेक्टरी के लिए `-r` ज़रूरी है, वरना केवल फ़ोल्डर की प्रविष्टि जाती है और भीतर का कुछ नहीं, और `-e` वाला पासवर्ड पुराना ZipCrypto है, जो कमज़ोर है।',
    '和 tar 不同，它自己就带压缩，也是 Windows 不装任何东西就能打开的那种格式；装目录必须加 `-r`，否则只把目录这一条记进去、里面的东西一个也没有，而 `-e` 设的密码是老式 ZipCrypto，很弱。',
    '和 tar 不同，它自己就帶壓縮，也是 Windows 不裝任何東西就能開啟的那種格式；要放整個目錄必須加 `-r`，否則只把目錄這一條記進去、裡面的東西一個也沒有，而 `-e` 設的密碼是老式 ZipCrypto，很弱。',
  ],

  unzip: [
    '.zip을 풀어 놓습니다 — `-d`를 안 주면 지금 폴더에 그대로 쏟아지니 `-l`로 안에 상위 폴더가 있는지 먼저 보고, 옛 Windows에서 만든 것은 이름이 깨져 `-O CP932`가 필요하며, `-P`로 적은 비밀번호는 셸 기록과 ps에 그대로 남습니다.',
    'Extracts a .zip, and without `-d` it empties straight into the current directory, so run `-l` first to see whether the archive has a top-level folder; names from older Windows zips need `-O CP932`, and a password given with `-P` is visible in your shell history and in ps.',
    'Extrae un .zip y, sin `-d`, lo vuelca directamente en el directorio actual, así que usa `-l` antes para ver si trae una carpeta raíz; los nombres de zips hechos en Windows antiguos requieren `-O CP932`, y la contraseña de `-P` queda a la vista en el historial del shell y en ps.',
    'Extrai um .zip e, sem `-d`, despeja tudo no diretório atual, então rode `-l` antes para ver se existe uma pasta raiz dentro; nomes de zips feitos em Windows antigos pedem `-O CP932`, e a senha passada com `-P` fica visível no histórico do shell e no ps.',
    '.zip を展開しますが、`-d` を与えないと今いるディレクトリにそのままぶちまけるので、先に `-l` で最上位のフォルダーがあるかを見ます。古い Windows で作られたものは名前が化けるため `-O CP932` が要り、`-P` で渡した合言葉はシェルの履歴と ps に残ります。',
    'Entpackt ein .zip und schüttet es ohne `-d` direkt in das aktuelle Verzeichnis, prüfen Sie also vorher mit `-l`, ob ein oberster Ordner enthalten ist; Namen aus älteren Windows-Zips brauchen `-O CP932`, und ein mit `-P` übergebenes Passwort steht sichtbar in der Shell-Historie und in ps.',
    'Extrait une archive .zip et, sans `-d`, la déverse directement dans le répertoire courant : regardez d’abord avec `-l` si elle contient un dossier racine ; les noms issus d’anciens zips Windows exigent `-O CP932`, et un mot de passe passé par `-P` reste visible dans l’historique du shell et dans ps.',
    '.zip को खोलता है, और `-d` न देने पर सब कुछ सीधे मौजूदा डायरेक्टरी में उलट देता है, इसलिए पहले `-l` से देखें कि भीतर कोई शीर्ष फ़ोल्डर है या नहीं; पुराने Windows पर बने zip के नामों के लिए `-O CP932` चाहिए, और `-P` से दिया पासवर्ड शेल के इतिहास और ps में दिखता रहता है।',
    '解开 .zip；不给 `-d` 就直接倒在当前目录里，所以先用 `-l` 看看包内有没有一层顶层文件夹，旧版 Windows 打的包要靠 `-O CP932` 才不乱码，而 `-P` 写在命令行里的密码会留在 shell 历史和 ps 里。',
    '解開 .zip；不給 `-d` 就直接倒在目前目錄裡，所以先用 `-l` 看看包內有沒有一層最上層資料夾，舊版 Windows 打的包要靠 `-O CP932` 才不會亂碼，而 `-P` 寫在命令列裡的密碼會留在 shell 歷史和 ps 裡。',
  ],

  bzip2: [
    'gzip보다 느리지만 더 작게 줄입니다 — gzip과 마찬가지로 원본을 지우고 .bz2만 남기니 남기려면 `-k`가 필요하고, tar와 함께 쓸 때 붙일 글자는 `-j`이며 푸는 것은 `-d`(=bunzip2)입니다.',
    'Squeezes harder than gzip but takes longer, and like gzip it deletes the original and leaves only the .bz2 unless you pass `-k`; the tar flag for it is `-j`, and decompressing is `-d`, which is what bunzip2 does.',
    'Comprime más que gzip a cambio de tardar más y, como gzip, borra el original y deja solo el .bz2 salvo que pases `-k`; su opción en tar es `-j`, y descomprimir es `-d`, que es lo que hace bunzip2.',
    'Comprime mais que o gzip em troca de demorar mais e, como o gzip, apaga o original e deixa só o .bz2 a menos que você passe `-k`; a opção dele no tar é `-j`, e descomprimir é `-d`, exatamente o que o bunzip2 faz.',
    'gzip より時間はかかりますが小さくなります。gzip と同じく元のファイルを消して .bz2 だけを残すため `-k` が要り、tar と組むときの文字は `-j`、展開は `-d`(bunzip2 と同じ)です。',
    'Packt stärker als gzip, braucht dafür länger und löscht wie gzip das Original, sodass ohne `-k` nur die .bz2 übrig bleibt; in tar heißt die passende Option `-j`, und entpackt wird mit `-d`, was bunzip2 tut.',
    'Compresse plus fort que gzip mais met plus de temps et, comme gzip, supprime l’original pour ne laisser que le .bz2 si l’on omet `-k` ; dans tar l’option correspondante est `-j`, et la décompression se fait avec `-d`, ce que fait bunzip2.',
    'gzip से ज़्यादा कसकर दबाता है पर समय भी ज़्यादा लेता है, और gzip की तरह मूल फ़ाइल मिटाकर सिर्फ़ .bz2 छोड़ता है जब तक `-k` न दें; tar में इसका अक्षर `-j` है और खोलना `-d` से होता है, वही काम bunzip2 करता है।',
    '比 gzip 压得更小，代价是更慢，而且和 gzip 一样会删掉原文件、只留下 .bz2，除非加 `-k`；配 tar 时对应的字母是 `-j`，解压用 `-d`，也就是 bunzip2 做的事。',
    '比 gzip 壓得更小，代價是更慢，而且和 gzip 一樣會刪掉原檔、只留下 .bz2，除非加 `-k`；配 tar 時對應的字母是 `-j`，解壓用 `-d`，也就是 bunzip2 做的事。',
  ],

  xz: [
    '흔히 쓰는 셋 가운데 가장 작게 줄이지만 느리고 `-9`에서는 700 MB 남짓 메모리를 씁니다 — 기본이 한 코어라 `-T0`을 붙여야 여럿을 쓰고, 원본은 `-k` 없이는 사라지며 tar에 붙일 글자는 대문자 `-J`입니다.',
    'Gives the smallest output of the three common formats but is slow and wants roughly 700 MB of memory at `-9`; it runs single-threaded unless you add `-T0`, it removes the original without `-k`, and the tar flag for it is a capital `-J`.',
    'Da la salida más pequeña de los tres formatos habituales, aunque es lento y a `-9` pide unos 700 MB de memoria; usa un solo núcleo si no añades `-T0`, borra el original sin `-k`, y su opción en tar es la `-J` mayúscula.',
    'Gera a menor saída dos três formatos comuns, mas é lento e a `-9` pede cerca de 700 MB de memória; roda em um só núcleo se você não acrescentar `-T0`, apaga o original sem `-k`, e a opção dele no tar é o `-J` maiúsculo.',
    'よく使う三つの中でいちばん小さくなりますが遅く、`-9` では 700 MB ほどのメモリーを使います。既定では一つの核だけで動くので `-T0` を足し、`-k` を付けないと元のファイルは消え、tar で指定する文字は大文字の `-J` です。',
    'Liefert von den drei gängigen Formaten die kleinste Ausgabe, ist dafür langsam und verlangt bei `-9` etwa 700 MB Speicher; ohne `-T0` läuft es einkernig, ohne `-k` verschwindet das Original, und in tar lautet die Option das große `-J`.',
    'Produit la plus petite sortie des trois formats courants, au prix de la lenteur et d’environ 700 Mo de mémoire au niveau `-9` ; il reste monothread sans `-T0`, efface l’original sans `-k`, et son option dans tar est le `-J` majuscule.',
    'तीनों आम रूपों में सबसे छोटा परिणाम देता है, पर धीमा है और `-9` पर लगभग 700 MB मेमोरी माँगता है; `-T0` न जोड़ें तो एक ही कोर पर चलता है, `-k` के बिना मूल फ़ाइल मिटा देता है, और tar में इसका अक्षर बड़ा `-J` है।',
    '三种常见格式里它压得最小，但慢，而且 `-9` 大约要吃 700 MB 内存；默认只用一个核，加 `-T0` 才用满，不加 `-k` 原文件就没了，配 tar 时的字母是大写的 `-J`。',
    '三種常見格式裡它壓得最小，但慢，而且 `-9` 大約要吃 700 MB 記憶體；預設只用一個核心，加 `-T0` 才會全用上，不加 `-k` 原檔就沒了，配 tar 時的字母是大寫的 `-J`。',
  ],

  zcat: [
    '.gz 안의 내용을 디스크에 풀지 않고 표준출력으로 바로 흘려보냅니다 — 그래서 돌려 놓은 로그를 그 자리에서 grep할 수 있고, 압축이 아닌 파일까지 섞어 읽으려면 `-f`이며, bzip2·xz·zstd에는 bzcat·xzcat·zstdcat이 따로 있습니다.',
    'Streams what is inside a .gz to stdout without ever unpacking it to disk, which is how you grep a rotated log in place; `-f` lets plain uncompressed files through in the same pass, and bzip2, xz and zstd have their own bzcat, xzcat and zstdcat.',
    'Envía el contenido de un .gz a la salida estándar sin descomprimirlo en disco, que es la forma de hacer grep sobre un log rotado tal cual; `-f` deja pasar también los archivos sin comprimir en la misma pasada, y bzip2, xz y zstd tienen sus propios bzcat, xzcat y zstdcat.',
    'Manda o conteúdo de um .gz para a saída padrão sem descompactar nada em disco, que é como se faz grep num log rotacionado do jeito que ele está; `-f` deixa passar também arquivos não comprimidos na mesma passada, e bzip2, xz e zstd têm seus próprios bzcat, xzcat e zstdcat.',
    '.gz の中身をディスクに展開せずそのまま標準出力へ流すので、ローテーションしたログをその場で grep できます。圧縮されていないファイルも一緒に読ませるなら `-f`、bzip2・xz・zstd には bzcat・xzcat・zstdcat が別にあります。',
    'Gibt den Inhalt einer .gz-Datei auf die Standardausgabe aus, ohne sie je auf die Platte zu entpacken — so greppt man ein rotiertes Log an seinem Platz; `-f` lässt auch unkomprimierte Dateien im selben Durchgang durch, und für bzip2, xz und zstd gibt es bzcat, xzcat und zstdcat.',
    'Envoie le contenu d’un .gz sur la sortie standard sans jamais le décompresser sur le disque, ce qui permet de grepper un journal déjà tourné tel quel ; `-f` laisse passer aussi les fichiers non compressés dans la même passe, et bzip2, xz et zstd ont leurs propres bzcat, xzcat et zstdcat.',
    '.gz के भीतर का पाठ डिस्क पर खोले बिना सीधे मानक आउटपुट पर बहा देता है, इसी तरह घुमाए गए लॉग पर जहाँ है वहीं grep किया जाता है; `-f` उसी चक्कर में बिना दबी फ़ाइलों को भी गुज़र जाने देता है, और bzip2, xz तथा zstd के लिए अलग bzcat, xzcat और zstdcat हैं।',
    '把 .gz 里的内容直接送到标准输出，完全不在磁盘上解开，所以能就地 grep 一份已经轮转的日志；`-f` 让没压缩的普通文件也一起通过，而 bzip2、xz、zstd 各有自己的 bzcat、xzcat、zstdcat。',
    '把 .gz 裡的內容直接送到標準輸出，完全不在磁碟上解開，所以能就地 grep 一份已經輪替的日誌；`-f` 讓沒壓縮的普通檔案也一起通過，而 bzip2、xz、zstd 各有自己的 bzcat、xzcat、zstdcat。',
  ],

  '7z': [
    'a·x·e·l 같은 낱말을 맨 앞에 두고 쓰는 도구로, .7z뿐 아니라 zip·tar·rar까지 읽고 압축률도 높은 편입니다 — 내보낼 폴더는 `-o` 뒤에 띄어쓰기 없이 붙여 적어야 하고, 이름까지 감추려면 `-p`에 `-mhe=on`을 더합니다.',
    'Takes a verb first — `a` to add, `x` to extract with paths, `e` to flatten them, `l` to list — reads far more than .7z including zip, tar and rar, and packs tighter than most; the output directory goes right after `-o` with no space, and `-mhe=on` alongside `-p` is what also hides the file names.',
    'Se usa con un verbo delante — `a` para añadir, `x` para extraer con rutas, `e` para aplanarlas, `l` para listar —, lee mucho más que .7z, también zip, tar y rar, y comprime más que la mayoría; el directorio de salida va pegado a `-o`, sin espacio, y `-mhe=on` junto a `-p` es lo que además oculta los nombres.',
    'Usa-se com um verbo na frente — `a` para adicionar, `x` para extrair com os caminhos, `e` para achatá-los, `l` para listar —, lê muito mais que .7z, também zip, tar e rar, e comprime mais que a maioria; o diretório de saída vem colado ao `-o`, sem espaço, e `-mhe=on` junto de `-p` é o que também esconde os nomes.',
    '最初に a(追加)・x(パスのまま展開)・e(平らに展開)・l(一覧)といった語を置いて使い、.7z だけでなく zip・tar・rar も読め、圧縮率も高いほうです。出力先は `-o` に空白を入れず続けて書き、名前まで隠すには `-p` に `-mhe=on` を添えます。',
    'Wird mit einem Verb davor benutzt — `a` zum Hinzufügen, `x` zum Entpacken mit Pfaden, `e` flach, `l` zum Auflisten —, liest weit mehr als .7z, auch zip, tar und rar, und packt dichter als die meisten; das Zielverzeichnis steht ohne Leerzeichen direkt hinter `-o`, und erst `-mhe=on` neben `-p` verbirgt auch die Dateinamen.',
    'S’emploie avec un verbe en tête — `a` pour ajouter, `x` pour extraire avec les chemins, `e` pour les aplatir, `l` pour lister —, lit bien plus que le .7z, y compris zip, tar et rar, et compresse plus fort que la plupart ; le dossier de sortie s’écrit collé à `-o`, sans espace, et c’est `-mhe=on` ajouté à `-p` qui masque aussi les noms de fichiers.',
    'पहले एक क्रिया लिखकर चलता है — जोड़ने के लिए `a`, पथ सहित निकालने के लिए `x`, सपाट निकालने के लिए `e`, सूची के लिए `l` — और .7z के अलावा zip, tar तथा rar भी पढ़ लेता है, कसाव भी अधिकतर से ज़्यादा है; निर्गम डायरेक्टरी `-o` के तुरंत बाद बिना जगह छोड़े लिखी जाती है, और नाम भी छिपाने के लिए `-p` के साथ `-mhe=on` जोड़ना पड़ता है।',
    '它把动作写在最前面——`a` 添加、`x` 带路径解压、`e` 摊平解压、`l` 列出——能读的远不止 .7z，zip、tar、rar 都行，压缩率也比多数工具高；`-o` 后面不能空格，目录要紧贴着写，而想连文件名一起藏起来，得在 `-p` 之外再加 `-mhe=on`。',
    '它把動作寫在最前面——`a` 新增、`x` 帶路徑解壓、`e` 攤平解壓、`l` 列出——能讀的遠不止 .7z，zip、tar、rar 都行，壓縮率也比多數工具高；`-o` 後面不能有空格，目錄要緊貼著寫，而想連檔名一起藏起來，得在 `-p` 之外再加 `-mhe=on`。',
  ],

  dd: [
    '블록 단위로 그대로 복사해 디스크 이미지를 쓰거나 굽습니다 — `of=`에 적은 대상은 확인 한 번 없이 0번 블록부터 덮어써지고 되돌릴 방법이 없으니, Enter를 누르기 직전에 lsblk로 장치 이름을 다시 확인하고, 속도는 `bs=4M`, 진행은 `status=progress`(macOS는 Ctrl+T)로 봅니다.',
    'Copies block for block, which is how an installer image gets written to a device; whatever you name in `of=` is overwritten from block zero with no confirmation and no way back, so confirm the device with lsblk immediately before pressing Enter, use `bs=4M` for speed and `status=progress` (Ctrl+T on macOS) to watch it.',
    'Copia bloque a bloque, y así se escriben las imágenes de instalación en un dispositivo; lo que pongas en `of=` se sobrescribe desde el bloque cero, sin confirmación y sin vuelta atrás, así que verifica el dispositivo con lsblk justo antes de pulsar Enter, usa `bs=4M` por velocidad y `status=progress` (Ctrl+T en macOS) para seguirlo.',
    'Copia bloco a bloco, e é assim que uma imagem de instalação vai para um dispositivo; o que você escrever em `of=` é sobrescrito a partir do bloco zero, sem confirmação e sem volta, então confirme o dispositivo com lsblk logo antes de apertar Enter, use `bs=4M` pela velocidade e `status=progress` (Ctrl+T no macOS) para acompanhar.',
    'ブロックのまま丸ごと写すので、起動用のイメージを装置へ書き込むのはこれです。`of=` に書いた相手は確認もなく 0 番のブロックから上書きされ、戻す手立てはないため、Enter を押す直前に lsblk で装置名を確かめ、速さは `bs=4M`、進み具合は `status=progress`(macOS は Ctrl+T)で見ます。',
    'Kopiert Block für Block — so schreibt man ein Installationsabbild auf ein Gerät; was in `of=` steht, wird ohne Rückfrage ab Block null überschrieben, und es gibt kein Zurück, also prüfen Sie das Gerät unmittelbar vor dem Enter noch einmal mit lsblk, nehmen `bs=4M` für Tempo und `status=progress` (unter macOS Ctrl+T) zum Mitschauen.',
    'Copie bloc par bloc, et c’est ainsi qu’on écrit une image d’installation sur un périphérique ; ce que vous nommez dans `of=` est écrasé depuis le bloc zéro, sans confirmation et sans retour possible, alors vérifiez le périphérique avec lsblk juste avant d’appuyer sur Entrée, prenez `bs=4M` pour la vitesse et `status=progress` (Ctrl+T sous macOS) pour suivre.',
    'ब्लॉक-दर-ब्लॉक नक़ल करता है, इसी से इंस्टॉलर इमेज किसी डिवाइस पर लिखी जाती है; `of=` में जो लिखा है वह बिना किसी पुष्टि के, ब्लॉक शून्य से ऊपर लिख दिया जाता है और वापसी का कोई रास्ता नहीं, इसलिए Enter दबाने से ठीक पहले lsblk से डिवाइस का नाम फिर जाँचें, गति के लिए `bs=4M` लें और चाल देखने के लिए `status=progress` (macOS पर Ctrl+T)।',
    '它按块原样复制，装机镜像就是这样写进设备的；`of=` 指到哪里，那里就从第 0 块起被覆盖，不问一声也没法撤销，所以按下回车前一定要用 lsblk 再确认一次设备名，用 `bs=4M` 提速，用 `status=progress`（macOS 按 Ctrl+T）看进度。',
    '它按區塊原樣複製，裝機映像就是這樣寫進裝置的；`of=` 指到哪裡，那裡就從第 0 塊起被覆蓋，不問一聲也無法還原，所以按下 Enter 前一定要用 lsblk 再確認一次裝置名稱，用 `bs=4M` 提速，用 `status=progress`（macOS 按 Ctrl+T）看進度。',
  ],

  cpio: [
    '무엇을 담을지 표준입력으로 받은 이름 목록에서 정하는 옛 묶음 도구라, 그 목록은 대개 find가 만듭니다 — `-o`가 묶기, `-i`가 풀기, `-t`가 목록이고, initramfs가 쓰는 꼴은 `-H newc`이며, 남이 준 묶음은 절대경로로 튀어나오지 않게 `--no-absolute-filenames`를 붙입니다.',
    'An older archiver that takes the list of names to pack from stdin, which is why find usually feeds it; `-o` writes an archive, `-i` extracts one, `-t` lists it, `-H newc` is the portable format initramfs images use, and `--no-absolute-filenames` stops an archive from someone else writing into absolute paths.',
    'Un archivador antiguo que toma de la entrada estándar la lista de nombres a empaquetar, y por eso suele alimentarlo find; `-o` crea el archivo, `-i` lo extrae, `-t` lo lista, `-H newc` es el formato portable que usan las imágenes initramfs, y `--no-absolute-filenames` evita que un archivo ajeno escriba en rutas absolutas.',
    'Um arquivador antigo que recebe da entrada padrão a lista de nomes a empacotar, e por isso quem costuma alimentá-lo é o find; `-o` cria o arquivo, `-i` extrai, `-t` lista, `-H newc` é o formato portátil que as imagens initramfs usam, e `--no-absolute-filenames` impede que um arquivo de outra pessoa escreva em caminhos absolutos.',
    '何を詰めるかを標準入力から受けた名前の一覧で決める古い書庫道具で、その一覧はふつう find が作ります。`-o` が書き出し、`-i` が取り出し、`-t` が一覧、initramfs が使う可搬な形は `-H newc`、他人からもらった書庫は絶対パスに展開されないよう `--no-absolute-filenames` を添えます。',
    'Ein alter Archivierer, der die Liste der zu packenden Namen von der Standardeingabe nimmt, weshalb ihn meist find füttert; `-o` schreibt ein Archiv, `-i` entpackt eines, `-t` listet es, `-H newc` ist das portable Format der initramfs-Abbilder, und `--no-absolute-filenames` verhindert, dass ein fremdes Archiv in absolute Pfade schreibt.',
    'Un archiveur ancien qui prend sur l’entrée standard la liste des noms à empaqueter, d’où le find qui l’alimente presque toujours ; `-o` écrit une archive, `-i` en extrait une, `-t` la liste, `-H newc` est le format portable des images initramfs, et `--no-absolute-filenames` empêche une archive venue d’ailleurs d’écrire dans des chemins absolus.',
    'यह पुराना संग्राहक है जो पैक करने वाले नामों की सूची मानक इनपुट से लेता है, इसीलिए उसे आम तौर पर find खिलाता है; `-o` संग्रह बनाता है, `-i` उसे खोलता है, `-t` सूची दिखाता है, `-H newc` वही पोर्टेबल रूप है जिसे initramfs इमेज इस्तेमाल करती हैं, और `--no-absolute-filenames` किसी और के भेजे संग्रह को पूर्ण पथ पर लिखने से रोकता है।',
    '这是个老式打包工具，要打进去哪些文件由标准输入送来的名单决定，所以喂它的通常是 find；`-o` 写包、`-i` 解包、`-t` 看目录，initramfs 用的可移植格式是 `-H newc`，而别人给的包要加 `--no-absolute-filenames`，免得它往绝对路径里写。',
    '這是個老式打包工具，要打進去哪些檔案由標準輸入送來的名單決定，所以餵它的通常是 find；`-o` 寫包、`-i` 解包、`-t` 看清單，initramfs 用的可攜格式是 `-H newc`，而別人給的包要加 `--no-absolute-filenames`，免得它往絕對路徑裡寫。',
  ],

  zstd: [
    'gzip과 같은 크기를 몇 배 빠르게 내고 `-19`에서는 xz에 가까운 크기까지 훨씬 짧은 시간에 만드는 요즘 압축기입니다 — gzip과 달리 원본을 그대로 두고(지우려면 `--rm`), `-T0`으로 모든 코어를 쓰며 tar에서는 `--zstd`로 부릅니다.',
    'The modern one: it reaches gzip’s size several times faster, and at `-19` it lands near xz sizes in a fraction of xz’s time; unlike gzip it keeps the source file by default (`--rm` deletes it), `-T0` uses every core, and tar calls it with `--zstd`.',
    'El moderno: alcanza el tamaño de gzip varias veces más rápido y, en `-19`, se acerca al de xz en una fracción de su tiempo; a diferencia de gzip conserva el original por defecto (`--rm` lo borra), `-T0` usa todos los núcleos y en tar se invoca con `--zstd`.',
    'O moderno: chega ao tamanho do gzip várias vezes mais rápido e, em `-19`, fica perto do tamanho do xz numa fração do tempo dele; diferente do gzip, mantém o original por padrão (`--rm` apaga), `-T0` usa todos os núcleos e no tar ele é chamado com `--zstd`.',
    '今どきの圧縮器で、gzip と同じ大きさを何倍も速く出し、`-19` では xz に近い小ささを xz よりずっと短い時間で出します。gzip と違って元のファイルは残し(消すなら `--rm`)、`-T0` で全コアを使い、tar からは `--zstd` で呼びます。',
    'Der moderne Packer: er erreicht gzip-Größen um ein Mehrfaches schneller und kommt mit `-19` in einem Bruchteil der xz-Zeit fast an xz heran; anders als gzip behält er das Original (mit `--rm` löscht er es), `-T0` nutzt alle Kerne, und in tar heißt er `--zstd`.',
    'Le moderne : il atteint la taille de gzip plusieurs fois plus vite et, en `-19`, s’approche de celle de xz en une fraction du temps de xz ; contrairement à gzip il conserve le fichier source par défaut (`--rm` le supprime), `-T0` exploite tous les cœurs, et tar l’appelle avec `--zstd`.',
    'यह आजकल वाला दबाने वाला औज़ार है: gzip जितना आकार कई गुना तेज़ी से देता है और `-19` पर xz के आकार के पास पहुँचता है, वह भी xz के समय के एक अंश में; gzip से उलट यह मूल फ़ाइल डिफ़ॉल्ट रूप से रखता है (`--rm` उसे मिटाता है), `-T0` सारे कोर लगाता है, और tar में इसे `--zstd` से बुलाया जाता है।',
    '这是现在这一代的压缩器：达到 gzip 的体积要快好几倍，`-19` 时能接近 xz 的体积，花的时间却只有 xz 的一小截；和 gzip 不同，它默认保留原文件（要删就加 `--rm`），`-T0` 吃满所有核心，在 tar 里写成 `--zstd`。',
    '這是現在這一代的壓縮器：達到 gzip 的體積要快好幾倍，`-19` 時能接近 xz 的體積，花的時間卻只有 xz 的一小截；和 gzip 不同，它預設保留原檔（要刪就加 `--rm`），`-T0` 吃滿所有核心，在 tar 裡寫成 `--zstd`。',
  ],

  rar: [
    '독점 형식이라 만드는 rar는 유료이고 푸는 unrar만 공짜로 옵니다 — 남이 보낸 .rar는 `unrar x`(적힌 경로 그대로)나 RARLAB에서 아무것도 안 깔고 `7z x`로 열면 되며, `e`는 경로를 눌러 펴고 `-hp`는 이름까지 감추며 `-v100m`은 여러 권으로 나눕니다.',
    'A proprietary format where only the extractor is free: creating an archive needs the paid rar binary, while `unrar x` — or `7z x` with nothing installed from RARLAB — unpacks one someone sent you, `x` keeping the stored paths and `e` flattening them, `-hp` hiding the file names too and `-v100m` splitting the archive into volumes.',
    'Un formato propietario del que solo el extractor es gratis: crear un archivo requiere el binario rar de pago, mientras que `unrar x` —o `7z x`, sin instalar nada de RARLAB— abre el que te han enviado; `x` mantiene las rutas guardadas y `e` las aplana, `-hp` oculta también los nombres y `-v100m` lo parte en volúmenes.',
    'Um formato proprietário em que só o extrator é gratuito: criar um arquivo exige o binário rar pago, enquanto `unrar x` — ou `7z x`, sem instalar nada da RARLAB — abre o que alguém lhe mandou; `x` mantém os caminhos guardados e `e` os achata, `-hp` esconde também os nomes e `-v100m` divide o arquivo em volumes.',
    '専有の形式で、無料なのは取り出す側だけです。作るには有料の rar が要り、もらった .rar は `unrar x`(記録されたパスのまま)か、RARLAB から何も入れずに `7z x` で開きます。`e` はパスを平らにし、`-hp` は名前まで隠し、`-v100m` は分冊にします。',
    'Ein geschlossenes Format, bei dem nur das Entpacken kostenlos ist: zum Erstellen braucht man das bezahlte rar-Programm, während `unrar x` — oder `7z x`, ohne irgendetwas von RARLAB zu installieren — das öffnet, was jemand geschickt hat; `x` behält die gespeicherten Pfade, `e` macht sie flach, `-hp` verbirgt auch die Dateinamen und `-v100m` teilt das Archiv in Bände.',
    'Un format propriétaire dont seule l’extraction est gratuite : créer une archive exige le binaire rar payant, alors que `unrar x` — ou `7z x`, sans rien installer de chez RARLAB — ouvre celle qu’on vous a envoyée ; `x` conserve les chemins enregistrés, `e` les aplatit, `-hp` masque aussi les noms de fichiers et `-v100m` découpe en volumes.',
    'यह मालिकाना रूप है जिसमें मुफ़्त सिर्फ़ खोलना है: संग्रह बनाने के लिए पैसे वाला rar बाइनरी चाहिए, जबकि किसी का भेजा .rar `unrar x` से या RARLAB से कुछ भी इंस्टॉल किए बिना `7z x` से खुल जाता है; `x` सहेजे गए पथ रखता है, `e` उन्हें सपाट कर देता है, `-hp` नाम भी छिपाता है और `-v100m` उसे खंडों में बाँट देता है।',
    '这是私有格式，免费的只有解包那一头：要新建得用付费的 rar，而别人发来的 .rar 用 `unrar x` 就能解开，或者干脆 `7z x`，不必从 RARLAB 装任何东西；`x` 保留包里记的路径，`e` 把它们摊平，`-hp` 连文件名一起藏，`-v100m` 切成多卷。',
    '這是私有格式，免費的只有解包那一頭：要新建得用付費的 rar，而別人寄來的 .rar 用 `unrar x` 就能解開，或者乾脆 `7z x`，不必從 RARLAB 裝任何東西；`x` 保留包裡記的路徑，`e` 把它們攤平，`-hp` 連檔名一起藏，`-v100m` 切成多卷。',
  ],
};

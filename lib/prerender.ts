/**
 * 빌드 때 미리 만들 낱장 수를 정한다.
 *
 * ── 왜 있는가 ────────────────────────────────────────────────────
 * 이 사이트는 낱장이 99,177개다(한 언어당 약 9,800 × 열 언어). 전에는
 * `output: "export"`로 그 전부를 빌드마다 새로 구웠는데, Vercel 무료 티어
 * (2코어 8GB · 45분)에서 감당이 안 됐다 — 컴파일에서 exit 137로 죽거나,
 * 넘겨도 생성만 38분이 걸려 제한에 닿는다.
 *
 * 그래서 만드는 **시점**을 옮겼다. 빌드에서는 정해진 수만큼만 만들고, 나머지는
 * 처음 열릴 때 만들어 CDN에 캐시한다. 두 번째 방문부터는 지금과 똑같다.
 * 페이지는 한 장도 없어지지 않는다 — `dynamicParams`가 기본으로 켜져 있어
 * 목록에 없는 낱장도 그대로 열린다.
 *
 * ── 값을 어떻게 잡는가 ───────────────────────────────────────────
 * 0이면 빌드에서 낱장을 한 장도 안 만든다(허브는 정적 라우트라 그대로 만들어진다).
 * 숫자를 주면 라우트마다 앞에서 그만큼 만든다. 목록의 앞쪽이 대개 대표 항목이라
 * (원소는 수소, 색은 빨강) 크롤러와 사람이 먼저 닿는 것들이 미리 구워진다.
 *
 * 환경변수로 조절한다 — 빌드 시간과 미리 굽는 양의 맞바꿈이라, 나중에 빌드
 * 머신이 커지면 코드를 안 고치고 숫자만 올리면 된다.
 *
 * ── 왜 0이 아니라 100인가 (2026-08-06) ──────────────────────────
 * 0으로 두었더니 **ISR 쓰기 한도**에 걸렸다. 무료 티어가 한 달 20만 번인데
 * 300%를 썼다는 연락이 왔다. 산수는 단순하다.
 *
 *   쓰기 = 크롤러가 훑은 낱장 수 × 배포 횟수
 *
 * 사이트맵이 115,157개를 내걸고 있고 배포하면 ISR 캐시가 차가워지므로,
 * 크롤러가 다시 훑는 만큼 다시 쓴다. 세 번 배포하니 그대로 34만이다.
 *
 * **빌드에서 구운 페이지는 정적이라 ISR 쓰기가 0이다.** 그래서 미리 굽는 만큼
 * 쓰기가 줄어든다. 100이면 61,694장(전체의 54%)이 정적이 되어 훑을 때마다
 * 드는 쓰기가 115,157에서 53,000쯤으로 준다.
 *
 * ── 100으로 올렸다가 되돌린 이유: 디스크 (2026-08-06) ────────────
 * 100으로 배포했더니 Vercel이 48,769/61,694장에서 **ENOSPC(디스크 없음)**로
 * 죽었다. 시간이 아니라 공간이 벽이었다.
 *
 * 페이지 한 장이 디스크에 남기는 것은 셋이다(로컬 실측, 61,694장 기준):
 *   .html      191KB   11.2GB
 *   .rsc       100KB    5.9GB   (HTML 안에 인라인된 것과 별개로 또 쓴다)
 *   .segments  210KB   12.9GB   (세그먼트별 프리페치, 한 장에 파일 열 개)
 *   합계       501KB   30.0GB
 *
 * 그 뒤 되풀이되는 Tailwind 클래스 묶음을 CSS 클래스로 빼서 한 장이
 * 501KB → 441KB가 됐다(HTML 191KB → 149KB). 그만큼 더 구울 수 있다.
 *
 * ── 24에서 20으로 낮췄다 (2026-08-07) ───────────────────────────
 * newsection을 두 번 머지해 동적 라우트가 730 → 930이 됐다. 같은 PR로도
 * 디스크가 따라 는다:
 *   라우트 730 → 8.3GB · 860 → 10GB · 930 → 11.1GB
 *
 * ENOSPC로 죽은 자리가 .next 약 23GB이고 Vercel이 산출물을 한 벌 더 복사하므로
 * PR=24는 22.2GB로 그 문턱에 닿는다. 낮췄을 때의 값은 이렇다(한 장 469KB 실측):
 *
 *   PR=24  11.1GB → 복사본까지 22.2GB   ISR 138,547  월 1.44바퀴
 *   PR=20   9.4GB → 복사본까지 18.9GB   ISR 142,267  월 1.41바퀴
 *
 * **디스크 안전을 사는 값이 크롤 0.03바퀴다.** 사이트맵이 163,367장이라 굽는
 * 장수를 몇 천 장 움직여도 ISR 쓰기는 2%도 안 바뀐다. 반대로 빌드가 죽으면
 * 배포 자체가 안 나간다. 그래서 20으로 둔다.
 *
 * 낱장을 더 줄이거나 빌드 머신이 커지면 그때 다시 올린다.
 *
 * ── 2026-08-06에 다시 잰 값 ──────────────────────────────────────
 * 낱장 한 장은 이제 **325KB**다(.html 118 + .rsc 61 + .segments 144).
 * 위에 적은 441KB는 낡은 값이었다 — 되풀이되는 클래스를 두 차례 CSS로 빼면서
 * 줄었고, 처음 잰 441은 허브 장이 섞인 표본이었다.
 *
 * PR=24에서 낱장 19,009장 · .next 8.3GB · 사이트맵 129,347장.
 * 그래서 크롤 한 바퀴에 드는 ISR 쓰기는 110,338이고, 월 20만이면 **1.8바퀴**다.
 *
 * 굽는 수를 올려도 크게 나아지지 않는다. 디스크 12GB가 상한이라 많아야
 * 38,000장(29%)이고, 그때 ISR은 91,329로 2.2바퀴다. 1.8 → 2.2를 위해
 * 디스크를 14GB까지 밀면 Vercel이 산출물을 한 벌 더 복사할 때 24GB에 닿는다.
 * 한 번 ENOSPC로 죽어 본 자리라 그 맞바꿈은 하지 않는다.
 *
 * **남은 수단은 배포를 모으는 것이다.** 배포할 때마다 캐시가 차가워져 크롤이
 * 처음부터 다시 쓴다. 한 달에 배포를 두 번 넘기지 않으면 한도 안이다.
 *
 * .segments를 끄면 한 장이 181KB가 되어 셈이 완전히 달라지지만, Next 16.2에는
 * 그 설정이 없다(config-shared.d.ts에 없고 cacheComponents: false로도 그대로다).
 *
 * 48,769장에서 죽었으니 빌드 컨테이너의 여유는 24GB쯤이다.
 * `cacheComponents: false`로 .segments를 끄려 해 봤지만 그대로 나온다 —
 * Next 16이 늘 만든다.
 *
 * 그래서 24로 잡는다. 19,962장 × 441KB = 8.4GB로, 죽은 자리의 35%다.
 * ── 2026-08-07: 0으로 되돌렸다 ─────────────────────────────────
 * Vercel 빌드가 45분 한도에 두 번 걸렸다(PR=20에서 한 번, 8에서 또 한 번).
 *
 * **원인 진단이 두 번 틀렸던 까닭:** "마지막으로 푸시한 커밋 = 마지막으로
 * 배포된 커밋"이라고 넘겨짚고 기준선을 잡았다. 라이브 사이트를 열어 보니
 * 실제로 성공한 마지막 배포는 2026-08-06 08:13(b36bc08b)이었고, **그때 값이
 * 바로 0이었다.** 그 뒤의 24·20·8은 한 번도 배포에 성공한 적이 없는 값이다.
 *
 * 로컬 실측(같은 워커 설정, cpus: 2):
 *   PR=8  10,116장 · 컴파일 2.9분 + 굽기 8.7분 = 전체 12분 45초 → Vercel 45분 초과
 *   PR=0   2,948장 · 컴파일 3.1분 + 굽기 4.4분 = 전체  8분 57초
 * 굽기가 시간의 대부분이고 컴파일은 라우트 수를 따라간다(지금 3,504개).
 *
 * **0은 바닥값이다 — 이보다 줄일 수 없다.** 그런데도 지난 성공 배포보다
 * 정적 라우트가 1,954 → 2,574로 32% 많아졌으니 여유가 얼마인지는 모른다.
 * 여기서 또 걸리면 숫자가 아니라 구조를 봐야 한다(라우트 수를 줄이거나,
 * 빌드를 나누거나, 한도가 긴 티어로 가거나).
 *
 * ── 2026-08-10: 낱장은 이제 캐시를 아예 안 쓴다 ─────────────────
 * 모든 [slug] page.tsx에 `export const dynamic = 'force-dynamic'`을 넣었다.
 * 낱장은 요청 때마다 그리고 **ISR 캐시에 쓰지 않는다.**
 *
 * 왜: ISR로 두면 배포마다 캐시가 비워져, 크롤러가 한 바퀴 돌 때마다
 * 사이트맵 163,730장만큼 쓰기가 든다(월 20만의 80% — 한 달에 배포 한 번이
 * 한계였고, 전에 300%를 넘겨 정지 경고까지 받았다). 사이트맵을 줄여 봤지만
 * 그건 노출을 포기하는 것이라 되돌렸다.
 *
 * 요청 때 그리는 값은 **다른 예산**에서 나간다 — 함수 실행(월 100만)과
 * CPU 시간. 크롤 한 바퀴 ≈ 16만 실행 = 16%. 대신 배포가 캐시를 비워도
 * 잃을 것이 없어져 **배포 횟수 제한이 사실상 사라진다.**
 *
 * 이 함수(굽는 목록)는 그래서 지금 비어 있어야 한다. **PR을 다시 올리려면
 * 그 라우트의 force-dynamic부터 걷어야 한다** — force-dynamic이 있으면
 * revalidate가 0으로 덮여 구운 것이 바로 낡은 것이 된다
 * (node_modules/next/dist/build/utils.js의 appConfig.revalidate = 0).
 *
 * **올리려면 먼저 배포가 성공하는 것을 보고 나서 올린다.** 검증 없이 올린
 * 값으로 두 번 실패했다. → tests/build-page-budget.test.ts
 *
 * ── 2026-08-10 같은 날, 진짜 벽은 컴파일이었다 ──────────────────
 * 위의 force-dynamic으로 ISR 쓰기는 풀렸는데 빌드는 계속 45분에 잘렸다.
 * `Compiled successfully`가 한 번도 안 찍혔다 — **굽기 이전에 컴파일이
 * 45분을 다 먹고 있었다.** 이 파일의 숫자로는 못 고치는 문제다.
 * 굽기는 장수를 따라가지만 컴파일은 **라우트 수**를 따라간다.
 *
 * 그래서 아홉 언어의 라우트 파일 3,015개를 접었다(라우트 3,504 → 1,335,
 * 컴파일 39초, 빌드 1분 43초). 지금 구조는 이렇다 —
 *   · 국제 **허브**는 언어마다 [[...path]] 캐치올 하나가 굽는다.
 *     여기 LIMIT과 무관하다 — lib/fold/registry.ts가 그 목록이다.
 *   · 국제 **낱장**과 한국어 낱장은 이 함수를 그대로 쓴다.
 * 자세한 것은 tests/fold-routes.test.ts 머리말.
 *
 * **이것만으로는 ISR 쓰기 한도가 편해지지 않는다.** 미리 굽는 것이 거의 없어
 * 한 번 훑을 때 사이트맵 전부(163,730장)를 쓴다 — 월 20만의 82%다.
 * 배포를 한 달에 한 번으로 모으는 것이 같이 가야 한다.
 *
 * ── 2026-08-13: 무료(Hobby)로 돌아가는 판 ───────────────────────
 * 위의 force-dynamic은 ISR 쓰기를 0으로 만드는 대신 **Fast Origin Transfer**를
 * 태웠다 — 매 요청 원본 전송·304 없음이라, 30일 10GB 한도의 348%까지 올라
 * 사이트가 멈췄고 어쩔 수 없이 유료(Pro)로 올렸다. 무료로 돌아가려면 두 벽을
 * 한꺼번에 지켜야 한다:
 *
 *   force-dynamic     전송이 샌다 — 매 요청 원본 전송, 304 없음 → 정지까지 갔다
 *   revalidate=86400  쓰기가 샌다 — 요청이 오는 장마다 하루 한 번 다시 쓴다
 *                     (크롤러가 매일 훑으면 한 바퀴 값 16만이 매일 든다)
 *   revalidate=false  둘 다 막는다 — 쓰기는 배포 뒤 첫 요청에만 들고,
 *                     재방문은 304(ETag)·캐시 HIT로 원본에 안 온다
 *
 * 그래서 낱장 전부(911개 + price-prediction)가 revalidate=false다. 낱장은 순수
 * 계산이라 내용이 빌드의 함수고, 고침은 어차피 배포로만 나가며 배포가 캐시를
 * 비우니 **신선도는 배포 주기가 정한다** — 기간을 둘 이유가 없다.
 *
 * 남는 쓰기 = 배포 횟수 × 그 사이 크롤러가 훑은 장수. 그래서 코드 밖의 둘이
 * 같이 간다:
 *   1) **배포를 모은다** — 커밋은 쌓고 푸시(=배포)는 주 1회쯤. 어차피 푸시는
 *      손으로 한다.
 *   2) **크롤 양 자체를 줄인다** — app/robots.ts가 학습·SEO 봇을 가리고,
 *      proxy.ts가 robots.txt를 무시하는 것에 403을 준다. 관측된 Edge 요청
 *      92만/월의 대부분이 이런 봇이었다면 남는 크롤은 검색 엔진 몫뿐이다.
 *
 * 아직 실측 못 한 미지수 둘(816fdf70 커밋 참고): 쓰기가 8KB 단위로 세어지면
 * 한 장이 22단위라 첫 훑기가 447만 단위일 수 있다는 것, 그리고 「내용이 같으면
 * 배포해도 쓰기가 없다」는 문서 문장이 실제로 그런지. **그래서 다운그레이드는
 * 재고 나서 한다** — Pro인 채로 이 판을 배포하고 Usage에서 ISR 쓰기·Origin
 * Transfer 추이를 며칠 본 뒤, 30일 추정치가 한도 안일 때 내린다.
 *
 * 시점 조심: Hobby에는 결제일이 없다. 한도는 **굴러가는 30일 창**이고 넘긴
 * 시점부터 30일 정지다. 이번 초과(348%)가 창에서 빠져나가기 전에 내리면 그
 * 자리에서 다시 정지를 맞을 수 있다 — 초과가 난 날부터 30일이 지나고, 창 안의
 * 사용량이 한도 아래로 내려온 것을 보고 내린다.
 */
const LIMIT = Number(process.env.PRERENDER_PER_ROUTE ?? '0');

/**
 * 검사에서는 자르지 않는다.
 *
 * "목록의 모든 항목이 페이지로 나가는가"를 보는 검사가 여럿이다. 그 검사들은
 * 이 함수를 거쳐 목록을 받는데, 여기서 잘라 버리면 전부 0을 보고 "페이지가
 * 없다"고 말한다 — 실제로는 요청 때 만들어지는데도 그렇다.
 *
 * node --test가 켜 주는 환경변수로 가른다. 빌드에는 없는 값이라 실제 빌드가
 * 잘리는 일은 없다.
 */
const IN_TEST = Boolean(process.env.NODE_TEST_CONTEXT);

/** 미리 만들 것만 남긴다. 빈 배열이어도 그 라우트는 정상으로 동작한다. */
export function prerender<T>(all: T[]): T[] {
  if (IN_TEST) return all;
  return LIMIT > 0 ? all.slice(0, LIMIT) : [];
}

/** 지금 몇 장씩 굽고 있는지 — 검사와 로그에서 쓴다 */
export const prerenderLimit = (): number => LIMIT;

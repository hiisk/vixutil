import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /*
   * ── output: "export"를 뗀 이유 (2026-08-05) ──────────────────────
   * 낱장이 99,177개(한 언어당 약 9,800 × 열 언어)가 되면서, 빌드마다 그 전부를
   * 굽는 방식이 무료 티어(2코어 8GB · 45분)에서 감당이 안 됐다. 컴파일에서
   * exit 137로 죽고, 넘겨도 생성만 38분이라 제한에 닿는다.
   *
   * 이제 빌드는 lib/prerender.ts가 정한 만큼만 굽고, 나머지는 처음 열릴 때
   * 만들어 CDN에 캐시한다(ISR). 페이지는 한 장도 없어지지 않는다 —
   * dynamicParams가 기본으로 켜져 있어 목록에 없는 낱장도 그대로 열린다.
   *
   * 값: 정적 파일 묶음이 아니게 되어 아무 호스팅에나 올릴 수 없고, 캐시에 없는
   * 첫 방문이 0.5~2초 걸린다. 그 대신 빌드가 몇 분에 끝난다.
   */

  /*
   * 타입 검사는 next build 안이 아니라 **그 앞에서 따로** 돌린다
   * (package.json의 "build": "tsc --noEmit && next build && …").
   *
   * 예전에 Vercel이 "Running TypeScript"에서 exit 137로 죽었다. tsc 자체는 냉간에서도
   * 1.3GB·17초밖에 안 쓰지만, next build가 3만 페이지를 컴파일한 힙을 그대로 쥔 채
   * 그 위에서 타입 검사를 시작해서 봉우리가 겹쳤다. 앞으로 빼니 최대 사용량이
   * 합이 아니라 둘 중 큰 쪽이 되면서 그 자리는 통과했다.
   *
   * 검사를 건너뛰는 것이 아니다. 여기서 끄지 않으면 같은 일을 두 번 하면서
   * 두 번째는 가장 나쁜 시점에 한다.
   */
  typescript: { ignoreBuildErrors: true },

  /*
   * 소스맵은 빌드 중에만 메모리를 먹고 정적 내보내기 결과물에는 쓰이지 않는다.
   * 기본값도 false지만 페이지가 3만 장이면 이 한 줄이 지켜 주는 양이 크므로
   * 명시해 둔다 — 누가 디버깅하려고 켰다가 잊는 일을 막는다.
   */
  productionBrowserSourceMaps: false,

  experimental: {
    /*
     * ── exit 137(컨테이너 OOM)의 원인 ──────────────────────────
     * node는 힙 상한을 **시스템 전체 메모리**를 보고 정한다. 8GB 기계에서 재 보면
     * 프로세스당 4,192MB다. 워커가 넷이면 허용된 힙만 16.8GB — 컨테이너가 가진
     * 8GB의 두 배다.
     *
     * 로컬(macOS)에서는 메모리 압축과 스왑이 봉우리를 받아 줘서 2GB 근처에서
     * 끝난다. Vercel의 리눅스 컨테이너는 스왑이 없고 cgroup 상한이 딱딱해서,
     * 워커 몇 개가 조금만 위로 흘러도 커널이 죽인다. 로그에서 본 자리가 거기다.
     *
     * 그래서 세 가지를 같이 건다.
     *   1) 워커 수를 둘로 — 아래 cpus
     *   2) 워커 하나가 동시에 그리는 장수를 넷으로 — 아래 staticGenerationMaxConcurrency
     *   3) 힙 상한을 명시 — package.json의 NODE_OPTIONS=--max-old-space-size=1536
     *
     * 2번을 빠뜨려서 한 번 더 죽었다. 워커만 줄였더니 "Collecting page data"는
     * 통과했지만 "Generating static pages" 24,233/36,812에서 또 137이 났다.
     * staticGenerationMaxConcurrency의 기본값이 8이라, 워커를 반으로 줄여도
     * 워커마다 여전히 8장을 동시에 그린다 — 2×8로 16장이 한꺼번에 메모리에 뜬다.
     * 워커 수는 그 배수의 한쪽일 뿐이었다.
     *
     * 3번은 실측으로 정한다. 로컬에서 가장 바쁜 워커가 536MB, 주 프로세스가
     * 868MB였다. 1536MB면 넉넉하고, 최악이라도 (주+워커 둘)×1.5GB ≈ 4.5GB로
     * 8GB 안에 실제 여유가 남는다. 2048은 너무 후했다.
     *
     * ── 2026-08-07: 1536이 이제 모자란다 ─────────────────────────
     * 라우트가 2,614 → 3,504개가 되면서 주 프로세스가 상한을 다 쓴다.
     * **모자라면 죽는 대신 GC가 계속 돌며 느려지기만 한다** — 로컬은 메모리
     * 압축이 받아 주지만 Vercel 컨테이너는 스왑이 없어 그대로 맞는다.
     *
     * 같은 기계에서 상한만 바꿔 재 봤다(PR=0, 2,948장):
     *   1,536MB  8분 57초  최대 RSS 1,930MB  ← 상한을 넘겼다. 포화 상태다
     *   2,560MB  8분 28초  최대 RSS 2,707MB
     *   3,072MB  7분 29초                    ← 16% 단축, 굽기만 23%
     * 2,560은 거의 안 줄었고 3,072에서 확 준다. 작업 집합이 3GB 근처라는 뜻이다.
     *
     * 그래서 package.json의 값을 3072로 올렸다. 실제 사용량은 주 프로세스
     * 3.2GB + 워커 둘(측정 536MB) ≈ 4.4GB로 8GB 안에 여유가 남는다.
     * **exit 137이 다시 나면 2,560으로 내린다** — 그 값도 위에 재 두었다.
     */
    cpus: 2,
    memoryBasedWorkersCount: true,

    /*
     * 워커 하나가 동시에 그리는 페이지 수. 기본값 8은 페이지가 3만 장이고
     * 컨테이너가 8GB인 이 사이트에는 과하다 — 동시에 그리는 만큼 렌더 트리가
     * 한꺼번에 살아 있는다. cpus와 곱해져서 실제 봉우리를 만드는 값이라,
     * 워커만 줄이고 이걸 두면 절반밖에 안 줄어든다.
     */
    staticGenerationMaxConcurrency: 4,

    /*
     * 이 줄은 지금 아무 일도 하지 않는다 — Next 16의 next build는 Turbopack만
     * 쓰고(webpack 플래그가 없어졌다) 이 설정은 웹팩 전용이다. 지우지 않는 것은
     * 웹팩으로 돌아갈 길이 생기면 다시 필요해서다.
     */
    webpackMemoryOptimizations: true,

    /*
     * ── 컴파일 단계에서 해 보고 접은 것 두 가지 (2026-08-05) ──────
     * 새 섹션 서른둘을 합쳐 라우트 엔트리가 4,546개가 되자 컴파일 도중
     * exit 137이 났다. 아래 둘을 실측해 봤고, **둘 다 안 통했다.**
     *
     *   turbopackMemoryLimit: 4GB
     *     → Vercel에서 6분 반에 죽던 것이 37분을 헤매다 죽었다. 캐시를 비웠다
     *       다시 계산하기를 되풀이한다. 상한은 목표치라 딱 잘리지도 않는다.
     *
     *   deferredEntries: 새 섹션 640 라우트를 뒤로 미룸
     *     → 최대 RSS 2,888MB → 2,943MB(안 줄었다), 컴파일 3.4분 → 6.1분.
     *       봉우리가 갈리는 것이 아니라 두 번 세우기만 한다.
     *
     * 실측한 컴파일 사용량은 로컬 1.8GB, CI 흉내 2.9GB다. 3GB면 되는 일이
     * 8GB 컨테이너에서 죽는다는 뜻이라, 남은 설명은 Turbopack이 컨테이너가
     * 아니라 **호스트를 보고** 캐시와 스레드를 잡는다는 쪽이다
     * (위 cpus 주석의 node 힙과 같은 함정).
     *
     * 그 쪽이 맞았다 — 다만 고칠 자리가 이 파일이 아니었다. 아래 RAYON_NUM_THREADS
     * 문단을 볼 것.
     *
     * 같은 것을 또 시도하지 않도록 숫자와 함께 남긴다.
     */

    /*
     * ── Vercel에서는 빌드 캐시를 지우고 차게 시작한다 (2026-08-10) ──────
     * 이 설정이 아니라 package.json build 스크립트 맨 앞의 rmSync 한 줄이
     * 하는 일이다. 설명 둘 자리가 JSON에는 없어서 여기 적는다.
     *
     * 빌드가 45분 한도에 세 번 잘렸고, 세 번째는 컴파일이 45분을 통째로
     * 먹었다(Compiled successfully가 끝내 안 찍혔다). 로컬에서는 같은
     * 컴파일이 3.8분이다 — 성능 차(tsc 실측 2.3배)로는 설명이 안 된다.
     *
     * 세 실패 로그의 첫머리가 같은 줄이었다:
     *   Restored build cache from previous deployment (Gmdsu71N…)
     * 실패한 빌드는 캐시를 저장하지 않으므로, 세 번 다 **8월 6일 마지막
     * 성공 빌드의 캐시**를 복원해 시작한 것이다. 그 사이 저장소는 2,304개
     * 파일·라우트 +30%가 달라졌다. Turbopack이 그만큼 어긋난 캐시를 8GB
     * 컨테이너 안에서 대조하다 무효화→재계산을 되풀이한다 — 위
     * turbopackMemoryLimit 실측에서 본 "캐시를 비웠다 다시 계산"과 같은
     * 무늬다. 로컬은 매번 .next를 지우고 차게 빌드해서 재현이 안 됐다.
     *
     * 그래서 Vercel(VERCEL=1)에서만 복원된 .next를 지우고 시작한다.
     * 콜드 빌드는 로컬 8.5분 × 2.3 ≈ 20~30분으로 한도 안이다.
     * 값을 치르는 것: Vercel 증분 빌드를 포기한다(매번 콜드). 배포가
     * 다시 안정되면 이 줄을 빼고 신선한 캐시로 증분을 되살릴 수 있다.
     */
    /*
     * ── RAYON_NUM_THREADS: 컴파일이 31분을 헤매다 죽던 이유 (2026-08-05) ──
     * 이 설정으로는 못 거는 값이라 package.json의 build 스크립트에 있다.
     * 여기에 적는 것은 그 하나뿐인 설명을 둘 자리가 JSON에는 없어서다.
     *
     * Turbopack의 네이티브 부분(@next/swc)은 rayon-core 1.12.1로 병렬 처리를
     * 한다. rayon은 스레드 수를 RAYON_NUM_THREADS → RAYON_RS_NUM_CPUS →
     * available_parallelism() 순으로 정하는데, Next는 앞의 둘을 스스로 걸지
     * 않는다(node_modules를 뒤져 확인). 그래서 실제로는 마지막 것이 쓰인다.
     *
     * 빌드 컨테이너에 CPU 쿼터가 안 걸려 있으면 available_parallelism()은
     * 할당받은 2가 아니라 **호스트의 코어 수**를 돌려준다. 2코어 위에서 수십
     * 스레드가 서로 밀치면서, 스레드마다 자기 몫의 작업 메모리를 든다.
     * Vercel 로그에서 본 모습이 정확히 그것이다 — 로컬에서 3.3분에 끝나는
     * 컴파일이 31분을 헤매다 SIGKILL.
     *
     * 실측(CI 흉내, 8코어 기계):
     *   안 걸었을 때  최대 RSS 2,912MB · 전체 9.2분
     *   2로 걸었을 때 최대 RSS 2,024MB · 전체 11.0분   ← 메모리 30% 감소
     *
     * 시간을 조금 내주고 봉우리를 깎는 맞바꿈이다. 죽는 쪽이 시간이 아니라
     * 메모리이므로 그 값이 맞다.
     *
     * 기본값 2는 무료 티어에 맞춘 것이고, 밖에서 덮어쓸 수 있게 열어 뒀다
     * (`${RAYON_NUM_THREADS:-2}`). 빌드 머신이 커지면 숫자만 올린다.
     */

    /* 서버 소스맵도 같은 이유로 끈다 — 빌드 중에만 메모리를 먹는다 */
    serverSourceMaps: false,

    /*
     * 켜져 있으면 페이지 모듈 전부를 시작 시점에 메모리로 올린다. page.tsx가
     * 1,900장이라 그 선행 적재가 그대로 최대 사용량이 된다. 첫 응답이 느려지는
     * 대신 메모리를 아끼는 맞바꿈인데, 정적 내보내기라 첫 응답 속도는 무관하다.
     */
    preloadEntriesOnStart: false,
  },
  /*
   * 옛 사이트맵 주소를 살려 둔다.
   *
   * 2026-08-10에 사이트맵을 조각으로 나누면서 /sitemap.xml이 사라졌는데,
   * 구글과 빙은 이미 그 주소를 등록해 두고 주기적으로 찾아온다. 404를 내면
   * "사이트맵이 없어졌다"로 읽히므로, 같은 자리에서 묶음 목록을 그대로 낸다.
   * 리다이렉트가 아니라 rewrite인 것은 크롤러가 한 번에 내용을 받게 하려는 것이다.
   */
  async rewrites() {
    return [{ source: '/sitemap.xml', destination: '/sitemap-index.xml' }];
  },

};

export default nextConfig;
